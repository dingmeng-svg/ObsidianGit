---
title: "🌅 2026-08-18 洞察日报"
tags: [洞察, 系统文档]
date: 2026-08-18
created: 2026-08-18
updated: 2026-08-18
---

# 🗓️ 2026-08-18 星期二

## 🌐 观全局

**全库扫描。** 近 7 天原子笔记活跃 **31 篇**，BuJo 日记活跃 **3 篇**；Inbox 非 `_raw` 待处理 **6 篇**，`_raw` 待编译 **0 篇**；40_Archive 累计 **435 篇 Markdown 文件**。

```dataviewjs
function renderGreenBar(label, pctOrRank, displayText = null, useRankMode = false) {
  let c = "#c6e48b";
  if (useRankMode) { if (pctOrRank > .8) c="#196127"; else if (pctOrRank > .6) c="#2e8840"; else if (pctOrRank > .4) c="#49af5d"; else if (pctOrRank > .2) c="#7bc96f"; }
  else { if (pctOrRank > 60) c="#196127"; else if (pctOrRank > 45) c="#2e8840"; else if (pctOrRank > 30) c="#49af5d"; else if (pctOrRank > 15) c="#7bc96f"; }
  const t=displayText===null?`${pctOrRank}%`:displayText;
  return `<div style="display:flex;align-items:center;gap:12px;margin-bottom:10px"><span style="min-width:150px;text-align:right;color:#4B5563">${label}</span><div style="flex:1;max-width:460px;height:8px;background:#F3F4F6;border-radius:99px;overflow:hidden"><div style="width:${useRankMode?pctOrRank*100:pctOrRank}%;height:100%;background:${c}"></div></div><span style="min-width:50px;text-align:right;color:#4B5563;font-weight:600">${t}</span></div>`;
}
const items=[{label:"原子笔记 7d活跃",pct:100,val:"31 篇"},{label:"BuJo日记 7d活跃",pct:10,val:"3 篇"},{label:"Inbox待处理",pct:19,val:"6 篇"},{label:"_raw待编译",pct:0,val:"0 篇"},{label:"归档累计",pct:100,val:"435 篇"}];
let html="<div style='margin:1rem 0'>"; items.forEach(d=>html+=renderGreenBar(d.label,d.pct,d.val)); html+="</div>"; dv.paragraph(html);
```

**判断**：输出端仍有活动，但日记仅覆盖 3 天；输入端的 6 篇 Inbox 文件需要分流。`_raw` 已清空，当前不是素材编译积压，而是根目录 Inbox 与知识沉淀节奏需要收敛。

## 🌊 察潜流

全库共 564 篇原子笔记：无 `type` 284 篇、concept 141 篇、practice 123 篇、model 12 篇、critique 3 篇、moc 1 篇。

```dataviewjs
function renderGreenBar(label, pctOrRank, displayText = null, useRankMode = false) {
  let c="#c6e48b"; if(pctOrRank>60)c="#196127";else if(pctOrRank>45)c="#2e8840";else if(pctOrRank>30)c="#49af5d";else if(pctOrRank>15)c="#7bc96f";
  return `<div style="display:flex;align-items:center;gap:12px;margin-bottom:10px"><span style="min-width:150px;text-align:right;color:#4B5563">${label}</span><div style="flex:1;max-width:460px;height:8px;background:#F3F4F6"><div style="width:${pctOrRank}%;height:100%;background:${c}"></div></div><span style="min-width:50px;text-align:right">${displayText||pctOrRank+"%"}</span></div>`;
}
const data=[{label:"无 type",n:284},{label:"concept 概念",n:141},{label:"practice 实践",n:123},{label:"model 模型",n:12},{label:"critique 批判",n:3},{label:"moc 知识地图",n:1}]; const m=284; let h=""; data.forEach(d=>h+=renderGreenBar(d.label,Math.round(d.n/m*100),d.n+" 篇")); dv.paragraph(h);
```

284 篇缺少 type，是最大的结构性噪声。concept 与 practice 合计 264 篇，输入与实践接近；但 moc 仅 1 篇、critique 仅 3 篇，知识地图与批判性回看明显不足。近 7 天标签以「心理学」3 次、「个人成长」1 次为主。

## 🪸 拂遗珠

- **Inbox 待处理 6 篇**：`00_Inbox/00_Inbox.md`、`Collector.md`、`_raw.md`、`外部导入.md`、`爱和信任，都是一种珍贵的心理资源.md`、`📘 操盘手英语 · A to Z 核心金融词表.md`。前四篇已放置 22–34 天，后两篇最近 4 天有更新。
- **_raw 待编译**：0 篇；目录无 Markdown 素材。
- **近 90 天未修改且 active**：0 篇。
- **ai_generated 待审核**：本轮扫描未检出可明确归入待审核的 `ai_generated` 项，不虚构领域数量。
- **critique / moc**：3 篇 / 1 篇，分别占原子笔记 0.5% / 0.2%。

最具体的遗珠是 Inbox 中超过 14 天的系统入口文件与两篇内容素材，而不是 `_raw`。

## 🔦 照暗面

近 30 天 Inbox 有 3 个文件更新，同期原子笔记有 **345 篇**更新，数量上并非「只收不产」。阻力在于项目未完成事项与 Inbox 长期滞留：

- **操盘手骑士**：92 篇，检测到未完成任务 73 项，近 30 天活跃 42 篇。
- **知→行→进化**：33 篇，检测到未完成任务 32 项，近 30 天活跃 9 篇。
- **100 Word Tales｜百词百篇**：108 篇，未检测到未完成任务，近 30 天无更新。
- **大脑盲盒**：2 篇，未检测到未完成任务，近 30 天无更新。

**判定**：🟡 有阻力。内容编辑活跃，但两个核心项目仍挂着 73 项与 32 项未完成任务。

## 🍃 辨风势

近 7 天标签热度：心理学 3 篇、个人成长 1 篇。全库标签存量前三：语言 126、概念 89、个人成长 71；心理学 66、知识管理 33、健身 32。

```dataviewjs
function renderGreenBar(label, pctOrRank, displayText = null, useRankMode = false) {
  let c=pctOrRank>.8?"#196127":pctOrRank>.6?"#2e8840":pctOrRank>.4?"#49af5d":pctOrRank>.2?"#7bc96f":"#c6e48b";
  return `<div style="display:flex;align-items:center;gap:12px;margin-bottom:10px"><span style="min-width:150px;text-align:right">${label}</span><div style="flex:1;max-width:460px;height:8px;background:#F3F4F6"><div style="width:${pctOrRank*100}%;height:100%;background:${c}"></div></div><span style="min-width:50px;text-align:right">${displayText}</span></div>`;
}
const d=[{label:"心理学",n:3},{label:"个人成长",n:1}]; let h=""; d.forEach((x,i)=>h+=renderGreenBar(x.label,(d.length-i-1)/(d.length-1),x.n+" 篇",true)); dv.paragraph(h);
```

**趋势判断**：风向集中在心理学与个人成长，但样本仍小；应把已有主题转成 practice 或 critique，而不是继续扩张输入。

## 🌡️ 测热力

以下为 Heatmap Calendar 渲染的每日活跃度分布（色块越深表示当天修改笔记越多）：

```dataviewjs
try {
  const pages=dv.pages('"20_Areas/A2_能力接口/Bullet Journal" or "00_Inbox" or "10_Projects" or "20_Areas" or "30_Resources"').where(p=>p.file.day);
  const map={}; pages.forEach(p=>{const k=p.file.day.toFormat("yyyy-MM-dd");map[k]=(map[k]||0)+1;});
  renderHeatmapCalendar(this.container,{colors:"default",entries:Object.entries(map).map(([date,intensity])=>({date,intensity}))});
} catch(e) { dv.paragraph("⚠️ Heatmap Calendar 插件未启用或渲染失败。"); }
```

### 近 30 天注意力分布

```dataviewjs
function renderGreenBar(label, pctOrRank, displayText = null, useRankMode = false) {
  let c="#c6e48b"; if(pctOrRank>60)c="#196127";else if(pctOrRank>45)c="#2e8840";else if(pctOrRank>30)c="#49af5d";else if(pctOrRank>15)c="#7bc96f";
  return `<div style="display:flex;align-items:center;gap:12px;margin-bottom:10px"><span style="min-width:150px;text-align:right">${label}</span><div style="flex:1;max-width:460px;height:8px;background:#F3F4F6"><div style="width:${pctOrRank}%;height:100%;background:${c}"></div></div><span style="min-width:50px;text-align:right">${displayText}</span></div>`;
}
const pages=dv.pages('"20_Areas/A2_能力接口/Bullet Journal" or "00_Inbox" or "10_Projects" or "20_Areas" or "30_Resources"').where(p=>p.file.day); const m={}; pages.forEach(p=>{if(p.file.day>=dv.date("2026-07-20")){const k=p.file.day.toFormat("MM-dd");m[k]=(m[k]||0)+1;}}); const mx=Math.max(1,...Object.values(m)); let h=""; Object.entries(m).sort().forEach(([k,n])=>h+=renderGreenBar(k,Math.round(n/mx*100),n+" 篇")); dv.paragraph(h);
```

近 7 天 BuJo 活跃集中在 8 月 12–14 日，共 3 篇；原子笔记有 31 篇活跃。注意力偏向知识库编辑，日志连续性落后于内容编辑。

## 🎯 践一诺

**唯一承诺：在下一次工作时段，完成 `00_Inbox/爱和信任，都是一种珍贵的心理资源.md` 的分流：编译为 1 篇带 type 的原子笔记，或明确归档，并同步清理其余 5 篇 Inbox 根目录入口文件。**

依据：Inbox 有 6 篇，其中 4 篇滞留 22–34 天；近 7 天原子笔记已活跃 31 篇，但 284 篇仍无 type、moc 仅 1 篇。先完成一篇真实分流，比继续增加输入更能修复入口—沉淀断点。

> [!note] 唯一承诺
> 本周洞察收敛为唯一可执行承诺。多则失焦，少则无力。
>
> **约束**：此区块仅保留 1 条最高优先级行动项。多个候选须在 `## 察潜流` 或 `## 辨风势` 中完成筛选，只有胜出者方可进入此处。

*本报告由 Hermes Agent 于 2026-08-18 22:01 自动生成。*
