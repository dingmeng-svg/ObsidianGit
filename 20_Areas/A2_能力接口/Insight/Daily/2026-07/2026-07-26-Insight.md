---
title: "Insight · Daily — 2026-07-26"
date: 2026-07-26
tags: [洞察, 系统文档]
aliases: [2026-07-26-Insight]
---

## 🗓️ 2026-07-26 周日·知识洞察

```dataviewjs
const renderGreenBar = (label, pct, displayText = null, useRankMode = false) => {
    let barColor = "#c6e48b";
    const val = useRankMode ? pct : pct;
    if (useRankMode) {
        if (val > 0.8) barColor = "#196127";
        else if (val > 0.6) barColor = "#2e8840";
        else if (val > 0.4) barColor = "#49af5d";
        else if (val > 0.2) barColor = "#7bc96f";
        else barColor = "#c6e48b";
    } else {
        if (val <= 15) barColor = "#c6e48b";
        else if (val <= 30) barColor = "#7bc96f";
        else if (val <= 45) barColor = "#49af5d";
        else if (val <= 60) barColor = "#2e8840";
        else barColor = "#196127";
    }
    const rightText = displayText !== null ? displayText : `${Math.round(pct)}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${useRankMode ? (val * 100) : val}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
};

const items = [
    { label: "原子笔记", pct: 100, val: "534 篇" },
    { label: "近 7 天修改", pct: 58, val: "312 篇" },
    { label: "BuJo 近 7 天", pct: 100, val: "7/7 天" },
    { label: "归档积累", pct: 70, val: "375 篇" },
    { label: "外部导入待处理", pct: 58, val: "7 篇" },
    { label: "_raw 待编译", pct: 0, val: "0 篇" },
];
let html = `<div style="margin:1rem 0;font-family:-apple-system,sans-serif;">`;
items.forEach(d => {
    html += renderGreenBar(d.label, d.pct, d.val, false);
});
html += `</div>`;
dv.paragraph(html);
dv.paragraph("> **判断**：知识库总量稳定，近7天原子笔记修改活跃（312篇），BuJo连续每日记录。_raw已清零，外部导入积压7篇待处理。整体处于「输入端积压 + 产出端活跃」状态。");
```

## 🌐 观全局

**全库扫描**结果：

| 指标 | 数值 | 状态 |
|:---|---:|:---|
| 原子笔记总数 | 534 篇 | — |
| 近 7 天原子笔记修改 | 312 篇 | 🔥 活跃 |
| BuJo 近 7 天活跃 | 7/7 天 | ✅ 连续每日记录 |
| 归档积累 | 375 篇 | — |
| 外部导入待处理 | 7 篇 | ⏳ 待处理 |
| _raw 待编译 | 0 篇 | ✅ 已清零 |
| MOC 知识地图 | 17 篇 | ⚠️ 仅占笔记总量 3% |

**判断**：本周原子笔记修改活跃（312篇），日均约45篇修改。_raw保持清零。7篇外部导入待处理，其中金融语料类占多数。MOC总数17篇，知识地图建设处于起步阶段。

---

## 🌊 察潜流

**全库 type 分布**（降序）：

| type | 数量 | 占比 |
|:---|---:|---:|
| practice | 123 | 46.2% |
| concept | 111 | 41.7% |
| model | 12 | 4.5% |
| critique | 3 | 1.1% |
| moc | 1 | 0.4% |

**判断**：practice（实践型）占主导 46%，concept（概念型）紧随其后 42%，两者合计占 88%。这是一个「知行双轮」的均衡结构。但 critique（批判型）仅 3 篇（1.1%）——批判性思考和知识地图建设是明显短板。moc 在 type 字段仅标记 1 篇（独立 MOC 文件有 17 篇，但未在原子笔记中标记 type）。

---

## 🪸 拂遗珠

### _raw 已全部编译 ✅

6 篇 _raw 素材均已标记 `ingest_status: ingested`。

### 外部导入待处理（7 篇）

| 文件 | 大小 | 导入时间 |
|:---|---:|:---|
| market language chunk.md | 11KB | 7月24日 |
| stock language chunk.md | 4KB | 7月24日 |
| 纯金融高频词表.md | 34KB | 7月25日 |
| 知→行→进化 分镜台本.md | 32KB | 7月20日 |
| 词汇表.md | 23KB | 7月22日 |
| 金融语料库.md | 250KB | 7月21日 |
| 富贵包矫正 · 唤醒期（W1-W4）每日执行计划.md | ~5KB | 7月26日 |

**建议**：优先编译 `market language chunk.md`（11KB）和 `stock language chunk.md`（4KB），与已有 Language Chunk 体系直接衔接。

### ai_generated 待审核（7 篇）

| 领域 | 数量 |
|:---|---:|
| 关系 | 4 篇 |
| 生活 | 3 篇 |

**建议**：关系领域占比最高（4/7），建议优先审核——关系类笔记涉及人际实践，AI 生成内容需要人工校准。

### Inbox 超期检查

无放置超过 14 天的文件 ✅

---

## 🔦 照暗面

### critique 仅 3 篇，批判性思考严重不足

全库 534 篇原子笔记中，critique 类型仅 3 篇（0.6%）。知识库以接受性学习为主，缺乏批判性审视。建议每月新建至少 1-2 篇 critique 笔记。

### MOC 知识地图建设薄弱

独立 MOC 文件 17 篇（占全库 3%），覆盖不到主要领域的 1/3。没有知识地图，原子笔记之间难以形成结构化关联，知识检索和温习效率下降。

### 关系领域 4 篇 ai_generated 无人审核

关系领域的 AI 生成笔记全部缺少人工审校。AI 在人际关系这种高度情境化的主题上容易输出泛化建议，建议尽快完成人工校准。

---

## 🍃 辨风势

```dataviewjs
const renderGreenBar = (label, pct, displayText = null, useRankMode = false) => {
    let barColor = "#c6e48b";
    const val = useRankMode ? pct : pct;
    if (useRankMode) {
        if (val > 0.8) barColor = "#196127";
        else if (val > 0.6) barColor = "#2e8840";
        else if (val > 0.4) barColor = "#49af5d";
        else if (val > 0.2) barColor = "#7bc96f";
        else barColor = "#c6e48b";
    } else {
        if (val <= 15) barColor = "#c6e48b";
        else if (val <= 30) barColor = "#7bc96f";
        else if (val <= 45) barColor = "#49af5d";
        else if (val <= 60) barColor = "#2e8840";
        else barColor = "#196127";
    }
    const rightText = displayText !== null ? displayText : `${Math.round(pct)}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${useRankMode ? (val * 100) : val}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
};

const tagsData = [
    { label: "语言", count: 126 },
    { label: "概念", count: 89 },
    { label: "个人成长", count: 71 },
    { label: "心理学", count: 63 },
    { label: "知识管理", count: 33 },
    { label: "健身", count: 32 },
    { label: "模型", count: 23 },
    { label: "关系", count: 19 },
    { label: "生活", count: 18 },
    { label: "技术", count: 16 },
];
const totalTags = tagsData.length;
let tagHtml = `<div style="margin:1rem 0;font-family:-apple-system,sans-serif;">`;
for (let i = 0; i < totalTags; i++) {
    const d = tagsData[i];
    const rankPct = (totalTags - i - 1) / (totalTags - 1);
    tagHtml += renderGreenBar(d.label, rankPct, `${d.count} 篇`, true);
}
tagHtml += `</div>`;
dv.paragraph(tagHtml);
```

**趋势判断**：语言（126篇）持续领跑全库，概念（89篇）和个人成长（71篇）分列二三。心理学（63篇）和知识管理（33篇）构成第二梯队。健身（32篇）和关系（19篇）作为生活实践类稳步积累。技术仅16篇——作为数字知识工作者，技术类笔记偏少值得关注。

---

## 🌡️ 测热力

以下为 Heatmap Calendar 渲染的每日活跃度分布（色块越深表示当天修改笔记越多）：

```dataviewjs
try {
  const pages = dv.pages('"20_Areas/A2_能力接口/Bullet Journal" or "00_Inbox" or "10_Projects" or "20_Areas" or "30_Resources"')
    .where(p => p.file.day);
  const countMap = {};
  pages.forEach(p => {
    const key = p.file.day.toFormat("yyyy-MM-dd");
    countMap[key] = (countMap[key] || 0) + 1;
  });
  const entries = Object.entries(countMap).map(([date, count]) => ({ date: date, intensity: count }));
  renderHeatmapCalendar(this.container, { colors: "default", entries: entries });
} catch(e) {
  dv.span("⚠️ Heatmap Calendar 插件未启用，请安装后查看热力图");
}
```

**近 30 天注意力分布概览**（按近7天各目录活跃文件数排序）：

```dataviewjs
const renderGreenBar = (label, pct, displayText = null, useRankMode = false) => {
    let barColor = "#c6e48b";
    const val = useRankMode ? pct : pct;
    if (useRankMode) {
        if (val > 0.8) barColor = "#196127";
        else if (val > 0.6) barColor = "#2e8840";
        else if (val > 0.4) barColor = "#49af5d";
        else if (val > 0.2) barColor = "#7bc96f";
        else barColor = "#c6e48b";
    } else {
        if (val <= 15) barColor = "#c6e48b";
        else if (val <= 30) barColor = "#7bc96f";
        else if (val <= 45) barColor = "#49af5d";
        else if (val <= 60) barColor = "#2e8840";
        else barColor = "#196127";
    }
    const rightText = displayText !== null ? displayText : `${Math.round(pct)}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${useRankMode ? (val * 100) : val}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
};

const attentionData = [
    { label: "Resources (原子笔记)", pct: 100, val: "315 篇" },
    { label: "Areas (领域)", pct: 46, val: "144 篇" },
    { label: "Archive (归档)", pct: 18, val: "57 篇" },
    { label: "Projects (项目)", pct: 9, val: "28 篇" },
    { label: "Inbox (收件箱)", pct: 3, val: "8 篇" },
];
let attnHtml = `<div style="margin:1rem 0;font-family:-apple-system,sans-serif;">`;
attentionData.forEach(d => {
    attnHtml += renderGreenBar(d.label, d.pct, d.val, false);
});
attnHtml += `</div>`;
dv.paragraph(attnHtml);
dv.paragraph("**注意力分布判断**：Resources（原子笔记）是本周最活跃区域（315篇），Areas领域（144篇）中A2_能力接口（Atomic/Bullet/Insight）和A3_精力底盘（健身/食物）有活跃更新。Projects领域（28篇）中工作项目（15篇）活跃度高于个人项目（7篇）。Inbox活跃度最低（8篇），收件箱清理效果好。");
```

---

## 🎯 践一诺

**从外部导入中选取 1 篇完成编译入库，并新建 1 篇 critique 类原子笔记，平衡知识的输入与批判性审视。**

优先选择 `market language chunk.md`（11KB，与 Language Chunk MOC 直接衔接），完成后新建一篇 critique 笔记，对近期阅读的某个观点进行批判性分析。

> [!note] 唯一承诺
> 本周洞察收敛为唯一可执行承诺。多则失焦，少则无力。
>
> **约束**：此区块仅保留 1 条最高优先级行动项。多个候选须在 `## 察潜流` 或 `## 辨风势` 中完成筛选，只有胜出者方可进入此处。

---

*本报告由 Hermes Agent 于 2026-07-26 21:07 自动生成。*
