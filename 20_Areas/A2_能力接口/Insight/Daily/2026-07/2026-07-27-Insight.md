---
title: "Insight · Daily — 2026-07-27"
date: 2026-07-27
tags: [洞察, 系统文档]
aliases: [2026-07-27-Insight]
---

## 🗓️ 2026-07-27 周一·知识洞察

```dataviewjs
const renderGreenBar = (label, pct, displayText = null, useRankMode = false) => {
    let barColor = "#F4FCF6";
    let targetVal = pct;
    if (useRankMode) {
        if (targetVal > 0.8) barColor = "#196127";
        else if (targetVal > 0.6) barColor = "#2e8840";
        else if (targetVal > 0.4) barColor = "#49af5d";
        else if (targetVal > 0.2) barColor = "#7bc96f";
        else barColor = "#c6e48b";
    } else {
        if (targetVal <= 15) barColor = "#c6e48b";
        else if (targetVal <= 30) barColor = "#7bc96f";
        else if (targetVal <= 45) barColor = "#49af5d";
        else if (targetVal <= 60) barColor = "#2e8840";
        else barColor = "#196127";
    }
    let rightText = displayText !== null ? displayText : `${pct}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${useRankMode ? (targetVal * 100) : targetVal}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
};

// 今日数据采集
const today = dv.date("2026-07-27");
const weekStart = today.startOf('week');
const weekEnd = today.endOf('week');

// 本周日记数
const dailyLogs = dv.pages('"20_Areas/A2_能力接口/Bullet Journal/Daily"')
    .where(p => p.file.day && p.file.day >= weekStart && p.file.day <= weekEnd);

// 习惯完成率
let totalHabits = 0, completedHabits = 0;
for (let p of dailyLogs) {
    if (p.file.tasks) {
        const habits = p.file.tasks.filter(t => t.text.includes("[habit::"));
        totalHabits += habits.length;
        completedHabits += habits.filter(t => t.completed).length;
    }
}
const habitRate = totalHabits > 0 ? Math.round(completedHabits / totalHabits * 100) : 0;

// 本周原子笔记新增
const notes = dv.pages('"30_Resources/31_Atomic_Notes"')
    .where(p => p.file.cday && p.file.cday >= weekStart && p.file.cday <= weekEnd);

// _raw 待编译数
const rawPending = dv.pages('"00_Inbox/_raw"')
    .where(p => p.ingest_status !== "ingested" && p.ingest_status !== "archived").length;

const items = [
    { label: "习惯投票率", pct: habitRate, displayText: `${habitRate}% (${completedHabits}/${totalHabits})` },
    { label: "原子笔记新增", pct: Math.min(notes.length * 10, 100), displayText: `${notes.length} 篇` },
    { label: "_raw 待编译", pct: Math.min(rawPending * 20, 100), displayText: `${rawPending} 篇` },
];
dv.span(items.map(d => renderGreenBar(d.label, d.pct, d.displayText)).join(""));
```

**本周概述**：今日是系统素材归档整理日。批量处理了 6 项 Inbox 素材，金融英语 5 篇原子笔记完成标序，Collector [T] 分流 cron 首次成功运行。系统处于稳定运行期。

---

## 🌐 观全局

**关键词**：归档整理 · 金融英语 · cron 上线 · 模型切换

今日完成的工作主要分三块：

**① Inbox 归档整理** (6 项)
- 纯金融高频词表 → 拆分为流程词表 + 每日一词 Week 1
- 金融语料库 → 校准 YAML + 归档
- 知→行→进化分镜台本 → 补 YAML
- 富贵包矫正 → 补 YAML
- 低头族体态矫正 → 拆为 1 方案 + 4 动作笔记
- 愿力·业力·努力 → 归档到 Archive

**② 金融英语体系**
- Language Chunks 标号 01-05（market/stock/trade/price/share）
- 统一 related 引用，MOC 已建
- 操盘手英语·每日流程词表 + 每日一词 Week 1 归档到位

**③ 自动化上线**
- Collector [T] 分流 cron 首次运行成功（1 条分流）
- 模型 deepseek-v4-flash → glm-5.2 切换完成

---

## 🌊 察潜流

> 本周重复出现的主题、模式或认知波动。

1. **批量归档模式 vs 日常输入模式**：今天集中处理了一批 Inbox 留存素材，效率比零散处理高得多。这种「批处理日」值得固定周期重复。

2. **金融英语体系的建立**：从语料库研究 → 每日一词教学 → Language Chunks 原子笔记，形成了完整的三层结构。这是知识从"原材料"到"教学产品"的完整转化链条。

---

## 🪸 拂遗珠

> 被忽略但值得回捡的存量。

- 7/22-24 三天的 Insight 归档文件中热力图代码缺失（旧 cron 生成问题），已修复模板但历史文件未追溯
- 分镜台本的推广文案/剪辑指引尚未产出

---

## 🔦 照暗面

> 系统阻力点分析。

1. **问题**：`_raw/` 素材积累速度快于处理速度
   - **处理**：已明确原则——`_raw/` 不删、不动原文、校准 YAML 后按 `ingest_status: archived` 移入 Archive
   - **判定**：🟢 已解决

2. **问题**：Daily Insight cron 在模型切换后短暂不可用
   - **根因**：模型 API 切换导致 cron 运行时调用异常
   - **处理**：手动触发后恢复，后续需持续观察
   - **判定**：🟡 需观察

---

## 🍃 辨风势

```dataviewjs
const renderGreenBar = (label, pct, displayText = null) => {
    let barColor = "#F4FCF6";
    let targetVal = pct;
    if (targetVal <= 15) barColor = "#c6e48b";
    else if (targetVal <= 30) barColor = "#7bc96f";
    else if (targetVal <= 45) barColor = "#49af5d";
    else if (targetVal <= 60) barColor = "#2e8840";
    else barColor = "#196127";
    let rightText = displayText !== null ? displayText : `${pct}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${pct}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
};

const types = { concept: 0, model: 0, practice: 0, critique: 0, reference: 0, moc: 0 };
const allNotes = dv.pages('"30_Resources/31_Atomic_Notes"');
allNotes.forEach(p => { if (p.type && types[p.type] !== undefined) types[p.type]++; });
const total = Object.values(types).reduce((a, b) => a + b, 0) || 1;
const items = [
    { label: "concept 概念", pct: Math.round(types.concept / total * 100) },
    { label: "practice 实践", pct: Math.round(types.practice / total * 100) },
    { label: "model 模型", pct: Math.round(types.model / total * 100) },
    { label: "reference 参考", pct: Math.round(types.reference / total * 100) },
    { label: "critique 批判", pct: Math.round(types.critique / total * 100) },
    { label: "MOC 索引", pct: Math.round(types.moc / total * 100) },
];
dv.span(items.map(d => renderGreenBar(d.label, d.pct, `${d.pct}% (${types[d.label.split(' ')[0]] || 0}篇)`)).join(""));
```

**趋势判断**：「个人成长」仍是核心领域。今日新增大量金融英语内容，`concept` 类笔记因 5 篇金融英语原子笔记加入而进一步增长。`critique` 和 `moc` 依旧严重偏少，知识地图建设近乎空白。

---

## 🌡️ 测热力

以下为 Heatmap Calendar 渲染的每日活跃度分布（色块越深表示当天修改笔记越多）：

```dataviewjs
try {
  const pages = dv.pages('"20_Areas/A2_能力接口/Bullet Journal" or "00_Inbox" or "10_Projects" or "20_Areas" or "30_Resources"')
    .where(p => p.file.day);
  const countMap = {};
  pages.forEach(p => {
    const key = p.file.day.toFormat("yyyy-MM-dd");
    countMap[key] = (countMap[key] || 0) + 1;
  });
  const entries = Object.entries(countMap).map(([date, count]) => ({ date: date, intensity: count }));
  renderHeatmapCalendar(this.container, { colors: "default", entries: entries });
} catch(e) {
  dv.span("⚠️ Heatmap Calendar 插件未启用，请安装后查看热力图");
}
```

**活跃度**：7/27 全天多轮工具调用和文件操作，活跃度应该在本周前列。

---

## 🎯 践一诺

> [!note] 今日承诺
> 批量清理 Inbox 素材，完成 6 项归档整理 + 金融英语体系标序。

**P0 — 持续稳定 cron 运行，观察每日 Insight 自动生成是否正常。**

- 今日 Cron 首跑：Collector 分流 ✅
- 待观察：Daily Insight cron 在 glm-5.2 下稳定情况