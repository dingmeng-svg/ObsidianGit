---
title: "🌅 2026-07-28 洞察日报"
tags: [洞察, 系统文档]
date: 2026-07-28
created: 2026-07-28
updated: 2026-07-28
---

# 🗓️ 2026-07-28 周二

## 🌐 观全局

**判断**：输入端无积压，输出端正常运转——系统处于稳定运行期。BuJo 每日打卡满勤，但近7天原子笔记产出集中在22-23日，后5天零新增，呈「脉冲式」产出模式。

```dataviewjs
function renderGreenBar(label, pctOrRank, displayText = null, useRankMode = false) {
    let barColor = "#c6e48b";
    let targetVal = pctOrRank;
    if (useRankMode) {
        if (targetVal > 0.8) barColor = "#196127";
        else if (targetVal > 0.6) barColor = "#2e8840";
        else if (targetVal > 0.4) barColor = "#49af5d";
        else if (targetVal > 0.2) barColor = "#7bc96f";
        else barColor = "#c6e48b";
    } else {
        if (targetVal <= 15) barColor = "#c6e48b";
        else if (targetVal <= 30) barColor = "#7bc96f";
        else if (targetVal <= 45) barColor = "#49af5d";
        else if (targetVal <= 60) barColor = "#2e8840";
        else barColor = "#196127";
    }
    let rightText = displayText !== null ? displayText : `${pctOrRank}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${useRankMode ? (targetVal * 100) : targetVal}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
}

const items = [
    { label: "原子笔记 7d活跃", val: "27 篇", pct: 27 },
    { label: "BuJo日记 7d活跃", val: "8 篇", pct: 8 },
    { label: "Inbox待处理", val: "0", pct: 0 },
    { label: "_raw待编译", val: "0", pct: 0 },
    { label: "MOC 知识地图", val: "17 篇", pct: 17 },
    { label: "归档累计", val: "376 篇", pct: 100 },
];
const maxVal = Math.max(...items.map(d => d.pct));
let html = `<div style="margin:1rem 0;font-family:-apple-system,sans-serif;">`;
for (let d of items) {
    const pct = maxVal > 0 ? Math.round((d.pct / maxVal) * 100) : 0;
    html += renderGreenBar(d.label, pct, d.val, false);
}
html += `</div>`;
dv.paragraph(html);
```

## 🌊 察潜流

**全库 type 分布**（自建库以来累计）：

```dataviewjs
function renderGreenBar(label, pctOrRank, displayText = null, useRankMode = false) {
    let barColor = "#c6e48b";
    let targetVal = pctOrRank;
    if (useRankMode) {
        if (targetVal > 0.8) barColor = "#196127";
        else if (targetVal > 0.6) barColor = "#2e8840";
        else if (targetVal > 0.4) barColor = "#49af5d";
        else if (targetVal > 0.2) barColor = "#7bc96f";
        else barColor = "#c6e48b";
    } else {
        if (targetVal <= 15) barColor = "#c6e48b";
        else if (targetVal <= 30) barColor = "#7bc96f";
        else if (targetVal <= 45) barColor = "#49af5d";
        else if (targetVal <= 60) barColor = "#2e8840";
        else barColor = "#196127";
    }
    let rightText = displayText !== null ? displayText : `${pctOrRank}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${useRankMode ? (targetVal * 100) : targetVal}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
}

const types = [
    { label: "practice 实践", count: 123 },
    { label: "concept 概念", count: 111 },
    { label: "model 模型", count: 12 },
    { label: "critique 批判", count: 3 },
    { label: "moc 知识地图", count: 1 },
];
const maxType = Math.max(...types.map(d => d.count));
let html = `<div style="margin:1rem 0;font-family:-apple-system,sans-serif;">`;
for (let d of types) {
    const pct = maxType > 0 ? Math.round((d.count / maxType) * 100) : 0;
    html += renderGreenBar(d.label, pct, `${d.count} 篇`, false);
}
html += `</div>`;
dv.paragraph(html);
```

**全库判断**：practice（123）略多于 concept（111），属于均衡偏实践的知识体系。但 critique（3）和 moc type（1）严重偏少——知识地图建设近乎空白，缺乏对已有知识的结构化梳理。

**近7天增量**：新增/修改以 concept 为主（17篇），practice 仅 2 篇，model 1 篇，moc 1 篇。这一周处于信息消化与概念梳理期，实践产出偏低。

**活跃项目**：
- 10_Projects/11_工作/01_操盘手骑士
- 10_Projects/12_个人/01_知→行→进化
- 10_Projects/12_个人/02_100 Word Tales｜百词百篇
- 10_Projects/12_个人/03_大脑盲盒

项目「知→行→进化」有多篇概念笔记产出（CODE法则、生命之轮、三力合一模型等），但尚未见到实践转化产物。

## 🪸 拂遗珠

### _raw 待编译清单
全部 4 篇素材已完成 Ingest（`ingest_status: ingested`），无积压 ✅
- Obsidian x AI 第71篇：零摩擦跨端自动日记工作流搭建指南 ✅
- Obsidian 附件管理完全指南 ✅
- Obsidian 高性价比多端同步方案 ✅
- Obsidian 图床搭建全指南 ✅

### 外部导入待处理
0 篇 ✅

### ai_generated 待审核
全库共 **7 篇** AI 生成笔记未经验证（`ai_generated` 标记，缺少 `human_edited`）：

| 领域 | 数量 | 状态 |
|:---|:---:|:---|
| 关系 | 4 篇 | 待人工审校 |
| 生活 | 3 篇 | 待人工审校 |

### Inbox 超期检查
放置超过 14 天的文件：0 ✅

### 领域笔记分布（按数量降序）
个人成长 173 篇｜语言 127 篇｜心理学 61 篇｜知识管理 37 篇｜健身 34 篇｜工作 24 篇｜关系 23 篇｜生活 20 篇｜国际政治 12 篇｜技术 12 篇｜认知算法 6 篇｜科学 4 篇

## 🔦 照暗面

### 近7天零实质修改——产出脉冲断裂
BuJo 每日打卡保持满勤（8/8天），但原子笔记近7天（7/21-28）**无实质内容新增**。虽有27篇文件被 `-mtime -7` 捕获，但其实际修改集中在7/22-23两日，后5天产出率为零。这种「脉冲式」产出模式意味着知识摄入后未及时沉淀为结构化笔记。

### ai_generated 无人审校
7 篇 AI 生成笔记（关系4篇、生活3篇）仍处于未经人工审核状态。其中关系领域4篇可能存在私密内容，不宜长期处于未审校状态。

### 知识地图建设薄弱
MOC 仅 17 篇，moc type 仅 1 篇。面对 534 篇原子笔记覆盖 12 个领域，MOC 覆盖率严重不足，知识之间缺乏结构性关联。语言领域（127篇）和个人成长（173篇）占全库过半，但没有对应的领域 MOC 做知识索引。

### 项目实践转化缺口
「知→行→进化」项目有大量概念产出（三力合一、生命之轮、业力惯性等）但缺少对应的 practice 类型产物，项目进展偏"知"轻"行"。

## 🍃 辨风势

**近7天活跃话题**（按修改笔记标签统计）：

```dataviewjs
function renderGreenBar(label, pctOrRank, displayText = null, useRankMode = false) {
    let barColor = "#c6e48b";
    let targetVal = pctOrRank;
    if (useRankMode) {
        if (targetVal > 0.8) barColor = "#196127";
        else if (targetVal > 0.6) barColor = "#2e8840";
        else if (targetVal > 0.4) barColor = "#49af5d";
        else if (targetVal > 0.2) barColor = "#7bc96f";
        else barColor = "#c6e48b";
    } else {
        if (targetVal <= 15) barColor = "#c6e48b";
        else if (targetVal <= 30) barColor = "#7bc96f";
        else if (targetVal <= 45) barColor = "#49af5d";
        else if (targetVal <= 60) barColor = "#2e8840";
        else barColor = "#196127";
    }
    let rightText = displayText !== null ? displayText : `${pctOrRank}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${useRankMode ? (targetVal * 100) : targetVal}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
}

const tagsData = [
    { label: "个人成长", count: 11 },
    { label: "认知算法", count: 6 },
    { label: "知识管理", count: 2 },
    { label: "模型", count: 2 },
    { label: "语言", count: 1 },
    { label: "科学", count: 1 },
];
const totalTags = tagsData.length;
let html = `<div style="margin:1rem 0;font-family:-apple-system,sans-serif;">`;
for (let i = 0; i < totalTags; i++) {
    let d = tagsData[i];
    let rankPct = totalTags > 1 ? (totalTags - i - 1) / (totalTags - 1) : 1;
    html += renderGreenBar(d.label, rankPct, `${d.count} 篇`, true);
}
html += `</div>`;
dv.paragraph(html);
```

**趋势判断**：「个人成长」仍是核心关注领域，近期对「中年危机」「性观念」「生命之轮」等话题的密集概念化梳理，反映对人生阶段重构的深层需求。新涌现的「认知算法」话题（6篇DeepSeek相关笔记）表明你在系统性研究组织战略与决策方法论——这是一个值得持续深挖的新方向。语言领域近7天活跃度下降，与此前高密度投入形成对比，可能进入整理沉淀期。

## 🌡️ 测热力

以下为 Heatmap Calendar 渲染的每日活跃度分布（色块越深表示当天修改笔记越多）：

```dataviewjs
try {
  const pages = dv.pages('"20_Areas/A2_能力接口/Bullet Journal" or "00_Inbox" or "10_Projects" or "20_Areas" or "30_Resources"')
    .where(p => p.file.day);
  const countMap = {};
  pages.forEach(p => {
    const key = p.file.day.toFormat("yyyy-MM-dd");
    countMap[key] = (countMap[key] || 0) + 1;
  });
  const entries = Object.entries(countMap).map(([date, count]) => ({ date: date, intensity: count }));
  renderHeatmapCalendar(this.container, { colors: "default", entries: entries });
} catch(e) {
  dv.span("⚠️ Heatmap Calendar 插件未启用，请安装后查看热力图");
}
```

### 近30天各领域活跃分布

```dataviewjs
function renderGreenBar(label, pct, displayText = null) {
    let barColor = "#c6e48b";
    if (pct <= 15) barColor = "#c6e48b";
    else if (pct <= 30) barColor = "#7bc96f";
    else if (pct <= 45) barColor = "#49af5d";
    else if (pct <= 60) barColor = "#2e8840";
    else barColor = "#196127";
    let rightText = displayText !== null ? displayText : `${pct}%`;
    return `<div style="display: flex; align-items: center; gap: 12px; margin-bottom: 10px;">
        <span style="min-width: 150px; font-size: 13px; color: #4B5563; flex-shrink: 0; text-align: right;">${label}</span>
        <div style="flex: 1; max-width: 460px; height: 8px; background-color: #F3F4F6; border-radius: 99px; overflow: hidden;">
            <div style="width: ${pct}%; height: 100%; border-radius: 99px; background-color: ${barColor};"></div>
        </div>
        <span style="min-width: 30px; font-size: 13px; font-weight: 600; color: #4B5563; text-align: right; white-space: nowrap; flex-shrink: 0;">${rightText}</span>
    </div>`;
}

const cutoff = dv.date("today").minus({ days: 30 });
const pages = dv.pages('"20_Areas" or "30_Resources" or "10_Projects"')
    .where(p => p.file.mtime && p.file.mtime > cutoff);

const domainMap = {};
pages.forEach(p => {
    const folders = p.file.path.split("/");
    let domain = folders[0];
    if (folders[0] === "20_Areas" && folders[1]) domain = folders[1];
    if (folders[0] === "30_Resources" && folders[1]) domain = folders[1];
    if (folders[0] === "10_Projects" && folders[1]) domain = folders[1];
    domainMap[domain] = (domainMap[domain] || 0) + 1;
});

const sorted = Object.entries(domainMap).sort((a, b) => b[1] - a[1]).slice(0, 8);
const maxCount = sorted.length > 0 ? sorted[0][1] : 1;
const items = sorted.map(([domain, count]) => ({
    label: domain,
    pct: Math.round(count / maxCount * 100),
    displayText: `${count} 篇`
}));
dv.span(items.map(d => renderGreenBar(d.label, d.pct, d.displayText)).join(""));
```

**近30天注意力趋势**：BuJo 日记连续四周满勤（28/28天），日循环执行极其稳定。但原子笔记产出仅集中在7/22-23两天，产出行为呈典型的「脉冲式」分布——消化日密集整理，其他日子零产出。这种模式适合深度思考但不适合知识库的持续更新。

全库 534 篇原子笔记 + 376 篇归档，知识资产总量已达 910 篇，但近7天零新增说明系统正在经历一个「消化期」。

## 🎯 践一诺

**从「认知算法」概念到实践清单**：近7天产出最密集的主题是「认知算法/DeepSeek」系列（6篇concept），其中「战术克制三件套——开源、低价、不树敌」最具迁移价值。建议在本周内将其提炼为一份可执行决策清单（practice类型），应用于操盘手骑士项目的竞品策略分析中，完成「知→行」闭环。

> [!note] 唯一承诺
> 本周洞察收敛为唯一可执行承诺。多则失焦，少则无力。
>
> **约束**：此区块仅保留 1 条最高优先级行动项。多个候选须在 `## 察潜流` 或 `## 辨风势` 中完成筛选，只有胜出者方可进入此处。

---

*本报告由 Hermes Agent 于 2026-07-28 09:44 自动生成。*
