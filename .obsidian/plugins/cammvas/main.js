/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => CanvasMindMapPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian9 = require("obsidian");

// src/canvas/canvas-api.ts
var import_obsidian = require("obsidian");

// src/ui/dom.ts
function isHtmlElement(value) {
  return typeof value === "object" && value !== null && typeof Reflect.get(value, "closest") === "function" && typeof Reflect.get(value, "offsetHeight") === "number";
}
function isDomNode(value) {
  return typeof value === "object" && value !== null && typeof Reflect.get(value, "nodeType") === "number";
}

// src/ui/spatial-navigation.ts
function isRectFullyVisible(rect, viewport) {
  return rect.top >= viewport.top && rect.right <= viewport.right && rect.bottom <= viewport.bottom && rect.left >= viewport.left;
}
function findNearestNodeInDirection(origin, candidates, direction) {
  const originX = origin.x + origin.width / 2;
  const originY = origin.y + origin.height / 2;
  let nearest = null;
  let nearestScore = Infinity;
  for (const candidate of candidates) {
    if (candidate.id === origin.id) continue;
    const dx = candidate.x + candidate.width / 2 - originX;
    const dy = candidate.y + candidate.height / 2 - originY;
    const primary = direction === "right" ? dx : direction === "left" ? -dx : direction === "down" ? dy : -dy;
    if (primary <= 0) continue;
    const perpendicular = direction === "left" || direction === "right" ? Math.abs(dy) : Math.abs(dx);
    const score = primary + perpendicular * 2;
    if (score < nearestScore) {
      nearest = candidate;
      nearestScore = score;
    }
  }
  return nearest;
}

// src/canvas/canvas-api.ts
function genId() {
  return Array.from(
    { length: 16 },
    () => Math.floor(Math.random() * 16).toString(16)
  ).join("");
}
function findNodeFromEvent(canvas, e) {
  var _a;
  const target = e.target;
  if (!isHtmlElement(target)) return null;
  for (const node of canvas.nodes.values()) {
    if ((_a = node.nodeEl) == null ? void 0 : _a.contains(target)) return node;
  }
  return null;
}
var CanvasAPI = class {
  constructor(app) {
    this.app = app;
    this.edgeIndex = null;
    this.indexedCanvas = null;
    this.indexedEdgeIds = null;
  }
  /**
   * Get or rebuild the edge index for the given canvas.
   * Rebuilds if canvas changed or edge count changed (structural mutation).
   */
  getEdgeIndex(canvas) {
    if (this.edgeIndex && this.indexedCanvas === canvas && this.edgeIdsMatch(canvas)) {
      return this.edgeIndex;
    }
    const incoming = /* @__PURE__ */ new Map();
    const outgoing = /* @__PURE__ */ new Map();
    for (const edge of canvas.edges.values()) {
      const fromId = edge.from.node.id;
      const toId = edge.to.node.id;
      let out = outgoing.get(fromId);
      if (!out) {
        out = [];
        outgoing.set(fromId, out);
      }
      out.push(edge);
      let inc = incoming.get(toId);
      if (!inc) {
        inc = [];
        incoming.set(toId, inc);
      }
      inc.push(edge);
    }
    this.edgeIndex = { incoming, outgoing };
    this.indexedCanvas = canvas;
    this.indexedEdgeIds = new Set(canvas.edges.keys());
    return this.edgeIndex;
  }
  /**
   * Check whether the cached edge ID set still matches the live canvas.
   */
  edgeIdsMatch(canvas) {
    if (!this.indexedEdgeIds || canvas.edges.size !== this.indexedEdgeIds.size) return false;
    for (const id of canvas.edges.keys()) {
      if (!this.indexedEdgeIds.has(id)) return false;
    }
    return true;
  }
  /**
   * Invalidate the edge index (call after adding/removing edges).
   */
  invalidateEdgeIndex() {
    this.edgeIndex = null;
  }
  /**
   * Get the active canvas if a canvas view is currently focused.
   */
  getActiveCanvas() {
    var _a;
    const view = this.app.workspace.getActiveViewOfType(import_obsidian.ItemView);
    if (!view || view.getViewType() !== "canvas") return null;
    return (_a = view.canvas) != null ? _a : null;
  }
  /**
   * Get canvas from any open canvas leaf (first found).
   */
  getAnyCanvas() {
    var _a;
    const leaves = this.app.workspace.getLeavesOfType("canvas");
    if (leaves.length === 0) return null;
    const view = leaves[0].view;
    return (_a = view == null ? void 0 : view.canvas) != null ? _a : null;
  }
  /**
   * Get the currently selected node (single selection).
   */
  getSelectedNode(canvas) {
    const selection = canvas.selection;
    if (selection.size !== 1) return null;
    for (const item of selection) {
      return "nodeEl" in item ? item : null;
    }
    return null;
  }
  /**
   * Create a text node at a given position.
   */
  createTextNode(canvas, x, y, text = "", width = 260, height = 60) {
    const node = canvas.createTextNode({
      pos: { x, y },
      size: { width, height },
      text,
      focus: false,
      save: false
    });
    return node;
  }
  /**
   * Create an edge between two nodes using canvas.importData.
   */
  createEdge(canvas, fromNode, toNode, fromSide = "right", toSide = "left", color) {
    const id = genId();
    canvas.importData({
      edges: [
        {
          id,
          fromNode: fromNode.id,
          fromSide,
          fromEnd: "none",
          toNode: toNode.id,
          toSide,
          toEnd: "arrow",
          ...color ? { color } : {}
        }
      ],
      nodes: []
    });
    this.invalidateEdgeIndex();
  }
  /**
   * Remove a node and all its connected edges.
   */
  removeNode(canvas, node) {
    const connectedEdges = this.getConnectedEdges(canvas, node);
    for (const edge of connectedEdges) {
      canvas.removeEdge(edge);
    }
    canvas.removeNode(node);
    this.invalidateEdgeIndex();
  }
  /**
   * Get all edges connected to a node (incoming + outgoing).
   */
  getConnectedEdges(canvas, node) {
    var _a, _b;
    const idx = this.getEdgeIndex(canvas);
    const inc = (_a = idx.incoming.get(node.id)) != null ? _a : [];
    const out = (_b = idx.outgoing.get(node.id)) != null ? _b : [];
    return [...inc, ...out];
  }
  /**
   * Get parent node (the node that has an edge pointing TO this node).
   */
  getParentNode(canvas, node) {
    const idx = this.getEdgeIndex(canvas);
    const inc = idx.incoming.get(node.id);
    return inc && inc.length > 0 ? inc[0].from.node : null;
  }
  /**
   * Get child nodes (nodes that this node has edges pointing TO).
   */
  getChildNodes(canvas, node) {
    var _a;
    const idx = this.getEdgeIndex(canvas);
    const out = (_a = idx.outgoing.get(node.id)) != null ? _a : [];
    const children = out.map((e) => e.to.node);
    children.sort((a, b) => a.y - b.y);
    return children;
  }
  /**
   * Get outgoing edges from a node (for BFS traversal).
   */
  getOutgoingEdges(canvas, nodeId) {
    var _a;
    const idx = this.getEdgeIndex(canvas);
    return (_a = idx.outgoing.get(nodeId)) != null ? _a : [];
  }
  /**
   * Get sibling nodes (other children of the same parent).
   */
  getSiblingNodes(canvas, node) {
    const parent = this.getParentNode(canvas, node);
    if (!parent) return [];
    return this.getChildNodes(canvas, parent).filter(
      (n) => n.id !== node.id
    );
  }
  /**
   * Select a node and zoom to it with padding.
   */
  selectAndZoom(canvas, node, zoomPadding) {
    canvas.selectOnly(node);
    this.zoomToNode(canvas, node, zoomPadding);
  }
  /**
   * Select a node without moving the viewport unless it is clipped or off-screen.
   */
  selectAndReveal(canvas, node, zoomPadding) {
    const isVisible = isRectFullyVisible(
      node.nodeEl.getBoundingClientRect(),
      canvas.wrapperEl.getBoundingClientRect()
    );
    canvas.selectOnly(node);
    if (!isVisible) this.zoomToNode(canvas, node, zoomPadding);
  }
  zoomToNode(canvas, node, zoomPadding) {
    if (zoomPadding > 0) {
      const cx = node.x + node.width / 2;
      const cy = node.y + node.height / 2;
      canvas.zoomToBbox({
        minX: cx - zoomPadding,
        minY: cy - zoomPadding,
        maxX: cx + zoomPadding,
        maxY: cy + zoomPadding
      });
    } else {
      canvas.zoomToSelection();
    }
  }
  selectAndEdit(canvas, node, zoomPadding = 0, immediate = false) {
    this.selectAndZoom(canvas, node, zoomPadding);
    if (immediate) {
      node.startEditing();
      return;
    }
    canvas.wrapperEl.win.setTimeout(() => {
      node.startEditing();
    }, 50);
  }
};

// src/mindmap/tree-model.ts
function buildForest(canvas) {
  const nodeMap = /* @__PURE__ */ new Map();
  const childIds = /* @__PURE__ */ new Set();
  for (const edge of canvas.edges.values()) {
    childIds.add(edge.to.node.id);
  }
  const groupIds = getGroupIds(canvas);
  for (const node of canvas.nodes.values()) {
    if (groupIds.has(node.id)) continue;
    nodeMap.set(node.id, {
      canvasNode: node,
      parent: null,
      children: [],
      depth: 0,
      siblingIndex: 0,
      direction: null
    });
  }
  for (const edge of canvas.edges.values()) {
    const parentTree = nodeMap.get(edge.from.node.id);
    const childTree = nodeMap.get(edge.to.node.id);
    if (parentTree && childTree) {
      childTree.parent = parentTree;
      parentTree.children.push(childTree);
    }
  }
  for (const treeNode of nodeMap.values()) {
    treeNode.children.sort(
      (a, b) => a.canvasNode.y - b.canvasNode.y
    );
    treeNode.children.forEach((child, i) => {
      child.siblingIndex = i;
    });
  }
  const roots = [];
  for (const node of canvas.nodes.values()) {
    if (!childIds.has(node.id)) {
      const treeNode = nodeMap.get(node.id);
      if (treeNode) {
        setDepths(treeNode, 0);
        assignDirections(treeNode);
        roots.push(treeNode);
      }
    }
  }
  roots.sort((a, b) => countReachable(b) - countReachable(a));
  return roots;
}
function getGroupIds(canvas) {
  const ids = /* @__PURE__ */ new Set();
  for (const nd of canvas.getData().nodes) {
    if (nd.type === "group") ids.add(nd.id);
  }
  return ids;
}
function findTreeForNode(forest, nodeId) {
  for (const root of forest) {
    const found = findTreeNode(root, nodeId);
    if (found) return found;
  }
  return null;
}
function countReachable(node) {
  let count = 1;
  for (const child of node.children) {
    count += countReachable(child);
  }
  return count;
}
function setDepths(node, depth) {
  node.depth = depth;
  for (const child of node.children) {
    setDepths(child, depth + 1);
  }
}
function findTreeNode(root, nodeId) {
  if (root.canvasNode.id === nodeId) return root;
  for (const child of root.children) {
    const found = findTreeNode(child, nodeId);
    if (found) return found;
  }
  return null;
}
function getDescendants(node) {
  const result = [];
  for (const child of node.children) {
    result.push(child);
    result.push(...getDescendants(child));
  }
  return result;
}
function getNextSibling(node) {
  if (!node.parent) return null;
  const siblings = node.parent.children;
  const idx = siblings.indexOf(node);
  return idx < siblings.length - 1 ? siblings[idx + 1] : null;
}
function getPrevSibling(node) {
  if (!node.parent) return null;
  const siblings = node.parent.children;
  const idx = siblings.indexOf(node);
  return idx > 0 ? siblings[idx - 1] : null;
}
function assignDirections(root) {
  const rootCx = root.canvasNode.x + root.canvasNode.width / 2;
  for (const child of root.children) {
    const childCx = child.canvasNode.x + child.canvasNode.width / 2;
    child.direction = childCx >= rootCx ? "right" : "left";
    propagateDirection(child, child.direction);
  }
}
function propagateDirection(node, dir) {
  for (const child of node.children) {
    child.direction = dir;
    propagateDirection(child, dir);
  }
}
function countChildrenPerSide(root) {
  let left = 0;
  let right = 0;
  for (const child of root.children) {
    if (child.direction === "left") left++;
    else right++;
  }
  return { left, right };
}

// src/canvas/drag-reparent-state.ts
function collectDescendantIds(rootId, getChildIds) {
  return collectDescendantIdsForRoots([rootId], getChildIds);
}
function collectDescendantIdsForRoots(rootIds, getChildIds) {
  const descendants = /* @__PURE__ */ new Set();
  const queue = Array.from(rootIds);
  const visited = new Set(queue);
  while (queue.length > 0) {
    const nodeId = queue.shift();
    for (const childId of getChildIds(nodeId)) {
      if (visited.has(childId)) continue;
      visited.add(childId);
      descendants.add(childId);
      queue.push(childId);
    }
  }
  return descendants;
}
function findDropTarget(nodes, point, excludedIds, hitPadding = 0) {
  var _a;
  let best = null;
  let bestZIndex = -Infinity;
  const padding = Math.max(0, hitPadding);
  for (const node of nodes) {
    if (excludedIds.has(node.id)) continue;
    if (point.x < node.x - padding || point.x > node.x + node.width + padding) continue;
    if (point.y < node.y - padding || point.y > node.y + node.height + padding) continue;
    const zIndex = (_a = node.zIndex) != null ? _a : 0;
    if (!best || zIndex >= bestZIndex) {
      best = node;
      bestZIndex = zIndex;
    }
  }
  return best;
}
function shouldReparentOnDragEnd(eventType) {
  return eventType === "pointerup";
}
function getTopLevelSelectedIds(selectedIds, getParentId) {
  const roots = /* @__PURE__ */ new Set();
  for (const nodeId of selectedIds) {
    let parentId = getParentId(nodeId);
    const visited = /* @__PURE__ */ new Set([nodeId]);
    let nested = false;
    while (parentId && !visited.has(parentId)) {
      if (selectedIds.has(parentId)) {
        nested = true;
        break;
      }
      visited.add(parentId);
      parentId = getParentId(parentId);
    }
    if (!nested) roots.add(nodeId);
  }
  return roots;
}

// src/mindmap/node-operations.ts
var NodeOperations = class {
  constructor(canvasApi, config) {
    this.canvasApi = canvasApi;
    this.config = config;
  }
  /**
   * Add a child node to the selected node.
   * If parent is root, places on the side with fewer children (ties go right).
   * If parent is non-root, inherits direction from its branch.
   * Returns the new node so the caller can start editing it.
   */
  addChild(canvas, parentNode) {
    const forest = buildForest(canvas);
    const parentTreeNode = findTreeForNode(forest, parentNode.id);
    const isRoot = parentTreeNode && !parentTreeNode.parent;
    let direction;
    if (isRoot && parentTreeNode) {
      const counts = countChildrenPerSide(parentTreeNode);
      direction = counts.left < counts.right ? "left" : "right";
    } else {
      direction = this.detectDirection(canvas, parentNode);
    }
    const existingChildren = this.canvasApi.getChildNodes(canvas, parentNode);
    let x;
    if (direction === "right") {
      x = parentNode.x + parentNode.width + this.config.horizontalGap;
    } else {
      x = parentNode.x - this.config.nodeWidth - this.config.horizontalGap;
    }
    let y;
    if (existingChildren.length > 0) {
      const sameSideChildren = existingChildren.filter((c) => {
        const childCx = c.x + c.width / 2;
        const parentCx = parentNode.x + parentNode.width / 2;
        return direction === "right" ? childCx > parentCx : childCx < parentCx;
      });
      if (sameSideChildren.length > 0) {
        const lastChild = sameSideChildren[sameSideChildren.length - 1];
        y = lastChild.y + lastChild.height + this.config.verticalGap;
      } else {
        y = parentNode.y + (parentNode.height - this.config.nodeHeight) / 2;
      }
    } else {
      y = parentNode.y + (parentNode.height - this.config.nodeHeight) / 2;
    }
    const newNode = this.canvasApi.createTextNode(
      canvas,
      x,
      y,
      "",
      this.config.nodeWidth,
      this.config.nodeHeight
    );
    if (parentNode.color) newNode.setColor(parentNode.color);
    if (direction === "right") {
      this.canvasApi.createEdge(canvas, parentNode, newNode, "right", "left", parentNode.color || void 0);
    } else {
      this.canvasApi.createEdge(canvas, parentNode, newNode, "left", "right", parentNode.color || void 0);
    }
    canvas.requestSave();
    return newNode;
  }
  /**
   * Add a sibling node below the selected node (same parent).
   * Inherits the branch direction from the current node.
   * Returns the new node.
   */
  addSibling(canvas, currentNode) {
    const parent = this.canvasApi.getParentNode(canvas, currentNode);
    if (!parent) {
      return this.addChild(canvas, currentNode);
    }
    const direction = this.detectDirection(canvas, currentNode);
    const x = currentNode.x;
    const y = currentNode.y + currentNode.height + this.config.verticalGap;
    const newNode = this.canvasApi.createTextNode(
      canvas,
      x,
      y,
      "",
      this.config.nodeWidth,
      this.config.nodeHeight
    );
    if (currentNode.color) newNode.setColor(currentNode.color);
    if (direction === "right") {
      this.canvasApi.createEdge(canvas, parent, newNode, "right", "left", currentNode.color || void 0);
    } else {
      this.canvasApi.createEdge(canvas, parent, newNode, "left", "right", currentNode.color || void 0);
    }
    canvas.requestSave();
    return newNode;
  }
  /** Replace a node's incoming edge while preserving its complete subtree. */
  reparent(canvas, node, newParent) {
    if (node.id === newParent.id) return false;
    const descendants = collectDescendantIds(
      node.id,
      (nodeId) => this.canvasApi.getOutgoingEdges(canvas, nodeId).map((edge) => edge.to.node.id)
    );
    if (descendants.has(newParent.id)) return false;
    const incomingEdges = this.canvasApi.getConnectedEdges(canvas, node).filter((edge) => edge.to.node.id === node.id);
    if (incomingEdges.length === 1 && incomingEdges[0].from.node.id === newParent.id) {
      return false;
    }
    for (const edge of incomingEdges) canvas.removeEdge(edge);
    this.canvasApi.invalidateEdgeIndex();
    const nodeCenterX = node.x + node.width / 2;
    const parentCenterX = newParent.x + newParent.width / 2;
    if (nodeCenterX >= parentCenterX) {
      this.canvasApi.createEdge(canvas, newParent, node, "right", "left", newParent.color || void 0);
    } else {
      this.canvasApi.createEdge(canvas, newParent, node, "left", "right", newParent.color || void 0);
    }
    return true;
  }
  /** Delete a non-root node, reconnect its children, and return its parent. */
  deleteAndFocusParent(canvas, currentNode) {
    const parent = this.canvasApi.getParentNode(canvas, currentNode);
    if (!parent) return null;
    const direction = this.detectDirection(canvas, currentNode);
    for (const child of this.canvasApi.getChildNodes(canvas, currentNode)) {
      if (direction === "right") {
        this.canvasApi.createEdge(canvas, parent, child, "right", "left");
      } else {
        this.canvasApi.createEdge(canvas, parent, child, "left", "right");
      }
    }
    this.canvasApi.removeNode(canvas, currentNode);
    canvas.requestSave();
    return parent;
  }
  /**
   * Flip a branch to the other side of its parent.
   * Mirrors the node and all descendants horizontally around the parent's center X.
   * Returns the parent node (for caller to trigger restack/layout).
   */
  flipBranch(canvas, node) {
    const parent = this.canvasApi.getParentNode(canvas, node);
    if (!parent) return null;
    const parentCx = parent.x + parent.width / 2;
    const allNodes = [node];
    const visited = /* @__PURE__ */ new Set([node.id]);
    const queue = [node.id];
    while (queue.length > 0) {
      const id = queue.shift();
      for (const edge of this.canvasApi.getOutgoingEdges(canvas, id)) {
        const childId = edge.to.node.id;
        if (!visited.has(childId)) {
          visited.add(childId);
          allNodes.push(edge.to.node);
          queue.push(childId);
        }
      }
    }
    for (const n of allNodes) {
      const newX = 2 * parentCx - n.x - n.width;
      n.moveTo({ x: newX, y: n.y });
    }
    return parent;
  }
  /**
   * Detect the branch direction of a node based on actual positions.
   * If node has children, uses their position. Otherwise, uses parent position.
   */
  detectDirection(canvas, node) {
    const nodeCx = node.x + node.width / 2;
    const existingChildren = this.canvasApi.getChildNodes(canvas, node);
    if (existingChildren.length > 0) {
      const firstChildCx = existingChildren[0].x + existingChildren[0].width / 2;
      return firstChildCx < nodeCx ? "left" : "right";
    }
    const parent = this.canvasApi.getParentNode(canvas, node);
    if (parent) {
      const parentCx = parent.x + parent.width / 2;
      return nodeCx < parentCx ? "left" : "right";
    }
    return "right";
  }
};

// src/canvas/edge-updater.ts
function getCenter(node) {
  return {
    cx: node.x + node.width / 2,
    cy: node.y + node.height / 2
  };
}
function computeEdgeSides(fromNode, toNode) {
  const fromCenter = getCenter(fromNode);
  const toCenter = getCenter(toNode);
  const dx = toCenter.cx - fromCenter.cx;
  if (dx >= 0) {
    return { fromSide: "right", toSide: "left" };
  } else {
    return { fromSide: "left", toSide: "right" };
  }
}
function updateAllEdgeSides(canvas) {
  let changed = false;
  for (const edge of canvas.edges.values()) {
    const fromNode = edge.from.node;
    const toNode = edge.to.node;
    if (!fromNode || !toNode) continue;
    const { fromSide, toSide } = computeEdgeSides(fromNode, toNode);
    if (edge.from.side !== fromSide || edge.to.side !== toSide) {
      edge.from.side = fromSide;
      edge.to.side = toSide;
      changed = true;
    }
  }
  if (changed) {
    canvas.requestFrame();
    canvas.requestSave();
  }
}
function registerDragEndHandler(canvas) {
  var _a, _b, _c;
  let lastMoveUpdate = 0;
  const THROTTLE_MS = 40;
  const moveHandler = (e) => {
    if (e.pointerType === "touch") return;
    if (e.buttons === 0) return;
    const now = Date.now();
    if (now - lastMoveUpdate < THROTTLE_MS) return;
    lastMoveUpdate = now;
    updateAllEdgeSides(canvas);
  };
  const upHandler = (e) => {
    if (e.pointerType === "touch" && !e.isPrimary) return;
    updateAllEdgeSides(canvas);
  };
  const cancelHandler = () => {
    lastMoveUpdate = 0;
  };
  (_a = canvas.wrapperEl) == null ? void 0 : _a.addEventListener("pointermove", moveHandler);
  (_b = canvas.wrapperEl) == null ? void 0 : _b.addEventListener("pointerup", upHandler);
  (_c = canvas.wrapperEl) == null ? void 0 : _c.addEventListener("pointercancel", cancelHandler);
  return () => {
    var _a2, _b2, _c2;
    (_a2 = canvas.wrapperEl) == null ? void 0 : _a2.removeEventListener("pointermove", moveHandler);
    (_b2 = canvas.wrapperEl) == null ? void 0 : _b2.removeEventListener("pointerup", upHandler);
    (_c2 = canvas.wrapperEl) == null ? void 0 : _c2.removeEventListener("pointercancel", cancelHandler);
  };
}

// src/mindmap/layout-engine.ts
var DEFAULT_CONFIG = {
  horizontalGap: 80,
  verticalGap: 20,
  nodeWidth: 300,
  nodeHeight: 60,
  animate: true
};
var LayoutEngine = class {
  constructor(config) {
    this.config = { ...DEFAULT_CONFIG, ...config };
  }
  /**
   * Recalculate and apply layout to all trees in the canvas.
   * Each root's children are partitioned into left/right groups and
   * laid out independently, centered around their own root.
   */
  layout(canvas, skipAnimationNodeIds = /* @__PURE__ */ new Set()) {
    const forest = buildForest(canvas);
    if (forest.length === 0) return;
    const positions = /* @__PURE__ */ new Map();
    for (const root of forest) {
      const rootX = root.canvasNode.x;
      const rootY = root.canvasNode.y;
      positions.set(root.canvasNode.id, { x: rootX, y: rootY });
      const rightChildren = root.children.filter((c) => c.direction === "right");
      const leftChildren = root.children.filter((c) => c.direction === "left");
      this.layoutGroup(root, rightChildren, "right", rootX, rootY, positions);
      this.layoutGroup(root, leftChildren, "left", rootX, rootY, positions);
    }
    this.applyPositions(canvas, positions, skipAnimationNodeIds);
    updateAllEdgeSides(canvas);
  }
  /**
   * Partially re-layout only the children of a specific parent node
   * (and their subtrees). The parent stays in place; everything
   * outside this parent's subtree is untouched.
   */
  layoutChildren(canvas, parentNodeId, skipAnimationNodeIds = /* @__PURE__ */ new Set()) {
    const forest = buildForest(canvas);
    if (forest.length === 0) return;
    const parentTreeNode = findTreeForNode(forest, parentNodeId);
    if (!parentTreeNode || parentTreeNode.children.length === 0) return;
    const positions = /* @__PURE__ */ new Map();
    if (!parentTreeNode.parent) {
      const rightChildren = parentTreeNode.children.filter((c) => c.direction === "right");
      const leftChildren = parentTreeNode.children.filter((c) => c.direction === "left");
      const rootX = parentTreeNode.canvasNode.x;
      const rootY = parentTreeNode.canvasNode.y;
      this.layoutGroup(parentTreeNode, rightChildren, "right", rootX, rootY, positions);
      this.layoutGroup(parentTreeNode, leftChildren, "left", rootX, rootY, positions);
    } else {
      const parentCx = parentTreeNode.canvasNode.x + parentTreeNode.canvasNode.width / 2;
      const rightChildren = parentTreeNode.children.filter((c) => {
        const childCx = c.canvasNode.x + c.canvasNode.width / 2;
        return childCx >= parentCx;
      });
      const leftChildren = parentTreeNode.children.filter((c) => {
        const childCx = c.canvasNode.x + c.canvasNode.width / 2;
        return childCx < parentCx;
      });
      const px = parentTreeNode.canvasNode.x;
      const py = parentTreeNode.canvasNode.y;
      this.layoutGroup(parentTreeNode, rightChildren, "right", px, py, positions);
      this.layoutGroup(parentTreeNode, leftChildren, "left", px, py, positions);
    }
    this.applyPositions(canvas, positions, skipAnimationNodeIds);
    updateAllEdgeSides(canvas);
  }
  /**
   * Layout a group of same-side children, vertically centered around root.
   * Uses contour-based packing for compact spacing.
   */
  layoutGroup(root, children, direction, rootX, rootY, positions) {
    if (children.length === 0) return;
    const rootH = root.canvasNode.height || this.config.nodeHeight;
    const rootW = root.canvasNode.width || this.config.nodeWidth;
    const rootCenterY = rootY + rootH / 2;
    const subtrees = [];
    for (const child of children) {
      const childW = child.canvasNode.width || this.config.nodeWidth;
      const childX = direction === "right" ? rootX + rootW + this.config.horizontalGap : rootX - childW - this.config.horizontalGap;
      const tempPositions = /* @__PURE__ */ new Map();
      const contour = this.layoutSubtree(
        child,
        childX,
        0,
        0,
        direction,
        tempPositions
      );
      subtrees.push({ positions: tempPositions, contour });
    }
    const { yOffsets } = this.packSubtrees(subtrees);
    const lastIdx = children.length - 1;
    const lastChildH = children[lastIdx].canvasNode.height || this.config.nodeHeight;
    const blockTop = yOffsets[0];
    const blockBottom = yOffsets[lastIdx] + lastChildH;
    const globalShift = rootCenterY - (blockTop + blockBottom) / 2;
    for (let i = 0; i < subtrees.length; i++) {
      const yShift = yOffsets[i] + globalShift;
      for (const [id, pos] of subtrees[i].positions) {
        positions.set(id, { x: pos.x, y: pos.y + yShift });
      }
    }
  }
  /**
   * Recursively lay out a node and all its descendants.
   * Returns the contour (vertical extent per depth column).
   */
  layoutSubtree(node, nodeX, nodeY, depth, direction, positions) {
    const nodeH = node.canvasNode.height || this.config.nodeHeight;
    const nodeW = node.canvasNode.width || this.config.nodeWidth;
    positions.set(node.canvasNode.id, { x: nodeX, y: nodeY });
    const contour = /* @__PURE__ */ new Map();
    contour.set(depth, { top: nodeY, bottom: nodeY + nodeH });
    if (node.children.length === 0) return contour;
    const childSubtrees = [];
    for (const child of node.children) {
      const childW = child.canvasNode.width || this.config.nodeWidth;
      const nodeCenterX = node.canvasNode.x + node.canvasNode.width / 2;
      const childCenterX = child.canvasNode.x + child.canvasNode.width / 2;
      const childDirection = childCenterX >= nodeCenterX ? "right" : "left";
      const childX = childDirection === "right" ? nodeX + nodeW + this.config.horizontalGap : nodeX - childW - this.config.horizontalGap;
      const tempPositions = /* @__PURE__ */ new Map();
      const childContour = this.layoutSubtree(
        child,
        childX,
        0,
        depth + 1,
        childDirection,
        tempPositions
      );
      childSubtrees.push({ positions: tempPositions, contour: childContour });
    }
    const { yOffsets, combinedContour } = this.packSubtrees(childSubtrees);
    const lastIdx = node.children.length - 1;
    const lastChildH = node.children[lastIdx].canvasNode.height || this.config.nodeHeight;
    const blockTop = yOffsets[0];
    const blockBottom = yOffsets[lastIdx] + lastChildH;
    const centerShift = nodeY + nodeH / 2 - (blockTop + blockBottom) / 2;
    for (let i = 0; i < childSubtrees.length; i++) {
      const yShift = yOffsets[i] + centerShift;
      for (const [id, pos] of childSubtrees[i].positions) {
        positions.set(id, { x: pos.x, y: pos.y + yShift });
      }
    }
    for (const [d, ext] of combinedContour) {
      const shifted = { top: ext.top + centerShift, bottom: ext.bottom + centerShift };
      const existing = contour.get(d);
      if (existing) {
        if (shifted.top < existing.top) existing.top = shifted.top;
        if (shifted.bottom > existing.bottom) existing.bottom = shifted.bottom;
      } else {
        contour.set(d, { ...shifted });
      }
    }
    return contour;
  }
  /**
   * Pack an array of subtrees vertically using contour comparison.
   * First subtree stays at y=0; each subsequent one is shifted down
   * just enough to clear the combined contour at all shared depths.
   */
  packSubtrees(subtrees) {
    if (subtrees.length === 0) {
      return { yOffsets: [], combinedContour: /* @__PURE__ */ new Map() };
    }
    const yOffsets = [0];
    const combinedContour = /* @__PURE__ */ new Map();
    for (const [d, ext] of subtrees[0].contour) {
      combinedContour.set(d, { top: ext.top, bottom: ext.bottom });
    }
    for (let i = 1; i < subtrees.length; i++) {
      const sub = subtrees[i];
      let shift = 0;
      for (const [d, ext] of sub.contour) {
        const prev = combinedContour.get(d);
        if (prev !== void 0) {
          const needed = prev.bottom + this.config.verticalGap - ext.top;
          if (needed > shift) shift = needed;
        }
      }
      yOffsets.push(shift);
      for (const [d, ext] of sub.contour) {
        const shifted = { top: ext.top + shift, bottom: ext.bottom + shift };
        const existing = combinedContour.get(d);
        if (existing) {
          if (shifted.top < existing.top) existing.top = shifted.top;
          if (shifted.bottom > existing.bottom) existing.bottom = shifted.bottom;
        } else {
          combinedContour.set(d, { ...shifted });
        }
      }
    }
    return { yOffsets, combinedContour };
  }
  /**
   * Arrange multiple trees within a group using flow-based packing.
   * Lays out each tree internally first, then packs them into rows
   * targeting a roughly square overall shape.
   */
  layoutForest(canvas, groupId) {
    const group = canvas.nodes.get(groupId);
    if (!group) return;
    const forest = buildForest(canvas);
    if (forest.length === 0) return;
    const roots = forest.filter((root) => {
      const cx = root.canvasNode.x + root.canvasNode.width / 2;
      const cy = root.canvasNode.y + root.canvasNode.height / 2;
      return cx >= group.x && cx <= group.x + group.width && cy >= group.y && cy <= group.y + group.height;
    });
    for (const root of roots) {
      this.layoutChildren(canvas, root.canvasNode.id);
    }
    if (roots.length <= 1) return;
    const treeBboxes = roots.map((root) => ({
      root,
      bbox: this.getTreeBbox(root, canvas)
    }));
    treeBboxes.sort((a, b) => {
      const dy = a.root.canvasNode.y - b.root.canvasNode.y;
      if (Math.abs(dy) > 50) return dy;
      return a.root.canvasNode.x - b.root.canvasNode.x;
    });
    const gap = this.config.horizontalGap * 1.5;
    const vGap = this.config.verticalGap * 3;
    const treeSizes = treeBboxes.map((t) => ({
      w: t.bbox.maxX - t.bbox.minX,
      h: t.bbox.maxY - t.bbox.minY
    }));
    const treesPerRow = Math.ceil(Math.sqrt(roots.length));
    const avgWidth = treeSizes.reduce((sum, s) => sum + s.w, 0) / treeSizes.length;
    const targetWidth = treesPerRow * (avgWidth + gap);
    const rows = [];
    let currentRow = [];
    let currentRowWidth = 0;
    for (let i = 0; i < treeBboxes.length; i++) {
      const treeW = treeSizes[i].w + (currentRow.length > 0 ? gap : 0);
      if (currentRow.length > 0 && currentRowWidth + treeW > targetWidth) {
        rows.push(currentRow);
        currentRow = [i];
        currentRowWidth = treeSizes[i].w;
      } else {
        currentRow.push(i);
        currentRowWidth += treeW;
      }
    }
    if (currentRow.length > 0) rows.push(currentRow);
    const PADDING = 20;
    const originX = group.x + PADDING;
    const originY = group.y + PADDING;
    let cursorY = originY;
    const positions = /* @__PURE__ */ new Map();
    for (const row of rows) {
      const rowHeight = Math.max(...row.map((i) => treeSizes[i].h));
      let cursorX = originX;
      for (const i of row) {
        const t = treeBboxes[i];
        const dx = cursorX - t.bbox.minX;
        const dy = cursorY - t.bbox.minY;
        const allNodes = [t.root, ...getDescendants(t.root)];
        for (const treeNode of allNodes) {
          const n = treeNode.canvasNode;
          positions.set(n.id, { x: n.x + dx, y: n.y + dy });
        }
        cursorX += treeSizes[i].w + gap;
      }
      cursorY += rowHeight + vGap;
    }
    this.applyPositions(canvas, positions);
    updateAllEdgeSides(canvas);
    let gMinX = Infinity, gMinY = Infinity, gMaxX = -Infinity, gMaxY = -Infinity;
    for (const [nodeId, pos] of positions) {
      const node = canvas.nodes.get(nodeId);
      if (!node) continue;
      gMinX = Math.min(gMinX, pos.x);
      gMinY = Math.min(gMinY, pos.y);
      gMaxX = Math.max(gMaxX, pos.x + node.width);
      gMaxY = Math.max(gMaxY, pos.y + node.height);
    }
    group.moveAndResize({
      x: gMinX - PADDING,
      y: gMinY - PADDING,
      width: gMaxX - gMinX + PADDING * 2,
      height: gMaxY - gMinY + PADDING * 2
    });
    canvas.requestSave();
  }
  getTreeBbox(root, canvas) {
    const allNodes = [root, ...getDescendants(root)];
    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    for (const treeNode of allNodes) {
      const n = treeNode.canvasNode;
      minX = Math.min(minX, n.x);
      minY = Math.min(minY, n.y);
      maxX = Math.max(maxX, n.x + n.width);
      maxY = Math.max(maxY, n.y + n.height);
    }
    return { minX, minY, maxX, maxY };
  }
  /**
   * Apply calculated positions to canvas nodes.
   */
  applyPositions(canvas, positions, skipAnimationNodeIds = /* @__PURE__ */ new Set()) {
    var _a, _b;
    for (const [nodeId, pos] of positions) {
      const node = canvas.nodes.get(nodeId);
      if (!node) continue;
      if (this.config.animate && !skipAnimationNodeIds.has(nodeId)) {
        (_a = node.nodeEl) == null ? void 0 : _a.addClass("mindmap-animating");
      } else {
        (_b = node.nodeEl) == null ? void 0 : _b.removeClass("mindmap-animating");
      }
      node.moveTo({ x: pos.x, y: pos.y });
    }
    canvas.requestSave();
    canvas.requestFrame();
    if (this.config.animate) {
      canvas.wrapperEl.win.setTimeout(() => {
        var _a2;
        for (const node of canvas.nodes.values()) {
          (_a2 = node.nodeEl) == null ? void 0 : _a2.removeClass("mindmap-animating");
        }
      }, 350);
    }
  }
};

// src/mindmap/color-palette.ts
var DEFAULT_BRANCH_PALETTE = ["1", "2", "3", "4", "5", "6"];
function isValidCanvasColor(color) {
  return /^[1-6]$/.test(color) || /^#[0-9a-f]{6}$/i.test(color);
}
function normalizePalette(colors) {
  const normalized = colors.map((color) => color.trim()).filter(isValidCanvasColor);
  return normalized.length > 0 ? normalized : [...DEFAULT_BRANCH_PALETTE];
}
function parsePalette(value) {
  return normalizePalette(value.split(","));
}
function getBranchNodeColor(branchColor, childCount, colorLeafNodes) {
  return !colorLeafNodes && childCount === 0 ? "" : branchColor;
}

// src/mindmap/branch-colors.ts
var BranchColors = class {
  constructor(canvasApi, palette = DEFAULT_BRANCH_PALETTE, colorLeafNodes = true) {
    this.canvasApi = canvasApi;
    this.colorLeafNodes = colorLeafNodes;
    this.palette = normalizePalette(palette);
  }
  /**
   * Apply auto-coloring to all branches.
   */
  applyColors(canvas) {
    const forest = buildForest(canvas);
    if (forest.length === 0) return;
    for (const root of forest) {
      root.children.forEach((child, index) => {
        const color = this.palette[index % this.palette.length];
        this.colorBranch(canvas, child, color);
      });
    }
    canvas.requestSave();
    canvas.requestFrame();
  }
  /**
   * Complete colors for connections added directly in Canvas. Existing branch
   * colors take precedence, so manually colored branches remain unchanged.
   */
  inheritConnectionColors(canvas) {
    var _a;
    let changed = false;
    for (const edge of canvas.edges.values()) {
      const color = edge.from.node.color || ((_a = this.findIncomingEdge(canvas, edge.from.node)) == null ? void 0 : _a.color);
      if (!color) continue;
      if (!edge.color) {
        edge.setColor(color);
        changed = true;
      }
      if (!edge.to.node.color) {
        edge.to.node.setColor(color);
        changed = true;
      }
    }
    return changed;
  }
  /** Apply a native Canvas color to this node and its descendants. */
  setBranchColor(canvas, nodeId, color) {
    const forest = buildForest(canvas);
    const node = findTreeForNode(forest, nodeId);
    if (!node) return;
    this.colorBranch(canvas, node, color);
    canvas.requestSave();
    canvas.requestFrame();
  }
  /**
   * Color a single branch (node + all descendants + edges).
   */
  colorBranch(canvas, node, color) {
    node.canvasNode.setColor(getBranchNodeColor(color, node.children.length, this.colorLeafNodes));
    const incomingEdge = this.findIncomingEdge(canvas, node.canvasNode);
    if (incomingEdge) {
      incomingEdge.setColor(color);
    }
    for (const child of node.children) {
      this.colorBranch(canvas, child, color);
    }
  }
  /**
   * Find the edge pointing TO this node.
   */
  findIncomingEdge(canvas, node) {
    var _a;
    const edges = this.canvasApi.getConnectedEdges(canvas, node);
    return (_a = edges.find((e) => e.to.node.id === node.id)) != null ? _a : null;
  }
};

// src/ui/keyboard-handler.ts
var import_obsidian3 = require("obsidian");

// src/canvas/branch-collapse.ts
var import_obsidian2 = require("obsidian");

// src/canvas/branch-collapse-state.ts
function collectCollapsedDescendantIds(collapsedIds, getChildren) {
  const hidden = /* @__PURE__ */ new Set();
  for (const collapsedId of collapsedIds) {
    const visited = /* @__PURE__ */ new Set([collapsedId]);
    const queue = [...getChildren(collapsedId)];
    while (queue.length > 0) {
      const id = queue.shift();
      if (visited.has(id)) continue;
      visited.add(id);
      hidden.add(id);
      queue.push(...getChildren(id));
    }
  }
  return hidden;
}

// src/canvas/branch-collapse.ts
var BUTTON_CLASS = "cammvas-canvas-collapse-button";
var COLLAPSED_HIDDEN_CLASS = "cammvas-canvas-branch-hidden";
function registerBranchCollapse(canvas, canvasApi) {
  let disposed = false;
  let refreshRaf = null;
  let observer;
  const win = canvas.wrapperEl.win;
  const scheduleRefresh = () => {
    if (disposed || refreshRaf !== null) return;
    refreshRaf = win.requestAnimationFrame(() => {
      refreshRaf = null;
      refresh();
    });
  };
  const toggle = (nodeId) => {
    var _a;
    const data = canvas.getData();
    const collapsed = new Set((_a = data.mindmapCollapsed) != null ? _a : []);
    if (collapsed.has(nodeId)) collapsed.delete(nodeId);
    else collapsed.add(nodeId);
    data.mindmapCollapsed = [...collapsed];
    canvas.setData(data);
    canvas.requestSave();
    scheduleRefresh();
    win.setTimeout(scheduleRefresh, 50);
  };
  const syncButton = (node, collapsed, descendantCount) => {
    let button = node.nodeEl.querySelector(`:scope > .${BUTTON_CLASS}`);
    if (!button) {
      button = node.nodeEl.createEl("button");
      button.className = `${BUTTON_CLASS} clickable-icon`;
      button.setAttribute("type", "button");
      button.addEventListener("pointerdown", (event) => {
        event.preventDefault();
        event.stopPropagation();
      });
      button.addEventListener("click", (event) => {
        event.preventDefault();
        event.stopPropagation();
        toggle(node.id);
      });
      node.nodeEl.appendChild(button);
    }
    const state = collapsed ? "collapsed" : "expanded";
    if (button.dataset.state !== state) {
      button.empty();
      (0, import_obsidian2.setIcon)(button, collapsed ? "chevron-right" : "chevron-down");
      button.dataset.state = state;
    }
    button.setAttribute(
      "aria-label",
      `${collapsed ? "Expand" : "Collapse"} branch (${descendantCount} descendant${descendantCount === 1 ? "" : "s"})`
    );
  };
  const setEdgeHidden = (edge, hidden) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o;
    (_a = edge.lineGroupEl) == null ? void 0 : _a.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_b = edge.lineEl) == null ? void 0 : _b.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_c = edge.lineEndGroupEl) == null ? void 0 : _c.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_d = edge.startGroupEl) == null ? void 0 : _d.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_e = edge.endGroupEl) == null ? void 0 : _e.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_g = (_f = edge.fromLineEnd) == null ? void 0 : _f.el) == null ? void 0 : _g.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_i = (_h = edge.toLineEnd) == null ? void 0 : _h.el) == null ? void 0 : _i.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_k = (_j = edge.labelElement) == null ? void 0 : _j.wrapperEl) == null ? void 0 : _k.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_m = (_l = edge.path) == null ? void 0 : _l.display) == null ? void 0 : _m.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
    (_o = (_n = edge.path) == null ? void 0 : _n.interaction) == null ? void 0 : _o.toggleClass(COLLAPSED_HIDDEN_CLASS, hidden);
  };
  const refresh = () => {
    var _a;
    if (disposed) return;
    observer.disconnect();
    canvasApi.invalidateEdgeIndex();
    const collapsedIds = new Set((_a = canvas.getData().mindmapCollapsed) != null ? _a : []);
    const childIds = (nodeId) => canvasApi.getOutgoingEdges(canvas, nodeId).map((edge) => edge.to.node.id);
    const hiddenIds = collectCollapsedDescendantIds(collapsedIds, childIds);
    const hiddenSelection = Array.from(canvas.selection).some((item) => {
      if ("nodeEl" in item) return hiddenIds.has(item.id);
      return hiddenIds.has(item.from.node.id) || hiddenIds.has(item.to.node.id);
    });
    if (hiddenSelection) canvas.deselectAll();
    for (const node of canvas.nodes.values()) {
      node.nodeEl.toggleClass(COLLAPSED_HIDDEN_CLASS, hiddenIds.has(node.id));
      const children = childIds(node.id);
      const existing = node.nodeEl.querySelector(`:scope > .${BUTTON_CLASS}`);
      if (children.length === 0) {
        existing == null ? void 0 : existing.remove();
        continue;
      }
      const descendants = collectCollapsedDescendantIds([node.id], childIds);
      syncButton(node, collapsedIds.has(node.id), descendants.size);
    }
    for (const edge of canvas.edges.values()) {
      setEdgeHidden(
        edge,
        hiddenIds.has(edge.from.node.id) || hiddenIds.has(edge.to.node.id)
      );
    }
    observer.observe(canvas.wrapperEl, { childList: true, subtree: true });
  };
  const Observer = Reflect.get(win, "MutationObserver");
  observer = new Observer(scheduleRefresh);
  observer.observe(canvas.wrapperEl, { childList: true, subtree: true });
  refresh();
  return {
    refresh: scheduleRefresh,
    cleanup: () => {
      var _a;
      disposed = true;
      observer.disconnect();
      if (refreshRaf !== null) win.cancelAnimationFrame(refreshRaf);
      for (const node of canvas.nodes.values()) {
        node.nodeEl.removeClass(COLLAPSED_HIDDEN_CLASS);
        (_a = node.nodeEl.querySelector(`:scope > .${BUTTON_CLASS}`)) == null ? void 0 : _a.remove();
      }
      for (const edge of canvas.edges.values()) setEdgeHidden(edge, false);
    }
  };
}

// src/ui/editing-enter.ts
function shouldCreateSiblingOnEnter(event, enabled, isEditing) {
  return enabled && isEditing && event.key === "Enter" && !event.shiftKey && !event.ctrlKey && !event.altKey && !event.metaKey && !event.isComposing;
}
function shouldStartEditingOnEnter(event, enabled, isEditing) {
  return enabled && !isEditing && event.key === "Enter" && !event.shiftKey && !event.ctrlKey && !event.altKey && !event.metaKey && !event.isComposing;
}
function shouldCreateChildOnTab(event, enabled, hasSelectedNode) {
  return enabled && hasSelectedNode && event.key === "Tab" && !event.shiftKey && !event.ctrlKey && !event.altKey && !event.metaKey && !event.isComposing;
}

// src/ui/editing-state.ts
function isNodeEditorFocused(node) {
  var _a;
  const editor = (_a = node.child) == null ? void 0 : _a.editor;
  return typeof (editor == null ? void 0 : editor.hasFocus) === "function" && editor.hasFocus();
}

// src/ui/keyboard-handler.ts
var KeyboardHandler = class {
  constructor(plugin, canvasApi, nodeOps, layoutEngine, branchColors, autoColorEnabled, autoLayoutEnabled, arrowKeyNavigationEnabled, centerNodeOnArrowNavigation, enterCreatesSiblingEnabled, isMindmapEnabled = () => true, onNodesChanged = () => {
  }) {
    this.plugin = plugin;
    this.canvasApi = canvasApi;
    this.nodeOps = nodeOps;
    this.layoutEngine = layoutEngine;
    this.branchColors = branchColors;
    this.autoColorEnabled = autoColorEnabled;
    this.autoLayoutEnabled = autoLayoutEnabled;
    this.arrowKeyNavigationEnabled = arrowKeyNavigationEnabled;
    this.centerNodeOnArrowNavigation = centerNodeOnArrowNavigation;
    this.enterCreatesSiblingEnabled = enterCreatesSiblingEnabled;
    this.isMindmapEnabled = isMindmapEnabled;
    this.onNodesChanged = onNodesChanged;
    /** Called before actions that leave the current node, to finalize auto-resize. */
    this.onBeforeLeaveNode = null;
    /** Padding (px) added around target node when zooming after navigation. */
    this.zoomPadding = 0;
    this.arrowKeySelectionOnly = false;
    this.arrowNavigationCanvas = null;
    this.arrowKeyRestorers = [];
  }
  register() {
    this.plugin.addCommand({
      id: "mindmap-edit-node",
      name: "Edit selected node",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        const activeEl = canvas.wrapperEl.doc.activeElement;
        if (activeEl && !canvas.wrapperEl.contains(activeEl)) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        if (node.isEditing) return false;
        if (checking) return true;
        node.startEditing();
      }
    });
    this.plugin.addCommand({
      id: "mindmap-save-node",
      name: "Save and exit edit mode",
      checkCallback: (checking) => {
        var _a;
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        if (!node.isEditing) return false;
        if (checking) return true;
        (_a = this.onBeforeLeaveNode) == null ? void 0 : _a.call(this);
        node.blur();
      }
    });
    this.plugin.addCommand({
      id: "mindmap-add-child",
      name: "Add child node",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        if (checking) return true;
        this.addChildNode(canvas, node);
      }
    });
    this.plugin.addCommand({
      id: "mindmap-add-sibling",
      name: "Add sibling node",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        if (checking) return true;
        this.addSiblingNode(canvas, node);
      }
    });
    this.plugin.addCommand({
      id: "mindmap-delete-node",
      name: "Delete node and focus parent",
      checkCallback: (checking) => {
        var _a;
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        if (checking) return true;
        (_a = this.onBeforeLeaveNode) == null ? void 0 : _a.call(this);
        const parent = this.nodeOps.deleteAndFocusParent(canvas, node);
        if (parent) {
          this.onNodesChanged(canvas);
          this.canvasApi.selectAndEdit(canvas, parent, this.zoomPadding);
        }
      }
    });
    this.plugin.addCommand({
      id: "mindmap-flip-branch",
      name: "Flip branch to other side",
      checkCallback: (checking) => {
        var _a;
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (!this.isMindmapEnabled(canvas)) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        const parent = this.canvasApi.getParentNode(canvas, node);
        if (!parent) return false;
        if (checking) return true;
        const wasEditing = node.isEditing;
        if (!wasEditing) (_a = this.onBeforeLeaveNode) == null ? void 0 : _a.call(this);
        const parentNode = this.nodeOps.flipBranch(canvas, node);
        if (parentNode) {
          this.layoutEngine.layoutChildren(canvas, parentNode.id);
          if (this.autoColorEnabled()) {
            this.branchColors.applyColors(canvas);
          }
          this.onNodesChanged(canvas);
          if (wasEditing) node.startEditing();
        }
      }
    });
    this.plugin.addCommand({
      id: "mindmap-toggle-balance",
      name: "Toggle balanced layout",
      checkCallback: (checking) => {
        var _a;
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (!this.isMindmapEnabled(canvas)) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        const children = this.canvasApi.getChildNodes(canvas, node);
        if (children.length < 2) return false;
        if (checking) return true;
        (_a = this.onBeforeLeaveNode) == null ? void 0 : _a.call(this);
        const nodeCx = node.x + node.width / 2;
        let allRight = true;
        let allLeft = true;
        for (const child of children) {
          const childCx = child.x + child.width / 2;
          if (childCx >= nodeCx) allLeft = false;
          else allRight = false;
        }
        const allOneSide = allRight || allLeft;
        if (allOneSide) {
          const sorted = [...children].sort((a, b) => a.y - b.y);
          for (let i = 0; i < sorted.length; i++) {
            const child = sorted[i];
            if (i % 2 === 1) {
              const mirrorX = nodeCx - (child.x + child.width / 2 - nodeCx) - child.width / 2;
              child.moveTo({ x: mirrorX, y: child.y });
            }
          }
        } else {
          for (const child of children) {
            const childCx = child.x + child.width / 2;
            if (childCx < nodeCx) {
              const mirrorX = nodeCx + (nodeCx - childCx) - child.width / 2;
              child.moveTo({ x: mirrorX, y: child.y });
            }
          }
        }
        this.layoutEngine.layoutChildren(canvas, node.id);
        if (this.autoColorEnabled()) {
          this.branchColors.applyColors(canvas);
        }
        this.onNodesChanged(canvas);
      }
    });
    this.plugin.addCommand({
      id: "mindmap-nav-right",
      name: "Navigate right",
      checkCallback: (checking) => {
        return this.navigateCommand(checking, (tree) => {
          var _a, _b;
          if (!tree.direction) {
            const rightChildren = tree.children.filter((c) => c.direction === "right");
            return this.nearestChild(tree, rightChildren);
          }
          const nodeCx = tree.canvasNode.x + tree.canvasNode.width / 2;
          if (tree.children.length > 0) {
            const childCx = tree.children[0].canvasNode.x + tree.children[0].canvasNode.width / 2;
            if (childCx >= nodeCx) return this.nearestChild(tree);
            return (_b = (_a = tree.parent) == null ? void 0 : _a.canvasNode) != null ? _b : null;
          }
          if (tree.parent) {
            const parentCx = tree.parent.canvasNode.x + tree.parent.canvasNode.width / 2;
            if (parentCx >= nodeCx) return tree.parent.canvasNode;
          }
          return null;
        }, "right");
      }
    });
    this.plugin.addCommand({
      id: "mindmap-nav-left",
      name: "Navigate left",
      checkCallback: (checking) => {
        return this.navigateCommand(checking, (tree) => {
          var _a, _b;
          if (!tree.direction) {
            const leftChildren = tree.children.filter((c) => c.direction === "left");
            return this.nearestChild(tree, leftChildren);
          }
          const nodeCx = tree.canvasNode.x + tree.canvasNode.width / 2;
          if (tree.children.length > 0) {
            const childCx = tree.children[0].canvasNode.x + tree.children[0].canvasNode.width / 2;
            if (childCx < nodeCx) return this.nearestChild(tree);
            return (_b = (_a = tree.parent) == null ? void 0 : _a.canvasNode) != null ? _b : null;
          }
          if (tree.parent) {
            const parentCx = tree.parent.canvasNode.x + tree.parent.canvasNode.width / 2;
            if (parentCx < nodeCx) return tree.parent.canvasNode;
          }
          return null;
        }, "left");
      }
    });
    this.plugin.addCommand({
      id: "mindmap-nav-next-sibling",
      name: "Navigate to next sibling",
      checkCallback: (checking) => {
        return this.navigateCommand(checking, (tree) => {
          if (!tree.parent) return null;
          const balanced = this.balancedSiblingNav(tree, "down");
          if (balanced) return balanced;
          const next = getNextSibling(tree);
          if (next) return next.canvasNode;
          return tree.parent.children[0].canvasNode;
        }, "down");
      }
    });
    this.plugin.addCommand({
      id: "mindmap-nav-prev-sibling",
      name: "Navigate to previous sibling",
      checkCallback: (checking) => {
        return this.navigateCommand(checking, (tree) => {
          if (!tree.parent) return null;
          const balanced = this.balancedSiblingNav(tree, "up");
          if (balanced) return balanced;
          const prev = getPrevSibling(tree);
          if (prev) return prev.canvasNode;
          const siblings = tree.parent.children;
          return siblings[siblings.length - 1].canvasNode;
        }, "up");
      }
    });
    this.registerPhysicalKeyShortcuts();
    this.plugin.register(() => this.unregisterArrowKeyNavigation());
  }
  addChildNode(canvas, node, options = {}) {
    var _a;
    const selectedText = node.isEditing && options.transferSelection !== false ? this.extractAndDeleteSelection(node) : null;
    (_a = this.onBeforeLeaveNode) == null ? void 0 : _a.call(this);
    const newNode = this.nodeOps.addChild(canvas, node);
    if (!newNode) return;
    if (selectedText) newNode.setText(selectedText);
    if (this.autoLayoutEnabled()) {
      this.layoutEngine.layoutChildren(canvas, node.id, /* @__PURE__ */ new Set([newNode.id]));
    }
    if (this.autoColorEnabled() && this.isMindmapEnabled(canvas)) {
      this.branchColors.applyColors(canvas);
    }
    this.onNodesChanged(canvas);
    this.canvasApi.selectAndEdit(canvas, newNode, this.zoomPadding, options.immediateEdit);
  }
  addSiblingNode(canvas, node, options = {}) {
    var _a;
    const selectedText = node.isEditing && options.transferSelection !== false ? this.extractAndDeleteSelection(node) : null;
    (_a = this.onBeforeLeaveNode) == null ? void 0 : _a.call(this);
    const newNode = this.nodeOps.addSibling(canvas, node);
    if (!newNode) return;
    if (selectedText) newNode.setText(selectedText);
    const parent = this.canvasApi.getParentNode(canvas, node);
    if (parent && this.autoLayoutEnabled()) {
      this.layoutEngine.layoutChildren(canvas, parent.id, /* @__PURE__ */ new Set([newNode.id]));
    }
    if (this.autoColorEnabled() && this.isMindmapEnabled(canvas)) {
      this.branchColors.applyColors(canvas);
    }
    this.onNodesChanged(canvas);
    this.canvasApi.selectAndEdit(canvas, newNode, this.zoomPadding, options.immediateEdit);
  }
  handleEnter(canvas, event) {
    const node = this.canvasApi.getSelectedNode(canvas);
    if (!node || !this.isMindmapEnabled(canvas)) return false;
    if (shouldStartEditingOnEnter(event, this.enterCreatesSiblingEnabled(), node.isEditing)) {
      event.preventDefault();
      event.stopImmediatePropagation();
      node.startEditing();
      return true;
    }
    if (!shouldCreateSiblingOnEnter(event, this.enterCreatesSiblingEnabled(), node.isEditing)) return false;
    const executeCommand = this.getCommandExecutor();
    if (!executeCommand) return false;
    event.preventDefault();
    event.stopImmediatePropagation();
    executeCommand("cammvas:mindmap-add-sibling");
    return true;
  }
  handleChildTab(canvas, event) {
    const node = this.canvasApi.getSelectedNode(canvas);
    if (!node || !this.isMindmapEnabled(canvas)) return false;
    if (!shouldCreateChildOnTab(event, this.enterCreatesSiblingEnabled(), true)) return false;
    const executeCommand = this.getCommandExecutor();
    if (!executeCommand) return false;
    event.preventDefault();
    event.stopImmediatePropagation();
    executeCommand("cammvas:mindmap-add-child");
    return true;
  }
  registerArrowKeyNavigation(canvas) {
    var _a;
    this.unregisterArrowKeyNavigation();
    const commandIds = [
      ["ArrowRight", "cammvas:mindmap-nav-right"],
      ["ArrowLeft", "cammvas:mindmap-nav-left"],
      ["ArrowDown", "cammvas:mindmap-nav-next-sibling"],
      ["ArrowUp", "cammvas:mindmap-nav-prev-sibling"]
    ];
    const entries = (_a = canvas.view.scope) == null ? void 0 : _a.keys;
    if (!entries) return;
    for (const [key, commandId] of commandIds) {
      const entry = entries.find(
        (candidate) => candidate.key === key && candidate.modifiers === ""
      );
      if (!entry) continue;
      const original = entry.func;
      const replacement = (event, context) => {
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!this.arrowKeyNavigationEnabled() || !this.isMindmapEnabled(canvas) || !node || isNodeEditorFocused(node)) {
          return original(event, context);
        }
        const executeCommand = this.getCommandExecutor();
        if (!executeCommand) return original(event, context);
        event.preventDefault();
        this.arrowKeySelectionOnly = true;
        this.arrowNavigationCanvas = canvas;
        try {
          executeCommand(commandId);
        } finally {
          this.arrowKeySelectionOnly = false;
          this.arrowNavigationCanvas = null;
        }
        return false;
      };
      entry.func = replacement;
      this.arrowKeyRestorers.push(() => {
        if (entry.func === replacement) entry.func = original;
      });
    }
    this.registerCanvasKeyOverride(
      canvas,
      "Enter",
      (event) => this.handleEnter(canvas, event)
    );
    this.registerCanvasKeyOverride(
      canvas,
      "Tab",
      (event) => this.handleChildTab(canvas, event)
    );
  }
  registerCanvasKeyOverride(canvas, key, handle) {
    const scope = canvas.view.scope;
    const existing = scope.keys.find(
      (candidate) => candidate.key === key && candidate.modifiers === ""
    );
    const original = existing == null ? void 0 : existing.func;
    const replacement = (event, context) => {
      var _a;
      if (handle(event)) return false;
      if (original) return original(event, context);
      return (_a = scope.parent) == null ? void 0 : _a.handleKey(event, context);
    };
    if (existing) {
      existing.func = replacement;
      this.arrowKeyRestorers.push(() => {
        if (existing.func === replacement) existing.func = original;
      });
      return;
    }
    const entry = scope.register([], key, replacement);
    this.arrowKeyRestorers.push(() => scope.unregister(entry));
  }
  unregisterArrowKeyNavigation() {
    for (const restore of this.arrowKeyRestorers) restore();
    this.arrowKeyRestorers = [];
    this.arrowNavigationCanvas = null;
  }
  /**
   * Access the CodeMirror 6 EditorView inside a canvas node's iframe.
   */
  getEditorView(node) {
    var _a, _b, _c, _d, _e, _f;
    const iframe = (_a = node.contentEl) == null ? void 0 : _a.querySelector("iframe");
    const doc = (_c = iframe == null ? void 0 : iframe.contentDocument) != null ? _c : (_b = node.contentEl) == null ? void 0 : _b.ownerDocument;
    if (!doc) return null;
    const container = (_d = iframe == null ? void 0 : iframe.contentDocument) != null ? _d : node.contentEl;
    const cmContent = container == null ? void 0 : container.querySelector(".cm-content");
    return (_f = (_e = cmContent == null ? void 0 : cmContent.cmView) == null ? void 0 : _e.view) != null ? _f : null;
  }
  /**
   * Extract the selected text from a node's editor and delete it.
   * Returns the selected text, or null if nothing is selected.
   */
  extractAndDeleteSelection(node) {
    const view = this.getEditorView(node);
    if (!view) return null;
    const { from, to } = view.state.selection.main;
    if (from === to) return null;
    const text = view.state.sliceDoc(from, to);
    view.dispatch({ changes: { from, to, insert: "" } });
    return text;
  }
  /**
   * Fallback keydown listener that uses event.code (physical key position)
   * instead of event.key (character). Activates only when a non-Latin layout
   * is detected (event.key doesn't match the expected Latin character),
   * so it won't double-fire with Obsidian's built-in hotkey system.
   */
  registerPhysicalKeyShortcuts() {
    const shortcuts = [
      { code: "Period", key: ".", ctrl: true, shift: false, alt: false, cmdId: "cammvas:mindmap-add-child" },
      { code: "KeyS", key: "s", ctrl: true, shift: false, alt: false, cmdId: "cammvas:mindmap-save-node" },
      { code: "KeyS", key: "s", ctrl: true, shift: true, alt: false, cmdId: "cammvas:mindmap-flip-branch" },
      { code: "KeyD", key: "d", ctrl: true, shift: true, alt: false, cmdId: "cammvas:mindmap-toggle-balance" },
      { code: "KeyL", key: "l", ctrl: true, shift: true, alt: false, cmdId: "cammvas:mindmap-resize-subtree" },
      { code: "KeyR", key: "r", ctrl: true, shift: true, alt: true, cmdId: "cammvas:mindmap-resize-all" }
    ];
    const keydownHandler = (e) => {
      var _a, _b, _c;
      const canvas = this.canvasApi.getActiveCanvas();
      if (!canvas) return;
      const ctrlOrCmd = import_obsidian3.Platform.isMacOS ? e.metaKey : e.ctrlKey;
      if (!ctrlOrCmd) return;
      if (e.code === "KeyZ" && !e.altKey && e.key.toLowerCase() !== "z") {
        e.preventDefault();
        e.stopPropagation();
        if (e.shiftKey) {
          (_a = canvas.redo) == null ? void 0 : _a.call(canvas);
        } else {
          (_b = canvas.undo) == null ? void 0 : _b.call(canvas);
        }
        return;
      }
      if (e.code === "KeyY" && !e.shiftKey && !e.altKey && e.key.toLowerCase() !== "y") {
        e.preventDefault();
        e.stopPropagation();
        (_c = canvas.redo) == null ? void 0 : _c.call(canvas);
        return;
      }
      const executeCommand = this.getCommandExecutor();
      if (!executeCommand) return;
      for (const s of shortcuts) {
        if (e.code === s.code && ctrlOrCmd === s.ctrl && e.shiftKey === s.shift && e.altKey === s.alt) {
          if (e.key.toLowerCase() === s.key) return;
          e.preventDefault();
          e.stopPropagation();
          executeCommand(s.cmdId);
          return;
        }
      }
    };
    const registerDocument = (doc) => {
      this.plugin.registerDomEvent(doc, "keydown", keydownHandler);
    };
    registerDocument(this.plugin.app.workspace.containerEl.doc);
    this.plugin.registerEvent(this.plugin.app.workspace.on("window-open", (_workspace, win) => {
      registerDocument(win.document);
    }));
  }
  getCommandExecutor() {
    const app = this.plugin.app;
    if (!this.hasCommandExecutor(app)) return null;
    return (id) => Boolean(app.commands.executeCommandById(id));
  }
  hasCommandExecutor(value) {
    if (typeof value !== "object" || value === null || !("commands" in value)) return false;
    const commands = value.commands;
    return typeof commands === "object" && commands !== null && "executeCommandById" in commands && typeof commands.executeCommandById === "function";
  }
  /**
   * Navigate to a sibling using side-aware Y-order for balanced (two-sided) layouts.
   * Within the same side, moves to the next/prev sibling by Y position.
   * At the boundary, crosses to the other side's extreme (top↔top, bottom↔bottom).
   * Returns null if siblings are all on one side (caller should fall back to Y-order).
   */
  balancedSiblingNav(tree, direction) {
    if (!tree.parent || tree.parent.children.length < 2) return null;
    const siblings = tree.parent.children;
    const parentNode = tree.parent.canvasNode;
    const parentCx = parentNode.x + parentNode.width / 2;
    const leftSiblings = [];
    const rightSiblings = [];
    for (const s of siblings) {
      const sCx = s.canvasNode.x + s.canvasNode.width / 2;
      if (sCx < parentCx) leftSiblings.push(s);
      else rightSiblings.push(s);
    }
    if (leftSiblings.length === 0 || rightSiblings.length === 0) return null;
    const byCy = (a, b) => a.canvasNode.y + a.canvasNode.height / 2 - (b.canvasNode.y + b.canvasNode.height / 2);
    leftSiblings.sort(byCy);
    rightSiblings.sort(byCy);
    const currentCx = tree.canvasNode.x + tree.canvasNode.width / 2;
    const sameSide = currentCx < parentCx ? leftSiblings : rightSiblings;
    const otherSide = currentCx < parentCx ? rightSiblings : leftSiblings;
    const idx = sameSide.indexOf(tree);
    if (idx === -1) return null;
    if (direction === "down") {
      if (idx < sameSide.length - 1) return sameSide[idx + 1].canvasNode;
      return otherSide[otherSide.length - 1].canvasNode;
    } else {
      if (idx > 0) return sameSide[idx - 1].canvasNode;
      return otherSide[0].canvasNode;
    }
  }
  /**
   * Find the child whose vertical center is closest to the current node's.
   */
  nearestChild(tree, candidates) {
    const children = candidates != null ? candidates : tree.children;
    if (children.length === 0) return null;
    const nodeCy = tree.canvasNode.y + tree.canvasNode.height / 2;
    let best = children[0];
    let bestDist = Math.abs(best.canvasNode.y + best.canvasNode.height / 2 - nodeCy);
    for (let i = 1; i < children.length; i++) {
      const childCy = children[i].canvasNode.y + children[i].canvasNode.height / 2;
      const dist = Math.abs(childCy - nodeCy);
      if (dist < bestDist) {
        best = children[i];
        bestDist = dist;
      }
    }
    return best.canvasNode;
  }
  /**
   * Helper for navigation commands.
   */
  navigateCommand(checking, getTarget, spatialDirection) {
    var _a, _b, _c, _d;
    const canvas = (_a = this.arrowNavigationCanvas) != null ? _a : this.canvasApi.getActiveCanvas();
    if (!canvas) return false;
    const node = this.canvasApi.getSelectedNode(canvas);
    if (!node) return false;
    if (checking) return true;
    const forest = buildForest(canvas);
    if (forest.length === 0) return false;
    const treeNode = findTreeForNode(forest, node.id);
    if (!treeNode) return false;
    const groupIds = getGroupIds(canvas);
    const spatialCandidates = Array.from(canvas.nodes.values()).filter(
      (candidate) => !groupIds.has(candidate.id) && !candidate.nodeEl.hasClass(COLLAPSED_HIDDEN_CLASS)
    );
    const target = (_c = (_b = getTarget(treeNode)) != null ? _b : findNearestNodeInDirection(node, spatialCandidates, spatialDirection)) != null ? _c : node;
    if (target.id === node.id) return true;
    if (target.nodeEl.hasClass(COLLAPSED_HIDDEN_CLASS)) return false;
    (_d = this.onBeforeLeaveNode) == null ? void 0 : _d.call(this);
    if (!this.arrowKeySelectionOnly) {
      this.onNodesChanged(canvas);
    }
    if (this.arrowKeySelectionOnly) {
      if (this.centerNodeOnArrowNavigation()) {
        this.canvasApi.selectAndZoom(canvas, target, this.zoomPadding);
      } else {
        this.canvasApi.selectAndReveal(canvas, target, this.zoomPadding);
      }
    } else {
      this.canvasApi.selectAndEdit(canvas, target, this.zoomPadding);
    }
    return true;
  }
};

// src/ui/navigation.ts
var Navigation = class {
  constructor(canvasApi) {
    this.canvasApi = canvasApi;
  }
  /**
   * Select the entire tree (root + all descendants) that a node belongs to.
   * Triggered by Alt+click on a node.
   */
  selectTree(canvas, node) {
    const forest = buildForest(canvas);
    if (forest.length === 0) return;
    const treeNode = findTreeForNode(forest, node.id);
    if (!treeNode) return;
    let root = treeNode;
    while (root.parent) root = root.parent;
    const allNodes = [root, ...getDescendants(root)];
    canvas.deselectAll();
    for (const n of allNodes) {
      canvas.selection.add(n.canvasNode);
    }
    canvas.requestFrame();
  }
  /**
   * Zoom to fit an entire branch (node + all descendants).
   * Triggered by Ctrl+click on a node.
   */
  zoomToBranch(canvas, node) {
    const forest = buildForest(canvas);
    if (forest.length === 0) return;
    const treeNode = findTreeForNode(forest, node.id);
    if (!treeNode) return;
    const allNodes = [treeNode, ...getDescendants(treeNode)];
    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    for (const n of allNodes) {
      const cn = n.canvasNode;
      minX = Math.min(minX, cn.x);
      minY = Math.min(minY, cn.y);
      maxX = Math.max(maxX, cn.x + cn.width);
      maxY = Math.max(maxY, cn.y + cn.height);
    }
    const pad = 50;
    canvas.zoomToBbox({
      minX: minX - pad,
      minY: minY - pad,
      maxX: maxX + pad,
      maxY: maxY + pad
    });
  }
  /**
   * Register Ctrl+click handler for zoom-to-branch.
   */
  registerClickHandler(canvas) {
    var _a;
    const handler = (e) => {
      if (!e.ctrlKey && !e.metaKey && !e.altKey) return;
      const target = e.target;
      if (!isHtmlElement(target)) return;
      if (target.closest(".canvas-node-connection-point")) return;
      const nodeEl = target.closest(".canvas-node");
      if (!nodeEl) return;
      for (const node of canvas.nodes.values()) {
        if (node.nodeEl === nodeEl) {
          e.preventDefault();
          e.stopPropagation();
          if (e.altKey) {
            this.selectTree(canvas, node);
          } else {
            this.zoomToBranch(canvas, node);
          }
          break;
        }
      }
    };
    (_a = canvas.wrapperEl) == null ? void 0 : _a.addEventListener("click", handler, true);
    return () => {
      var _a2;
      (_a2 = canvas.wrapperEl) == null ? void 0 : _a2.removeEventListener("click", handler, true);
    };
  }
};

// src/settings.ts
var import_obsidian4 = require("obsidian");
var DEFAULT_SETTINGS = {
  autoLayout: true,
  autoColor: true,
  branchPalette: [...DEFAULT_BRANCH_PALETTE],
  colorLeafNodes: true,
  arrowKeyNavigation: true,
  centerNodeOnArrowNavigation: false,
  dragToReparent: true,
  enterCreatesSibling: true,
  horizontalGap: 80,
  verticalGap: 20,
  defaultNodeWidth: 300,
  defaultNodeHeight: 60,
  maxNodeHeight: 300,
  defaultMindmapMode: true,
  navigationZoomPadding: 200,
  mouseNavigation: false
};
var BOOLEAN_SETTING_KEYS = [
  "autoLayout",
  "autoColor",
  "colorLeafNodes",
  "arrowKeyNavigation",
  "centerNodeOnArrowNavigation",
  "dragToReparent",
  "enterCreatesSibling",
  "defaultMindmapMode",
  "mouseNavigation"
];
var POSITIVE_NUMBER_SETTING_KEYS = [
  "horizontalGap",
  "verticalGap",
  "defaultNodeWidth",
  "defaultNodeHeight",
  "maxNodeHeight"
];
function isSettingsData(value) {
  return typeof value === "object" && value !== null;
}
function isSettingKey(key) {
  return key in DEFAULT_SETTINGS;
}
function normalizeSettings(data) {
  const settings = {
    ...DEFAULT_SETTINGS,
    branchPalette: [...DEFAULT_SETTINGS.branchPalette]
  };
  if (!isSettingsData(data)) return settings;
  for (const key of BOOLEAN_SETTING_KEYS) {
    const value = data[key];
    if (typeof value === "boolean") settings[key] = value;
  }
  for (const key of POSITIVE_NUMBER_SETTING_KEYS) {
    const value = data[key];
    if (typeof value === "number" && Number.isFinite(value) && value > 0) {
      settings[key] = value;
    }
  }
  const zoomPadding = data.navigationZoomPadding;
  if (typeof zoomPadding === "number" && Number.isFinite(zoomPadding) && zoomPadding >= 0) {
    settings.navigationZoomPadding = zoomPadding;
  }
  const palette = data.branchPalette;
  if (Array.isArray(palette) && palette.every((color) => typeof color === "string")) {
    settings.branchPalette = parsePalette(palette.join(","));
  }
  return settings;
}
var MindMapSettingTab = class extends import_obsidian4.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  getSettingDefinitions() {
    const positiveNumber = (key) => ({
      type: "number",
      key,
      min: 1,
      step: 1,
      validate: (value) => value > 0 ? void 0 : "Enter a positive number."
    });
    return [
      { name: "Default mindmap mode", desc: "Whether canvases default to mindmap mode (can be toggled per canvas)", control: { type: "toggle", key: "defaultMindmapMode" } },
      { name: "Auto-layout", desc: "Automatically arrange nodes when Cammvas creates them", control: { type: "toggle", key: "autoLayout" } },
      { name: "Auto-color branches", desc: "Assign distinct colors to top-level branches", control: { type: "toggle", key: "autoColor" } },
      { name: "Branch color palette", desc: "Comma-separated Canvas colors (1-6) or hex colors, assigned to top-level branches", control: { type: "text", key: "branchPalette", placeholder: "1, 2, 3, 4, 5, 6" } },
      { name: "Color leaf nodes", desc: "Turn off to leave terminal nodes neutral while keeping their incoming edge colored", control: { type: "toggle", key: "colorLeafNodes" } },
      { name: "Arrow key navigation", desc: "Navigate between selected mind map nodes with the arrow keys; disable to move Canvas cards natively", control: { type: "toggle", key: "arrowKeyNavigation" } },
      { name: "Center node during arrow navigation", desc: "Always center the selected node when navigating with the arrow keys instead of moving the viewport only when needed", control: { type: "toggle", key: "centerNodeOnArrowNavigation" } },
      { name: "Drag to reparent", desc: import_obsidian4.Platform.isMobile ? "Long-press and drag a node onto another node to make it a child while preserving its branch" : "Drop a node onto another node to make it a child while preserving its branch", control: { type: "toggle", key: "dragToReparent" } },
      { name: "Mind mapping Enter and Tab", desc: import_obsidian4.Platform.isMobile ? "Use Enter and Tab from a hardware keyboard to create sibling and child nodes" : "Enter creates a sibling while editing, Tab creates a child, and Shift+Enter inserts a new line", control: { type: "toggle", key: "enterCreatesSibling" } },
      { name: "Horizontal gap", desc: "Space between parent and child nodes (px)", control: positiveNumber("horizontalGap") },
      { name: "Vertical gap", desc: "Space between sibling nodes (px)", control: positiveNumber("verticalGap") },
      { name: "Default node width", desc: "Width of newly created nodes (px)", control: positiveNumber("defaultNodeWidth") },
      { name: "Default node height", desc: "Height of newly created nodes (px)", control: positiveNumber("defaultNodeHeight") },
      { name: "Max node height", desc: "Maximum height a node can grow to before scrolling (px)", control: positiveNumber("maxNodeHeight") },
      { name: "Mouse back/forward navigation", desc: "Use mouse back/forward buttons for in-canvas navigation instead of Obsidian's default note navigation", visible: () => !import_obsidian4.Platform.isMobile, control: { type: "toggle", key: "mouseNavigation" } },
      { name: "Navigation zoom padding", desc: "Extra space around the target node when zooming after navigation (px). 0 = tight zoom.", control: { type: "number", key: "navigationZoomPadding", min: 0, step: 1, validate: (value) => value >= 0 ? void 0 : "Enter zero or a positive number." } }
    ];
  }
  getControlValue(key) {
    if (key === "branchPalette") return this.plugin.settings.branchPalette.join(", ");
    return isSettingKey(key) ? this.plugin.settings[key] : void 0;
  }
  async setControlValue(key, value) {
    if (key === "branchPalette" && typeof value === "string") {
      this.plugin.settings.branchPalette = parsePalette(value);
    } else if (BOOLEAN_SETTING_KEYS.some((settingKey) => settingKey === key)) {
      if (typeof value !== "boolean") return;
      Object.assign(this.plugin.settings, { [key]: value });
    } else if (POSITIVE_NUMBER_SETTING_KEYS.some((settingKey) => settingKey === key)) {
      if (typeof value !== "number" || !Number.isFinite(value) || value <= 0) return;
      Object.assign(this.plugin.settings, { [key]: value });
    } else if (key === "navigationZoomPadding") {
      if (typeof value !== "number" || !Number.isFinite(value) || value < 0) return;
      this.plugin.settings.navigationZoomPadding = value;
    } else {
      return;
    }
    await this.plugin.saveSettings();
  }
};

// src/canvas/subtree-drag.ts
function collectDescendants(canvas, canvasApi, nodeId) {
  const result = [];
  const visited = /* @__PURE__ */ new Set([nodeId]);
  const queue = [nodeId];
  while (queue.length > 0) {
    const id = queue.shift();
    for (const edge of canvasApi.getOutgoingEdges(canvas, id)) {
      const childId = edge.to.node.id;
      if (visited.has(childId)) continue;
      visited.add(childId);
      result.push(edge.to.node);
      queue.push(childId);
    }
  }
  return result;
}
function registerSubtreeDragHandler(canvas, canvasApi) {
  var _a, _b, _c, _d, _e;
  let wrappedBranches = [];
  let activePointerId = null;
  let didMove = false;
  let cancelled = false;
  const selectedNodes = () => Array.from(canvas.selection).filter((item) => "nodeEl" in item);
  const clearDragSession = () => {
    for (const { root } of wrappedBranches) {
      delete root.moveTo;
    }
    wrappedBranches = [];
    activePointerId = null;
    didMove = false;
    cancelled = false;
  };
  const installWrappers = (candidates) => {
    const selectedIds = new Set(candidates.map((node) => node.id));
    const rootIds = getTopLevelSelectedIds(
      selectedIds,
      (nodeId) => {
        var _a2, _b2;
        const node = canvas.nodes.get(nodeId);
        return node ? (_b2 = (_a2 = canvasApi.getParentNode(canvas, node)) == null ? void 0 : _a2.id) != null ? _b2 : null : null;
      }
    );
    const claimedIds = new Set(selectedIds);
    for (const rootId of rootIds) {
      const root = canvas.nodes.get(rootId);
      if (!root) continue;
      const descendants = collectDescendants(canvas, canvasApi, root.id).filter((node) => {
        if (claimedIds.has(node.id)) return false;
        claimedIds.add(node.id);
        return true;
      });
      if (descendants.length === 0) continue;
      const proto = Object.getPrototypeOf(root);
      const originalMoveTo = proto.moveTo.bind(root);
      const branch = { root, descendants, originalMoveTo };
      wrappedBranches.push(branch);
      root.moveTo = (pos) => {
        didMove = true;
        const dx = pos.x - root.x;
        const dy = pos.y - root.y;
        originalMoveTo(pos);
        for (const descendant of branch.descendants) {
          const descendantProto = Object.getPrototypeOf(descendant);
          descendantProto.moveTo.call(descendant, {
            x: descendant.x + dx,
            y: descendant.y + dy
          });
        }
      };
    }
  };
  const downHandler = (event) => {
    if (event.pointerType === "touch" && !event.isPrimary) {
      cancelled = true;
      return;
    }
    clearDragSession();
    if (event.altKey) return;
    const clickedNode = findNodeFromEvent(canvas, event);
    if (!clickedNode) return;
    activePointerId = event.pointerId;
    const currentSelection = selectedNodes();
    installWrappers(
      canvas.selection.has(clickedNode) && currentSelection.length > 1 ? currentSelection : [clickedNode]
    );
  };
  const moveHandler = (event) => {
    if (event.pointerType === "touch" && !event.isPrimary) {
      cancelled = true;
      return;
    }
    if (event.pointerId !== activePointerId) return;
    if (event.pointerType !== "touch" && event.buttons === 0) return;
    if (event.altKey) {
      clearDragSession();
      return;
    }
  };
  const upHandler = (event) => {
    if (event.pointerId !== activePointerId) return;
    if (wrappedBranches.length > 0 && didMove && !cancelled) canvas.requestSave();
    clearDragSession();
  };
  const cancelHandler = (event) => {
    if (event.pointerId === activePointerId) clearDragSession();
  };
  (_a = canvas.wrapperEl) == null ? void 0 : _a.addEventListener("pointerdown", downHandler, true);
  (_b = canvas.wrapperEl) == null ? void 0 : _b.addEventListener("pointermove", moveHandler);
  (_c = canvas.wrapperEl) == null ? void 0 : _c.addEventListener("pointerup", upHandler);
  (_d = canvas.wrapperEl) == null ? void 0 : _d.addEventListener("pointercancel", cancelHandler);
  (_e = canvas.wrapperEl) == null ? void 0 : _e.addEventListener("lostpointercapture", cancelHandler);
  return () => {
    var _a2, _b2, _c2, _d2, _e2;
    clearDragSession();
    (_a2 = canvas.wrapperEl) == null ? void 0 : _a2.removeEventListener("pointerdown", downHandler, true);
    (_b2 = canvas.wrapperEl) == null ? void 0 : _b2.removeEventListener("pointermove", moveHandler);
    (_c2 = canvas.wrapperEl) == null ? void 0 : _c2.removeEventListener("pointerup", upHandler);
    (_d2 = canvas.wrapperEl) == null ? void 0 : _d2.removeEventListener("pointercancel", cancelHandler);
    (_e2 = canvas.wrapperEl) == null ? void 0 : _e2.removeEventListener("lostpointercapture", cancelHandler);
  };
}

// src/canvas/drag-reparent.ts
var import_obsidian5 = require("obsidian");
var DROP_TARGET_CLASS = "cammvas-reparent-drop-target";
function registerDragReparent(canvas, canvasApi, isEnabled, onReparent, touchHitPadding = 12) {
  var _a, _b, _c;
  const original = canvas.handleSelectionDrag;
  if (!original) return () => {
  };
  let highlighted = null;
  let touchSession = 0;
  const clearHighlight = () => {
    highlighted == null ? void 0 : highlighted.nodeEl.removeClass(DROP_TARGET_CLASS);
    highlighted = null;
  };
  const pointerDownHandler = (event) => {
    if (event.pointerType !== "touch" || event.isPrimary) return;
    touchSession++;
    clearHighlight();
  };
  const cancelHandler = (event) => {
    if (event.pointerType === "touch") touchSession++;
    clearHighlight();
  };
  const replacement = function(event, dragEl, directNode) {
    const isTouchDrag = event.pointerType === "touch";
    const touchSessionId = isTouchDrag ? ++touchSession : 0;
    if (isTouchDrag) clearHighlight();
    const groupIds = getGroupIds(canvas);
    const selectedNodes = Array.from(canvas.selection).map((item) => canvas.nodes.get(item.id)).filter((node) => !!node && !groupIds.has(node.id));
    const candidateNodes = directNode && !canvas.selection.has(directNode) ? [directNode] : selectedNodes.length > 0 ? selectedNodes : directNode ? [directNode] : [];
    const candidateIds = new Set(candidateNodes.map((node) => node.id));
    const topLevelIds = getTopLevelSelectedIds(
      candidateIds,
      (nodeId) => {
        var _a2, _b2;
        const node = canvas.nodes.get(nodeId);
        return node ? (_b2 = (_a2 = canvasApi.getParentNode(canvas, node)) == null ? void 0 : _a2.id) != null ? _b2 : null : null;
      }
    );
    const draggedNodes = candidateNodes.filter((node) => topLevelIds.has(node.id));
    let eligible = isEnabled() && draggedNodes.length > 0 && (!directNode || !groupIds.has(directNode.id)) && (!isTouchDrag || event.isPrimary);
    const handler = original.call(this, event, dragEl, directNode);
    if (!handler || !eligible) return handler;
    const descendantIds = collectDescendantIdsForRoots(
      draggedNodes.map((node) => node.id),
      (nodeId) => canvasApi.getOutgoingEdges(canvas, nodeId).map((edge) => edge.to.node.id)
    );
    const excludedIds = new Set(descendantIds);
    for (const node of draggedNodes) excludedIds.add(node.id);
    for (const groupId of groupIds) excludedIds.add(groupId);
    const hasEligiblePointer = (pointerEvent) => {
      if (!isTouchDrag) return true;
      const valid = touchSessionId === touchSession && pointerEvent.pointerType === "touch" && pointerEvent.pointerId === event.pointerId && pointerEvent.isPrimary;
      if (!valid) {
        eligible = false;
        clearHighlight();
      }
      return valid;
    };
    const targetAt = (pointerEvent) => findDropTarget(
      canvas.nodes.values(),
      canvas.posFromEvt(pointerEvent),
      excludedIds,
      isTouchDrag ? touchHitPadding : 0
    );
    const updateHighlight = (pointerEvent) => {
      const target = eligible && hasEligiblePointer(pointerEvent) && isEnabled() ? targetAt(pointerEvent) : null;
      if (target === highlighted) return target;
      clearHighlight();
      target == null ? void 0 : target.nodeEl.addClass(DROP_TARGET_CLASS);
      highlighted = target;
      return target;
    };
    const originalMove = handler.move;
    const originalEnd = handler.end;
    const originalCancel = handler.cancel;
    const originalCleanup = handler.cleanup;
    handler.move = (moveEvent) => {
      originalMove == null ? void 0 : originalMove.call(handler, moveEvent);
      updateHighlight(moveEvent);
    };
    handler.end = (endEvent) => {
      const shouldCommit = shouldReparentOnDragEnd(endEvent.type) && eligible && hasEligiblePointer(endEvent);
      const target = shouldCommit ? updateHighlight(endEvent) : null;
      clearHighlight();
      const duplicating = import_obsidian5.Platform.isMacOS ? endEvent.altKey : endEvent.ctrlKey;
      try {
        if (target && isEnabled() && !duplicating) onReparent(draggedNodes, target);
      } finally {
        originalEnd == null ? void 0 : originalEnd.call(handler, endEvent);
      }
    };
    handler.cancel = () => {
      clearHighlight();
      originalCancel == null ? void 0 : originalCancel.call(handler);
    };
    handler.cleanup = () => {
      clearHighlight();
      originalCleanup == null ? void 0 : originalCleanup.call(handler);
    };
    return handler;
  };
  canvas.handleSelectionDrag = replacement;
  (_a = canvas.wrapperEl) == null ? void 0 : _a.addEventListener("pointerdown", pointerDownHandler, true);
  (_b = canvas.wrapperEl) == null ? void 0 : _b.addEventListener("pointercancel", cancelHandler, true);
  (_c = canvas.wrapperEl) == null ? void 0 : _c.addEventListener("lostpointercapture", cancelHandler, true);
  return () => {
    var _a2, _b2, _c2;
    touchSession++;
    clearHighlight();
    (_a2 = canvas.wrapperEl) == null ? void 0 : _a2.removeEventListener("pointerdown", pointerDownHandler, true);
    (_b2 = canvas.wrapperEl) == null ? void 0 : _b2.removeEventListener("pointercancel", cancelHandler, true);
    (_c2 = canvas.wrapperEl) == null ? void 0 : _c2.removeEventListener("lostpointercapture", cancelHandler, true);
    if (canvas.handleSelectionDrag === replacement) {
      canvas.handleSelectionDrag = original;
    }
  };
}

// src/canvas/group-drag.ts
function identifyStrangers(canvas, canvasApi, group, groupIds) {
  const gx = group.x;
  const gy = group.y;
  const gw = group.width;
  const gh = group.height;
  const insideIds = /* @__PURE__ */ new Set();
  const insideNodes = /* @__PURE__ */ new Map();
  for (const node of canvas.nodes.values()) {
    if (groupIds.has(node.id)) continue;
    const cx = node.x + node.width / 2;
    const cy = node.y + node.height / 2;
    if (cx >= gx && cx <= gx + gw && cy >= gy && cy <= gy + gh) {
      insideIds.add(node.id);
      insideNodes.set(node.id, node);
    }
  }
  const strangerIds = /* @__PURE__ */ new Set();
  for (const nodeId of insideIds) {
    const node = insideNodes.get(nodeId);
    const parent = canvasApi.getParentNode(canvas, node);
    if (parent && !insideIds.has(parent.id)) {
      const queue = [nodeId];
      strangerIds.add(nodeId);
      while (queue.length > 0) {
        const id = queue.shift();
        for (const edge of canvasApi.getOutgoingEdges(canvas, id)) {
          const childId = edge.to.node.id;
          if (!strangerIds.has(childId) && insideIds.has(childId)) {
            strangerIds.add(childId);
            queue.push(childId);
          }
        }
      }
    }
  }
  return Array.from(strangerIds).map((id) => insideNodes.get(id));
}
function registerGroupDragHandler(canvas, canvasApi) {
  var _a, _b;
  const frozenNodes = [];
  const downHandler = (e) => {
    if (!e.altKey) return;
    const node = findNodeFromEvent(canvas, e);
    if (!node) return;
    const groupIds = getGroupIds(canvas);
    if (!groupIds.has(node.id)) return;
    const strangers = identifyStrangers(canvas, canvasApi, node, groupIds);
    for (const stranger of strangers) {
      frozenNodes.push(stranger);
      stranger.moveTo = () => {
      };
    }
  };
  const upHandler = () => {
    if (frozenNodes.length === 0) return;
    for (const node of frozenNodes) {
      delete node.moveTo;
    }
    frozenNodes.length = 0;
    canvas.requestSave();
  };
  (_a = canvas.wrapperEl) == null ? void 0 : _a.addEventListener("pointerdown", downHandler, true);
  (_b = canvas.wrapperEl) == null ? void 0 : _b.addEventListener("pointerup", upHandler);
  return () => {
    var _a2, _b2;
    if (frozenNodes.length > 0) {
      for (const node of frozenNodes) {
        delete node.moveTo;
      }
      frozenNodes.length = 0;
    }
    (_a2 = canvas.wrapperEl) == null ? void 0 : _a2.removeEventListener("pointerdown", downHandler, true);
    (_b2 = canvas.wrapperEl) == null ? void 0 : _b2.removeEventListener("pointerup", upHandler);
  };
}

// src/ui/auto-resize.ts
function getEditorElements(node) {
  var _a;
  const iframe = (_a = node.contentEl) == null ? void 0 : _a.querySelector("iframe");
  if (!(iframe == null ? void 0 : iframe.contentDocument)) return { iframe: null, scroller: null, cmContent: null };
  const scroller = iframe.contentDocument.querySelector(".cm-scroller");
  const cmContent = iframe.contentDocument.querySelector(".cm-content");
  return { iframe, scroller, cmContent };
}
function registerAutoResize(canvas, config, onEditExit) {
  var _a, _b, _c;
  let activeNode = null;
  let observer = null;
  let inputHandler = null;
  let cachedCmContent = null;
  let cachedScroller = null;
  let cachedInputTarget = null;
  const win = canvas.wrapperEl.win;
  function onContentChange() {
    if (!activeNode || !cachedScroller || !cachedCmContent) return;
    const contentH = cachedCmContent.offsetHeight;
    const chrome = activeNode.height - cachedScroller.clientHeight;
    const targetH = Math.min(Math.max(contentH + chrome, config.minHeight), config.maxHeight);
    if (targetH > activeNode.height) {
      activeNode.moveAndResize({
        x: activeNode.x,
        y: activeNode.y,
        width: activeNode.width,
        height: targetH
      });
      canvas.requestSave();
    }
  }
  function startWatching(node) {
    var _a2, _b2;
    const { iframe, scroller, cmContent } = getEditorElements(node);
    activeNode = node;
    cachedCmContent = cmContent;
    cachedScroller = scroller;
    const observeTarget = cmContent != null ? cmContent : (_a2 = iframe == null ? void 0 : iframe.contentDocument) == null ? void 0 : _a2.body;
    if (observeTarget) {
      const Observer = Reflect.get(observeTarget.win, "MutationObserver");
      const nextObserver = new Observer(onContentChange);
      nextObserver.observe(observeTarget, {
        childList: true,
        subtree: true,
        characterData: true
      });
      observer = nextObserver;
    } else {
      const Observer = Reflect.get(node.contentEl.win, "MutationObserver");
      const nextObserver = new Observer(onContentChange);
      nextObserver.observe(node.contentEl, {
        childList: true,
        subtree: true,
        characterData: true
      });
      observer = nextObserver;
    }
    cachedInputTarget = (_b2 = iframe == null ? void 0 : iframe.contentDocument) != null ? _b2 : node.contentEl;
    const handler = () => onContentChange();
    inputHandler = handler;
    cachedInputTarget.addEventListener("input", handler);
    onContentChange();
  }
  function stopWatching(triggerRelayout = true) {
    if (!activeNode) return;
    const node = activeNode;
    observer == null ? void 0 : observer.disconnect();
    if (inputHandler && cachedInputTarget) {
      cachedInputTarget.removeEventListener("input", inputHandler);
    }
    activeNode = null;
    observer = null;
    inputHandler = null;
    cachedCmContent = null;
    cachedScroller = null;
    cachedInputTarget = null;
    if (triggerRelayout && onEditExit) {
      onEditExit(canvas, node);
    }
  }
  const focusInHandler = (e) => {
    const target = e.target;
    if (!isHtmlElement(target)) return;
    const nodeEl = target.closest(".canvas-node");
    if (!nodeEl) return;
    for (const node of canvas.nodes.values()) {
      if (node.nodeEl === nodeEl && node.isEditing && node !== activeNode) {
        if (activeNode) stopWatching();
        startWatching(node);
        return;
      }
    }
  };
  const focusOutHandler = () => {
    if (!activeNode) return;
    win.setTimeout(() => {
      if (activeNode && !activeNode.isEditing) {
        stopWatching();
      }
    }, 50);
  };
  const pointerHandler = (e) => {
    var _a2;
    if (!activeNode) return;
    if (isDomNode(e.target) && ((_a2 = activeNode.nodeEl) == null ? void 0 : _a2.contains(e.target))) return;
    win.setTimeout(() => {
      if (activeNode && !activeNode.isEditing) {
        stopWatching();
      }
    }, 50);
  };
  (_a = canvas.wrapperEl) == null ? void 0 : _a.addEventListener("focusin", focusInHandler);
  (_b = canvas.wrapperEl) == null ? void 0 : _b.addEventListener("focusout", focusOutHandler);
  (_c = canvas.wrapperEl) == null ? void 0 : _c.addEventListener("pointerdown", pointerHandler);
  return {
    cleanup: () => {
      var _a2, _b2, _c2;
      if (activeNode) stopWatching(false);
      (_a2 = canvas.wrapperEl) == null ? void 0 : _a2.removeEventListener("focusin", focusInHandler);
      (_b2 = canvas.wrapperEl) == null ? void 0 : _b2.removeEventListener("focusout", focusOutHandler);
      (_c2 = canvas.wrapperEl) == null ? void 0 : _c2.removeEventListener("pointerdown", pointerHandler);
    },
    finalizeNode: () => {
      if (activeNode) stopWatching(false);
    }
  };
}

// src/ui/outline-view.ts
var import_obsidian7 = require("obsidian");

// src/ui/clipboard.ts
var import_obsidian6 = require("obsidian");
async function copyText(win, text, successMessage) {
  try {
    await win.navigator.clipboard.writeText(text);
    new import_obsidian6.Notice(successMessage);
  } catch (e) {
    new import_obsidian6.Notice("Unable to copy to the clipboard");
  }
}

// src/ui/outline-view.ts
var OUTLINE_VIEW_TYPE = "cammvas-outline";
var OutlineView = class extends import_obsidian7.ItemView {
  constructor(leaf) {
    super(leaf);
    this.canvasLeaf = null;
    this.collapsedGroups = /* @__PURE__ */ new Set();
    this.collapsedNodes = /* @__PURE__ */ new Set();
    this.selectedRoots = /* @__PURE__ */ new Set();
    this.lastCanvas = null;
    this.groupIds = [];
    this.draggedRoot = null;
    this.dragSourceGroupId = null;
    this.activeNodeId = null;
    this.allItemEls = /* @__PURE__ */ new Map();
    this.groupElMap = /* @__PURE__ */ new Map();
    this.searchQuery = "";
    this.navHeaderEl = null;
    this.collapseBtnEl = null;
    this.searchContainerEl = null;
    this.searchComponent = null;
    this.zoomPadding = 0;
    this.onForestLayout = null;
  }
  getViewType() {
    return OUTLINE_VIEW_TYPE;
  }
  getDisplayText() {
    return "Map outline";
  }
  getIcon() {
    return "list-tree";
  }
  onOpen() {
    this.contentEl.addClass("cammvas-outline");
    const navHeader = this.containerEl.createDiv({ cls: "nav-header" });
    this.containerEl.insertBefore(navHeader, this.contentEl);
    this.navHeaderEl = navHeader;
    const navButtons = navHeader.createDiv({ cls: "nav-buttons-container" });
    const searchBtn = navButtons.createDiv({
      cls: "clickable-icon nav-action-button",
      attr: { "aria-label": "Search" }
    });
    (0, import_obsidian7.setIcon)(searchBtn, "search");
    this.collapseBtnEl = navButtons.createDiv({
      cls: "clickable-icon nav-action-button",
      attr: { "aria-label": "Collapse all" }
    });
    (0, import_obsidian7.setIcon)(this.collapseBtnEl, "chevrons-down-up");
    this.collapseBtnEl.addEventListener("click", () => {
      if (!this.lastCanvas) return;
      const allCollapsed = this.groupIds.length > 0 && this.groupIds.every((id) => this.collapsedGroups.has(id));
      if (allCollapsed) {
        this.collapsedGroups.clear();
      } else {
        for (const id of this.groupIds) this.collapsedGroups.add(id);
      }
      this.refresh(this.lastCanvas);
    });
    this.searchContainerEl = navHeader.createDiv({ cls: "cammvas-outline-search-container" });
    this.searchContainerEl.hide();
    this.searchComponent = new import_obsidian7.SearchComponent(this.searchContainerEl);
    this.searchComponent.setPlaceholder("Filter...");
    this.searchComponent.onChange((value) => {
      this.searchQuery = value;
      this.applyFilter();
    });
    searchBtn.addEventListener("click", () => {
      if (!this.searchContainerEl || !this.searchComponent) return;
      if (this.searchContainerEl.isShown()) {
        this.searchContainerEl.hide();
        this.searchQuery = "";
        this.searchComponent.setValue("");
        this.applyFilter();
      } else {
        this.searchContainerEl.show();
        this.searchComponent.inputEl.focus();
      }
    });
    return Promise.resolve();
  }
  onClose() {
    this.clear();
    if (this.navHeaderEl) {
      this.navHeaderEl.remove();
      this.navHeaderEl = null;
    }
    this.collapseBtnEl = null;
    this.searchContainerEl = null;
    this.searchComponent = null;
    return Promise.resolve();
  }
  /**
   * Rebuild the outline from the current canvas state.
   */
  refresh(canvas) {
    var _a;
    this.contentEl.empty();
    this.selectedRoots.clear();
    this.groupElMap.clear();
    this.allItemEls.clear();
    this.lastCanvas = canvas;
    this.canvasLeaf = (_a = this.app.workspace.getLeavesOfType("canvas").find((l) => {
      var _a2;
      return ((_a2 = l.view) == null ? void 0 : _a2.canvas) === canvas;
    })) != null ? _a : null;
    if (this.searchComponent) {
      this.searchComponent.setValue(this.searchQuery);
    }
    const forest = buildForest(canvas);
    if (forest.length === 0) {
      this.contentEl.createDiv({
        cls: "cammvas-outline-empty",
        text: "No root nodes"
      });
      return;
    }
    const groups = [];
    for (const nd of canvas.getData().nodes) {
      if (nd.type !== "group") continue;
      const node = canvas.nodes.get(nd.id);
      if (!node) continue;
      groups.push({
        node,
        label: (nd.label || "").trim() || "Untitled Group",
        area: node.width * node.height,
        roots: []
      });
    }
    groups.sort((a, b) => {
      const dy = a.node.y - b.node.y;
      if (Math.abs(dy) > 50) return dy;
      return a.node.x - b.node.x;
    });
    const ungrouped = [];
    for (const root of forest) {
      const cx = root.canvasNode.x + root.canvasNode.width / 2;
      const cy = root.canvasNode.y + root.canvasNode.height / 2;
      let bestGroup = null;
      for (const g of groups) {
        if (cx >= g.node.x && cx <= g.node.x + g.node.width && cy >= g.node.y && cy <= g.node.y + g.node.height) {
          if (!bestGroup || g.area < bestGroup.area) {
            bestGroup = g;
          }
        }
      }
      if (bestGroup) {
        bestGroup.roots.push(root);
      } else {
        ungrouped.push(root);
      }
    }
    const ungroupedZone = this.contentEl.createDiv({ cls: "cammvas-outline-ungrouped-zone" });
    ungroupedZone.addEventListener("dragover", (e) => {
      if (!this.draggedRoot || !this.dragSourceGroupId) return;
      e.preventDefault();
      ungroupedZone.addClass("is-drag-over");
    });
    ungroupedZone.addEventListener("dragleave", () => {
      ungroupedZone.removeClass("is-drag-over");
    });
    ungroupedZone.addEventListener("drop", (e) => {
      e.preventDefault();
      ungroupedZone.removeClass("is-drag-over");
      if (!this.draggedRoot || !this.dragSourceGroupId || !this.lastCanvas) return;
      this.ungroupTree(this.draggedRoot, this.dragSourceGroupId);
      this.draggedRoot = null;
      this.dragSourceGroupId = null;
    });
    for (const root of ungrouped) {
      this.renderRootItem(ungroupedZone, root, canvas, true);
    }
    ungroupedZone.toggleClass(
      "has-group-separator",
      ungrouped.length > 0 && groups.some((group) => group.roots.length > 0)
    );
    for (const group of groups) {
      if (group.roots.length === 0) continue;
      this.renderGroup(group, canvas);
    }
    this.groupIds = groups.filter((g) => g.roots.length > 0).map((g) => g.node.id);
    const allCollapsed = this.groupIds.length > 0 && this.groupIds.every((id) => this.collapsedGroups.has(id));
    if (this.collapseBtnEl) {
      (0, import_obsidian7.setIcon)(this.collapseBtnEl, allCollapsed ? "chevrons-up-down" : "chevrons-down-up");
      this.collapseBtnEl.setAttribute("aria-label", allCollapsed ? "Expand all" : "Collapse all");
    }
    if (this.searchQuery) this.applyFilter();
    if (this.activeNodeId) {
      const el = this.allItemEls.get(this.activeNodeId);
      if (el) el.addClass("is-active");
    }
  }
  applyFilter() {
    var _a, _b, _c, _d, _e, _f, _g;
    const q = this.searchQuery.toLowerCase().trim();
    this.contentEl.toggleClass("is-searching", q !== "");
    for (const root of Array.from(this.contentEl.querySelectorAll(".cammvas-outline-root-tree"))) {
      const text = (_b = (_a = root.textContent) == null ? void 0 : _a.toLowerCase()) != null ? _b : "";
      root.toggleClass("is-hidden", q !== "" && !text.includes(q));
    }
    for (const group of Array.from(this.contentEl.querySelectorAll(":scope > .tree-item"))) {
      const header = group.querySelector(":scope > .cammvas-outline-group");
      if (!header) continue;
      const groupLabel = (_e = (_d = (_c = header.querySelector(".tree-item-inner span")) == null ? void 0 : _c.textContent) == null ? void 0 : _d.toLowerCase()) != null ? _e : "";
      const groupLabelMatches = q !== "" && groupLabel.includes(q);
      if (groupLabelMatches) {
        for (const root of Array.from(group.querySelectorAll(".cammvas-outline-root-tree"))) {
          root.removeClass("is-hidden");
        }
      }
      const matches = groupLabelMatches || ((_g = (_f = group.textContent) == null ? void 0 : _f.toLowerCase()) != null ? _g : "").includes(q);
      group.toggleClass("is-hidden", q !== "" && !matches);
    }
  }
  /**
   * Render a single root node as a tree-item.
   */
  renderRootItem(container, root, canvas, isUngrouped, groupId) {
    const treeItem = container.createDiv({ cls: "tree-item cammvas-outline-root-tree" });
    treeItem.dataset.nodeId = root.canvasNode.id;
    const self = treeItem.createDiv({
      cls: "tree-item-self is-clickable cammvas-outline-item"
    });
    if (root.children.length > 0) {
      const collapseIcon = self.createDiv({ cls: "tree-item-icon collapse-icon" });
      (0, import_obsidian7.setIcon)(collapseIcon, "right-triangle");
      treeItem.toggleClass("is-collapsed", this.collapsedNodes.has(root.canvasNode.id));
      collapseIcon.addEventListener("click", (e) => {
        e.stopPropagation();
        this.toggleNodeCollapse(treeItem, root.canvasNode.id);
      });
    }
    const dragHandle = import_obsidian7.Platform.isMobile ? null : self.createDiv({ cls: "tree-item-icon cammvas-outline-drag-handle" });
    if (dragHandle) (0, import_obsidian7.setIcon)(dragHandle, "grip-vertical");
    self.createDiv({ cls: "tree-item-inner", text: getRootTitle(root.canvasNode.text) });
    if (dragHandle) {
      let dragAllowed = false;
      dragHandle.addEventListener("pointerdown", () => {
        dragAllowed = true;
      });
      self.addEventListener("pointerup", () => {
        dragAllowed = false;
      });
      self.setAttribute("draggable", "true");
      self.addEventListener("dragstart", (e) => {
        var _a;
        if (!dragAllowed) {
          e.preventDefault();
          return;
        }
        dragAllowed = false;
        this.draggedRoot = root;
        this.dragSourceGroupId = groupId != null ? groupId : null;
        self.addClass("is-dragging");
        (_a = e.dataTransfer) == null ? void 0 : _a.setData("text/plain", root.canvasNode.id);
      });
      self.addEventListener("dragend", () => {
        self.removeClass("is-dragging");
        this.draggedRoot = null;
        this.dragSourceGroupId = null;
        for (const [, el] of this.groupElMap) el.removeClass("is-drag-over");
      });
    }
    this.allItemEls.set(root.canvasNode.id, self);
    self.addEventListener("click", (e) => {
      if (isUngrouped && e.ctrlKey) {
        if (this.selectedRoots.has(root)) {
          this.selectedRoots.delete(root);
          self.removeClass("is-selected");
        } else {
          this.selectedRoots.add(root);
          self.addClass("is-selected");
        }
        return;
      }
      this.clearSelection();
      this.setActiveItem(root.canvasNode.id);
      if (this.canvasLeaf) {
        this.app.workspace.setActiveLeaf(this.canvasLeaf, { focus: true });
      }
      const node = root.canvasNode;
      canvas.selectOnly(node);
      const pad = this.zoomPadding;
      const cx = node.x + node.width / 2;
      const cy = node.y + node.height / 2;
      canvas.zoomToBbox({
        minX: cx - pad,
        minY: cy - pad,
        maxX: cx + pad,
        maxY: cy + pad
      });
    });
    self.addEventListener("contextmenu", (e) => {
      e.preventDefault();
      const menu = new import_obsidian7.Menu();
      menu.addItem((item) => {
        item.setTitle("Copy node link").setIcon("link").onClick(() => {
          const canvasPath = canvas.view.file.path;
          void copyText(
            self.win,
            `obsidian://cammvas-navigate?canvas=${encodeURIComponent(canvasPath)}&id=${root.canvasNode.id}`,
            "Node link copied"
          );
        });
      });
      if (isUngrouped) {
        if (!this.selectedRoots.has(root)) {
          this.clearSelection();
          this.selectedRoots.add(root);
          self.addClass("is-selected");
        }
        const count = this.selectedRoots.size;
        menu.addItem((item) => {
          item.setTitle(`Create group (${count} root${count > 1 ? "s" : ""})`).setIcon("group").onClick(() => this.createGroupFromSelection());
        });
      }
      menu.showAtMouseEvent(e);
    });
    if (root.children.length > 0) {
      const children = treeItem.createDiv({ cls: "tree-item-children" });
      for (const child of root.children) this.renderChildItem(children, child, canvas);
    }
  }
  renderChildItem(container, node, canvas) {
    const treeItem = container.createDiv({ cls: "tree-item cammvas-outline-node-tree" });
    treeItem.dataset.nodeId = node.canvasNode.id;
    const self = treeItem.createDiv({
      cls: "tree-item-self is-clickable cammvas-outline-item"
    });
    const collapseIcon = self.createDiv({ cls: "tree-item-icon collapse-icon" });
    if (node.children.length > 0) {
      (0, import_obsidian7.setIcon)(collapseIcon, "right-triangle");
      treeItem.toggleClass("is-collapsed", this.collapsedNodes.has(node.canvasNode.id));
      collapseIcon.addEventListener("click", (e) => {
        e.stopPropagation();
        this.toggleNodeCollapse(treeItem, node.canvasNode.id);
      });
    }
    self.createDiv({ cls: "tree-item-inner", text: getRootTitle(node.canvasNode.text) });
    this.allItemEls.set(node.canvasNode.id, self);
    self.addEventListener("click", () => this.navigateToNode(canvas, node.canvasNode));
    self.addEventListener("contextmenu", (e) => {
      e.preventDefault();
      const menu = new import_obsidian7.Menu();
      menu.addItem((item) => {
        item.setTitle("Copy node link").setIcon("link").onClick(() => {
          const canvasPath = canvas.view.file.path;
          void copyText(
            self.win,
            `obsidian://cammvas-navigate?canvas=${encodeURIComponent(canvasPath)}&id=${node.canvasNode.id}`,
            "Node link copied"
          );
        });
      });
      menu.showAtMouseEvent(e);
    });
    if (node.children.length > 0) {
      const children = treeItem.createDiv({ cls: "tree-item-children" });
      for (const child of node.children) this.renderChildItem(children, child, canvas);
    }
  }
  toggleNodeCollapse(treeItem, nodeId) {
    if (this.collapsedNodes.has(nodeId)) {
      this.collapsedNodes.delete(nodeId);
      treeItem.removeClass("is-collapsed");
    } else {
      this.collapsedNodes.add(nodeId);
      treeItem.addClass("is-collapsed");
    }
  }
  navigateToNode(canvas, node) {
    this.clearSelection();
    this.setActiveItem(node.id);
    if (this.canvasLeaf) {
      this.app.workspace.setActiveLeaf(this.canvasLeaf, { focus: true });
    }
    canvas.selectOnly(node);
    const cx = node.x + node.width / 2;
    const cy = node.y + node.height / 2;
    canvas.zoomToBbox({
      minX: cx - this.zoomPadding,
      minY: cy - this.zoomPadding,
      maxX: cx + this.zoomPadding,
      maxY: cy + this.zoomPadding
    });
  }
  clearSelection() {
    var _a;
    for (const root of this.selectedRoots) {
      (_a = this.allItemEls.get(root.canvasNode.id)) == null ? void 0 : _a.removeClass("is-selected");
    }
    this.selectedRoots.clear();
  }
  setActiveItem(nodeId) {
    var _a;
    if (this.activeNodeId) {
      (_a = this.allItemEls.get(this.activeNodeId)) == null ? void 0 : _a.removeClass("is-active");
    }
    this.activeNodeId = nodeId;
    const el = this.allItemEls.get(nodeId);
    this.revealItem(el);
    el == null ? void 0 : el.addClass("is-active");
    el == null ? void 0 : el.scrollIntoView({ block: "nearest" });
  }
  revealItem(item) {
    var _a, _b, _c, _d, _e;
    let ancestor = (_c = (_b = (_a = item == null ? void 0 : item.parentElement) == null ? void 0 : _a.parentElement) == null ? void 0 : _b.closest(".tree-item.is-collapsed")) != null ? _c : null;
    while (ancestor) {
      ancestor.removeClass("is-collapsed");
      const nodeId = ancestor.dataset.nodeId;
      const groupId = ancestor.dataset.groupId;
      if (nodeId) this.collapsedNodes.delete(nodeId);
      if (groupId) this.collapsedGroups.delete(groupId);
      ancestor = (_e = (_d = ancestor.parentElement) == null ? void 0 : _d.closest(".tree-item.is-collapsed")) != null ? _e : null;
    }
  }
  clearActiveItem() {
    var _a;
    if (this.activeNodeId) {
      (_a = this.allItemEls.get(this.activeNodeId)) == null ? void 0 : _a.removeClass("is-active");
    }
    this.activeNodeId = null;
  }
  /**
   * Sync outline highlight from the current canvas selection.
   */
  syncHighlightFromCanvas(canvas) {
    if (canvas.selection.size !== 1) {
      this.clearActiveItem();
      return;
    }
    let nodeId = null;
    for (const item of canvas.selection) {
      if ("nodeEl" in item) nodeId = item.id;
    }
    if (!nodeId) {
      this.clearActiveItem();
      return;
    }
    if (this.allItemEls.has(nodeId)) {
      this.setActiveItem(nodeId);
    } else {
      this.clearActiveItem();
    }
  }
  createGroupFromSelection() {
    const canvas = this.lastCanvas;
    if (!canvas || this.selectedRoots.size === 0) return;
    const allNodes = [];
    for (const root of this.selectedRoots) {
      allNodes.push(root.canvasNode);
      for (const desc of getDescendants(root)) {
        allNodes.push(desc.canvasNode);
      }
    }
    const PADDING = 20;
    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    for (const node of allNodes) {
      minX = Math.min(minX, node.x);
      minY = Math.min(minY, node.y);
      maxX = Math.max(maxX, node.x + node.width);
      maxY = Math.max(maxY, node.y + node.height);
    }
    minX -= PADDING;
    minY -= PADDING;
    maxX += PADDING;
    maxY += PADDING;
    const group = canvas.createGroupNode({
      pos: { x: minX, y: minY },
      size: { width: maxX - minX, height: maxY - minY },
      label: ""
    });
    canvas.requestSave();
    if (this.canvasLeaf) {
      this.app.workspace.setActiveLeaf(this.canvasLeaf, { focus: true });
    }
    canvas.selectOnly(group);
    this.contentEl.win.setTimeout(() => group.startEditing(), 50);
    this.clearSelection();
  }
  /**
   * Render a group as a collapsible tree-item section.
   */
  renderGroup(group, canvas) {
    const isCollapsed = this.collapsedGroups.has(group.node.id);
    const treeItem = this.contentEl.createDiv({
      cls: "tree-item" + (isCollapsed ? " is-collapsed" : "")
    });
    treeItem.dataset.groupId = group.node.id;
    const self = treeItem.createDiv({
      cls: "tree-item-self is-clickable cammvas-outline-group"
    });
    const collapseIcon = self.createDiv({ cls: "tree-item-icon collapse-icon" });
    (0, import_obsidian7.setIcon)(collapseIcon, "right-triangle");
    const labelContainer = self.createDiv({ cls: "tree-item-inner" });
    const labelSpan = labelContainer.createSpan({ text: group.label });
    labelContainer.createSpan({ cls: "cammvas-outline-group-count", text: `${group.roots.length}` });
    this.groupElMap.set(group.node.id, self);
    self.addEventListener("dragover", (e) => {
      if (!this.draggedRoot) return;
      e.preventDefault();
      self.addClass("is-drag-over");
    });
    self.addEventListener("dragleave", () => {
      self.removeClass("is-drag-over");
    });
    self.addEventListener("drop", (e) => {
      e.preventDefault();
      self.removeClass("is-drag-over");
      if (!this.draggedRoot || !this.lastCanvas) return;
      if (this.dragSourceGroupId === group.node.id) return;
      this.moveTreeToGroup(this.draggedRoot, group.node.id, this.dragSourceGroupId);
      this.draggedRoot = null;
      this.dragSourceGroupId = null;
    });
    let clickTimer = null;
    self.addEventListener("click", () => {
      if (clickTimer !== null) {
        this.contentEl.win.clearTimeout(clickTimer);
        clickTimer = null;
        return;
      }
      clickTimer = this.contentEl.win.setTimeout(() => {
        clickTimer = null;
        if (this.collapsedGroups.has(group.node.id)) {
          this.collapsedGroups.delete(group.node.id);
          treeItem.removeClass("is-collapsed");
        } else {
          this.collapsedGroups.add(group.node.id);
          treeItem.addClass("is-collapsed");
        }
      }, 250);
    });
    self.addEventListener("dblclick", () => {
      if (clickTimer !== null) {
        this.contentEl.win.clearTimeout(clickTimer);
        clickTimer = null;
      }
      this.startGroupRename(labelSpan, group, canvas);
    });
    self.addEventListener("contextmenu", (e) => {
      e.preventDefault();
      const menu = new import_obsidian7.Menu();
      menu.addItem((item) => {
        item.setTitle("Rename group").setIcon("pencil").onClick(() => this.startGroupRename(labelSpan, group, canvas));
      });
      menu.addItem((item) => {
        item.setTitle("Layout forest").setIcon("layout-grid").onClick(() => {
          if (this.lastCanvas && this.onForestLayout) {
            this.onForestLayout(this.lastCanvas, group.node.id);
          }
        });
      });
      menu.showAtMouseEvent(e);
    });
    const childrenContainer = treeItem.createDiv({ cls: "tree-item-children" });
    for (const root of group.roots) {
      this.renderRootItem(childrenContainer, root, canvas, false, group.node.id);
    }
  }
  moveTreeToGroup(root, targetGroupId, sourceGroupId) {
    const canvas = this.lastCanvas;
    if (!canvas) return;
    const group = canvas.nodes.get(targetGroupId);
    if (!group) return;
    const targetX = group.x + group.width / 2 - root.canvasNode.width / 2;
    const targetY = group.y + group.height / 2 - root.canvasNode.height / 2;
    const dx = targetX - root.canvasNode.x;
    const dy = targetY - root.canvasNode.y;
    root.canvasNode.moveTo({ x: targetX, y: targetY });
    for (const desc of getDescendants(root)) {
      desc.canvasNode.moveTo({ x: desc.canvasNode.x + dx, y: desc.canvasNode.y + dy });
    }
    if (this.onForestLayout) {
      this.onForestLayout(canvas, targetGroupId);
      if (sourceGroupId) {
        this.onForestLayout(canvas, sourceGroupId);
      }
    }
  }
  ungroupTree(root, sourceGroupId) {
    const canvas = this.lastCanvas;
    if (!canvas) return;
    const groupNodeIds = getGroupIds(canvas);
    let maxY = -Infinity;
    for (const gid of groupNodeIds) {
      const g = canvas.nodes.get(gid);
      if (g) maxY = Math.max(maxY, g.y + g.height);
    }
    const MARGIN = 80;
    const dx = 0;
    const dy = maxY + MARGIN - root.canvasNode.y;
    root.canvasNode.moveTo({ x: root.canvasNode.x, y: maxY + MARGIN });
    for (const desc of getDescendants(root)) {
      desc.canvasNode.moveTo({ x: desc.canvasNode.x + dx, y: desc.canvasNode.y + dy });
    }
    if (this.onForestLayout) {
      this.onForestLayout(canvas, sourceGroupId);
    }
  }
  startGroupRename(labelSpan, group, canvas) {
    var _a;
    const originalText = (_a = labelSpan.textContent) != null ? _a : "";
    labelSpan.contentEditable = "true";
    labelSpan.focus();
    const range = labelSpan.doc.createRange();
    range.selectNodeContents(labelSpan);
    const sel = labelSpan.win.getSelection();
    sel == null ? void 0 : sel.removeAllRanges();
    sel == null ? void 0 : sel.addRange(range);
    let done = false;
    const commit = () => {
      var _a2;
      if (done) return;
      done = true;
      const newLabel = ((_a2 = labelSpan.textContent) != null ? _a2 : "").trim() || "Untitled Group";
      labelSpan.contentEditable = "false";
      labelSpan.textContent = newLabel;
      cleanup();
      if (newLabel === originalText) return;
      const data = canvas.getData();
      const nodeData = data.nodes.find((n) => n.id === group.node.id);
      if (nodeData) {
        nodeData.label = newLabel;
        canvas.setData(data);
      }
    };
    const cancel = () => {
      done = true;
      labelSpan.contentEditable = "false";
      labelSpan.textContent = originalText;
      cleanup();
    };
    const onKeydown = (e) => {
      e.stopPropagation();
      if (e.key === "Enter") {
        e.preventDefault();
        commit();
      } else if (e.key === "Escape") {
        e.preventDefault();
        cancel();
      }
    };
    const onBlur = () => commit();
    const onClick = (e) => e.stopPropagation();
    const cleanup = () => {
      labelSpan.removeEventListener("keydown", onKeydown);
      labelSpan.removeEventListener("blur", onBlur);
      labelSpan.removeEventListener("click", onClick);
    };
    labelSpan.addEventListener("keydown", onKeydown);
    labelSpan.addEventListener("blur", onBlur);
    labelSpan.addEventListener("click", onClick);
  }
  /**
   * Clear the outline (no canvas active).
   */
  clear() {
    this.canvasLeaf = null;
    this.lastCanvas = null;
    this.selectedRoots.clear();
    this.groupElMap.clear();
    this.allItemEls.clear();
    this.collapsedGroups.clear();
    this.collapsedNodes.clear();
    this.activeNodeId = null;
    this.draggedRoot = null;
    this.dragSourceGroupId = null;
    this.contentEl.empty();
    this.contentEl.createDiv({
      cls: "cammvas-outline-empty",
      text: "Open a canvas to see root nodes"
    });
  }
};
function getRootTitle(text) {
  const firstLine = (text || "").split("\n")[0].trim();
  return firstLine.replace(/^#+\s*/, "").replace(/\[([^\]]*)\]\([^)]*\)/g, "$1") || "Untitled";
}

// src/ui/mobile-editing-bar.ts
var import_obsidian8 = require("obsidian");

// src/ui/mobile-editing-state.ts
function getMobileEditingNode(canvas, enabled) {
  if (!enabled || canvas.selection.size !== 1) return null;
  for (const item of canvas.selection) {
    return "nodeEl" in item && item.type === "text" && item.isEditing ? item : null;
  }
  return null;
}

// src/ui/mobile-editing-bar.ts
function registerMobileEditingBar(canvas, isEnabled, canAddSibling, onAddChild, onAddSibling, onDone) {
  const doc = canvas.wrapperEl.doc;
  const win = canvas.wrapperEl.win;
  const bar = doc.body.createDiv({ cls: "cammvas-mobile-editing-bar" });
  bar.hidden = true;
  bar.setAttribute("role", "toolbar");
  bar.setAttribute("aria-label", "Node editing actions");
  const createButton = (label, icon) => {
    const button = bar.createEl("button", {
      cls: "cammvas-mobile-editing-action",
      attr: { type: "button", "aria-label": `Create ${label.toLowerCase()} node` }
    });
    const iconEl = button.createSpan({ cls: "cammvas-mobile-editing-action-icon" });
    (0, import_obsidian8.setIcon)(iconEl, icon);
    button.createSpan({ text: label });
    return button;
  };
  const childButton = createButton("Child", "corner-down-right");
  const siblingButton = createButton("Sibling", "list-plus");
  const doneButton = bar.createEl("button", {
    cls: "cammvas-mobile-editing-action cammvas-mobile-editing-done",
    attr: { type: "button", "aria-label": "Finish editing node" }
  });
  const doneIcon = doneButton.createSpan({ cls: "cammvas-mobile-editing-action-icon" });
  (0, import_obsidian8.setIcon)(doneIcon, "check");
  doneButton.createSpan({ text: "Done" });
  let activeNode = null;
  let refreshRaf = 0;
  let refreshTimer = 0;
  const positionBar = () => {
    if (bar.hidden) return;
    const viewport2 = win.visualViewport;
    if (!viewport2) {
      bar.removeClass("is-visual-viewport-positioned");
      bar.style.removeProperty("top");
      bar.style.removeProperty("left");
      return;
    }
    const height = bar.getBoundingClientRect().height;
    bar.addClass("is-visual-viewport-positioned");
    bar.style.setProperty("left", `${viewport2.offsetLeft + viewport2.width / 2}px`);
    bar.style.setProperty(
      "top",
      `${Math.max(viewport2.offsetTop + 8, viewport2.offsetTop + viewport2.height - height - 8)}px`
    );
  };
  const refresh = () => {
    const node = getMobileEditingNode(canvas, isEnabled());
    activeNode = node;
    bar.hidden = !node;
    siblingButton.disabled = !node || !canAddSibling(node);
    if (node) positionBar();
  };
  const scheduleRefresh = (delay = 0) => {
    if (refreshTimer) win.clearTimeout(refreshTimer);
    if (refreshRaf) win.cancelAnimationFrame(refreshRaf);
    if (delay > 0) {
      refreshTimer = win.setTimeout(() => {
        refreshTimer = 0;
        refresh();
      }, delay);
      return;
    }
    refreshRaf = win.requestAnimationFrame(() => {
      refreshRaf = 0;
      refresh();
    });
  };
  const preserveEditorFocus = (event) => {
    event.preventDefault();
    event.stopPropagation();
  };
  const runAction = (event, action) => {
    event.preventDefault();
    event.stopPropagation();
    const node = activeNode != null ? activeNode : getMobileEditingNode(canvas, isEnabled());
    if (!node) return;
    action(node);
    scheduleRefresh();
  };
  const onChildClick = (event) => runAction(event, onAddChild);
  const onSiblingClick = (event) => runAction(event, onAddSibling);
  const onDoneClick = (event) => runAction(event, (node) => {
    onDone(node);
    activeNode = null;
    bar.hidden = true;
  });
  for (const button of [childButton, siblingButton, doneButton]) {
    button.addEventListener("pointerdown", preserveEditorFocus);
  }
  childButton.addEventListener("click", onChildClick);
  siblingButton.addEventListener("click", onSiblingClick);
  doneButton.addEventListener("click", onDoneClick);
  const onFocusIn = () => scheduleRefresh();
  const onFocusOut = () => scheduleRefresh(80);
  const onCanvasPointerDown = () => scheduleRefresh(80);
  canvas.wrapperEl.addEventListener("focusin", onFocusIn);
  canvas.wrapperEl.addEventListener("focusout", onFocusOut);
  canvas.wrapperEl.addEventListener("pointerdown", onCanvasPointerDown);
  const Observer = Reflect.get(win, "MutationObserver");
  const observer = new Observer(() => scheduleRefresh());
  observer.observe(canvas.wrapperEl, { childList: true, subtree: true });
  const viewport = win.visualViewport;
  viewport == null ? void 0 : viewport.addEventListener("resize", positionBar);
  viewport == null ? void 0 : viewport.addEventListener("scroll", positionBar);
  win.addEventListener("resize", positionBar);
  return {
    refresh: scheduleRefresh,
    cleanup: () => {
      if (refreshTimer) win.clearTimeout(refreshTimer);
      if (refreshRaf) win.cancelAnimationFrame(refreshRaf);
      observer.disconnect();
      canvas.wrapperEl.removeEventListener("focusin", onFocusIn);
      canvas.wrapperEl.removeEventListener("focusout", onFocusOut);
      canvas.wrapperEl.removeEventListener("pointerdown", onCanvasPointerDown);
      viewport == null ? void 0 : viewport.removeEventListener("resize", positionBar);
      viewport == null ? void 0 : viewport.removeEventListener("scroll", positionBar);
      win.removeEventListener("resize", positionBar);
      bar.remove();
    }
  };
}

// src/import/freemind-import.ts
function parseFreeMindXml(xml) {
  const parser = new DOMParser();
  const doc = parser.parseFromString(xml, "text/xml");
  const errorNode = doc.querySelector("parsererror");
  if (errorNode) return [];
  const mapEl = doc.querySelector("map");
  if (!mapEl) return [];
  const roots = [];
  for (const child of Array.from(mapEl.children)) {
    if (child.tagName === "node" || child.tagName === "x-coggle-rootnode") {
      roots.push(parseNode(child, "right"));
    }
  }
  return roots;
}
function parseNode(el, inheritedPosition) {
  const text = el.getAttribute("TEXT") || "Untitled";
  const posAttr = el.getAttribute("POSITION");
  const position = posAttr === "left" ? "left" : posAttr === "right" ? "right" : inheritedPosition;
  const children = [];
  for (const child of Array.from(el.children)) {
    if (child.tagName === "node" || child.tagName === "x-coggle-rootnode") {
      children.push(parseNode(child, position));
    }
  }
  return { text, position, children };
}
function estimateNodeHeight(text, nodeWidth, minHeight, maxHeight) {
  const AVG_CHAR_WIDTH = 8;
  const LINE_HEIGHT = 22;
  const PADDING = 20;
  const charsPerLine = Math.max(1, Math.floor((nodeWidth - PADDING) / AVG_CHAR_WIDTH));
  const paragraphs = text.split("\n");
  let totalLines = 0;
  for (const para of paragraphs) {
    if (para.length === 0) {
      totalLines += 1;
    } else {
      totalLines += Math.ceil(para.length / charsPerLine);
    }
  }
  const estimated = totalLines * LINE_HEIGHT + PADDING;
  return Math.min(Math.max(estimated, minHeight), maxHeight);
}
function nodeHeight(node, opts) {
  return estimateNodeHeight(node.text, opts.nodeWidth, opts.nodeHeight, opts.maxNodeHeight);
}
function subtreeHeight(node, opts) {
  if (node.children.length === 0) return nodeHeight(node, opts);
  let total = 0;
  for (let i = 0; i < node.children.length; i++) {
    if (i > 0) total += opts.verticalGap;
    total += subtreeHeight(node.children[i], opts);
  }
  return Math.max(nodeHeight(node, opts), total);
}
function groupHeight(children, opts) {
  if (children.length === 0) return 0;
  let total = 0;
  for (let i = 0; i < children.length; i++) {
    if (i > 0) total += opts.verticalGap;
    total += subtreeHeight(children[i], opts);
  }
  return total;
}
function layoutTree(root, startX, startY, opts, nodes, edges) {
  const rootH = nodeHeight(root, opts);
  const rootId = genId();
  nodes.push({
    id: rootId,
    type: "text",
    x: startX,
    y: startY,
    width: opts.nodeWidth,
    height: rootH,
    text: root.text
  });
  if (root.children.length === 0) return rootH;
  const rightChildren = root.children.filter((c) => c.position === "right");
  const leftChildren = root.children.filter((c) => c.position === "left");
  const rootCy = startY + rootH / 2;
  layoutSide(rootId, rightChildren, "right", startX, rootCy, opts, nodes, edges);
  layoutSide(rootId, leftChildren, "left", startX, rootCy, opts, nodes, edges);
  const rightH = groupHeight(rightChildren, opts);
  const leftH = groupHeight(leftChildren, opts);
  return Math.max(rootH, rightH, leftH);
}
function layoutSide(parentId, children, side, parentX, parentCy, opts, nodes, edges) {
  if (children.length === 0) return;
  const totalH = groupHeight(children, opts);
  let childY = parentCy - totalH / 2;
  const fromSide = side === "right" ? "right" : "left";
  const toSide = side === "right" ? "left" : "right";
  const childX = side === "right" ? parentX + opts.nodeWidth + opts.horizontalGap : parentX - opts.nodeWidth - opts.horizontalGap;
  for (const child of children) {
    const childH = subtreeHeight(child, opts);
    const childNodeY = childY + childH / 2 - nodeHeight(child, opts) / 2;
    const childId = layoutBranch(child, childX, childNodeY, side, opts, nodes, edges);
    edges.push({
      id: genId(),
      fromNode: parentId,
      fromSide,
      fromEnd: "none",
      toNode: childId,
      toSide,
      toEnd: "arrow"
    });
    childY += childH + opts.verticalGap;
  }
}
function layoutBranch(node, x, y, side, opts, nodes, edges) {
  const h = nodeHeight(node, opts);
  const id = genId();
  nodes.push({
    id,
    type: "text",
    x,
    y,
    width: opts.nodeWidth,
    height: h,
    text: node.text
  });
  if (node.children.length === 0) return id;
  const fromSide = side === "right" ? "right" : "left";
  const toSide = side === "right" ? "left" : "right";
  const childX = side === "right" ? x + opts.nodeWidth + opts.horizontalGap : x - opts.nodeWidth - opts.horizontalGap;
  const totalH = groupHeight(node.children, opts);
  let childY = y + h / 2 - totalH / 2;
  for (const child of node.children) {
    const childH = subtreeHeight(child, opts);
    const childNodeY = childY + childH / 2 - nodeHeight(child, opts) / 2;
    const childId = layoutBranch(child, childX, childNodeY, side, opts, nodes, edges);
    edges.push({
      id: genId(),
      fromNode: id,
      fromSide,
      fromEnd: "none",
      toNode: childId,
      toSide,
      toEnd: "arrow"
    });
    childY += childH + opts.verticalGap;
  }
  return id;
}
function freemindToCanvas(xml, opts) {
  const roots = parseFreeMindXml(xml);
  if (roots.length === 0) return null;
  const nodes = [];
  const edges = [];
  let currentY = 0;
  const treeGap = opts.verticalGap * 4;
  for (const root of roots) {
    const height = layoutTree(root, 0, currentY, opts, nodes, edges);
    currentY += height + treeGap;
  }
  return { nodes, edges, mindmap: true };
}

// src/main.ts
var CanvasMindMapPlugin = class extends import_obsidian9.Plugin {
  constructor() {
    super(...arguments);
    this.settings = DEFAULT_SETTINGS;
    this.cleanupClickHandler = null;
    this.cleanupDragHandler = null;
    this.cleanupSubtreeDragHandler = null;
    this.cleanupDragReparentHandler = null;
    this.cleanupGroupDragHandler = null;
    this.autoResizeHandle = null;
    this.branchCollapseHandle = null;
    this.interceptedCanvas = null;
    this.toggleBtnEl = null;
    this.dragReparentBtnEl = null;
    this.enterTabBtnEl = null;
    this.mobileActionsBtnEl = null;
    this.mobileEditingBarHandle = null;
    this.cleanupGroupBoundsHandler = null;
    this.cleanupSelectionSyncHandler = null;
    this.cleanupInsertNodeHandler = null;
    /** Pending timers/observers/RAFs to cancel on unload or canvas switch. */
    this.pendingTimers = /* @__PURE__ */ new Set();
    this.pendingRafs = /* @__PURE__ */ new Set();
    this.pendingObservers = /* @__PURE__ */ new Set();
    /** Original canvas methods for unwrapping on cleanup. */
    this.origCanvasMethods = {};
    /** Set to true on unload to prevent deferred callbacks from running. */
    this.unloaded = false;
    /** Navigation history for back/forward. */
    this.navHistory = [];
    this.navHistoryIndex = -1;
    this.navSkipTracking = false;
    this.lastNavCanvas = null;
    this.cleanupNavHandler = null;
    this.debouncedOutlineRefresh = (0, import_obsidian9.debounce)(() => {
      var _a;
      if (this.unloaded) return;
      const canvas = (_a = this.canvasApi.getActiveCanvas()) != null ? _a : this.canvasApi.getAnyCanvas();
      if (canvas) {
        this.refreshOutline(canvas);
      }
    }, 300);
  }
  async onload() {
    await this.loadSettings();
    this.registerBranchColorIcons();
    this.canvasApi = new CanvasAPI(this.app);
    this.nodeOps = new NodeOperations(this.canvasApi, {
      nodeWidth: this.settings.defaultNodeWidth,
      nodeHeight: this.settings.defaultNodeHeight,
      horizontalGap: this.settings.horizontalGap,
      verticalGap: this.settings.verticalGap
    });
    this.layoutEngine = new LayoutEngine({
      horizontalGap: this.settings.horizontalGap,
      verticalGap: this.settings.verticalGap,
      nodeWidth: this.settings.defaultNodeWidth,
      nodeHeight: this.settings.defaultNodeHeight
    });
    this.branchColors = new BranchColors(
      this.canvasApi,
      this.settings.branchPalette,
      this.settings.colorLeafNodes
    );
    this.navigation = new Navigation(this.canvasApi);
    this.keyboardHandler = new KeyboardHandler(
      this,
      this.canvasApi,
      this.nodeOps,
      this.layoutEngine,
      this.branchColors,
      () => this.settings.autoColor,
      () => this.settings.autoLayout,
      () => this.settings.arrowKeyNavigation,
      () => this.settings.centerNodeOnArrowNavigation,
      () => this.settings.enterCreatesSibling,
      (canvas) => this.isMindmapCanvas(canvas),
      (canvas) => this.updateGroupBounds(canvas)
    );
    this.keyboardHandler.zoomPadding = this.settings.navigationZoomPadding;
    this.keyboardHandler.register();
    this.addCommand({
      id: "mindmap-relayout",
      name: "Re-layout mind map",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (!this.isMindmapCanvas(canvas)) return false;
        if (checking) return true;
        this.layoutEngine.layout(canvas);
        this.updateGroupBounds(canvas);
      }
    });
    this.addCommand({
      id: "mindmap-relayout-selected-branch",
      name: "Re-layout selected branch",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas || !this.isMindmapCanvas(canvas)) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node || this.canvasApi.getChildNodes(canvas, node).length === 0) return false;
        if (checking) return true;
        this.relayoutSelectedBranch(canvas);
      }
    });
    this.addCommand({
      id: "mindmap-create-root",
      name: "Create root node",
      checkCallback: (checking) => {
        var _a;
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas || !this.isMindmapCanvas(canvas)) return false;
        if ((_a = this.canvasApi.getSelectedNode(canvas)) == null ? void 0 : _a.isEditing) return false;
        if (checking) return true;
        this.createRootNode();
      }
    });
    this.addCommand({
      id: "mindmap-layout-forest",
      name: "Layout forest",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (!this.isMindmapCanvas(canvas)) return false;
        const selected = this.canvasApi.getSelectedNode(canvas);
        if (!selected) return false;
        const groupIds = getGroupIds(canvas);
        const cx = selected.x + selected.width / 2;
        const cy = selected.y + selected.height / 2;
        let targetGroupId = null;
        let smallestArea = Infinity;
        for (const gid of groupIds) {
          const g = canvas.nodes.get(gid);
          if (!g) continue;
          if (cx >= g.x && cx <= g.x + g.width && cy >= g.y && cy <= g.y + g.height) {
            const area = g.width * g.height;
            if (area < smallestArea) {
              smallestArea = area;
              targetGroupId = gid;
            }
          }
        }
        if (!targetGroupId) return false;
        if (checking) return true;
        this.layoutEngine.layoutForest(canvas, targetGroupId);
      }
    });
    this.addCommand({
      id: "mindmap-detach-subtree",
      name: "Detach subtree as independent tree",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (!this.isMindmapCanvas(canvas)) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        const parent = this.canvasApi.getParentNode(canvas, node);
        if (!parent) return false;
        if (checking) return true;
        const edges = this.canvasApi.getOutgoingEdges(canvas, parent.id);
        const edge = edges.find((e) => e.to.node.id === node.id);
        if (!edge) return;
        canvas.removeEdge(edge);
        this.canvasApi.invalidateEdgeIndex();
        this.updateGroupBounds(canvas);
        canvas.requestSave();
      }
    });
    this.addCommand({
      id: "mindmap-resize-subtree",
      name: "Resize & re-layout selected subtree",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        const node = this.canvasApi.getSelectedNode(canvas);
        if (!node) return false;
        if (checking) return true;
        const wasEditing = node.isEditing;
        this.resizeNodes(canvas, this.collectSubtreeNodes(canvas, node));
        this.layoutEngine.layoutChildren(canvas, node.id);
        this.updateGroupBounds(canvas);
        if (wasEditing) node.startEditing();
      }
    });
    this.addCommand({
      id: "mindmap-resize-all",
      name: "Resize all nodes to fit content",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (!this.isMindmapCanvas(canvas)) return false;
        if (canvas.nodes.size === 0) return false;
        if (checking) return true;
        this.resizeNodes(canvas, Array.from(canvas.nodes.values()));
        this.layoutEngine.layout(canvas);
        this.updateGroupBounds(canvas);
      }
    });
    this.addCommand({
      id: "mindmap-apply-colors",
      name: "Apply branch colors",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (!this.isMindmapCanvas(canvas)) return false;
        if (checking) return true;
        this.branchColors.applyColors(canvas);
      }
    });
    this.addCommand({
      id: "mindmap-toggle-mode",
      name: "Toggle mindmap mode for this canvas",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas) return false;
        if (checking) return true;
        this.toggleMindmapMode(canvas);
      }
    });
    this.registerEvent(
      this.app.workspace.on("active-leaf-change", (leaf) => {
        this.onLeafChange(leaf);
      })
    );
    this.registerView(OUTLINE_VIEW_TYPE, (leaf) => new OutlineView(leaf));
    this.app.workspace.onLayoutReady(() => {
      const view = this.app.workspace.getActiveViewOfType(import_obsidian9.ItemView);
      if (view) this.onLeafChange(view.leaf);
    });
    this.registerEvent(
      this.app.workspace.on("file-menu", (menu, file) => {
        if (!(file instanceof import_obsidian9.TFolder)) return;
        menu.addItem((item) => {
          item.setTitle("Import mind map (.mm) to canvas").setIcon("file-input").onClick(() => this.importFreeMindFile(file.path));
        });
      })
    );
    this.registerEvent(
      this.app.workspace.on("canvas:menu", (menu, canvas) => {
        if (!this.isMindmapCanvas(canvas)) return;
        menu.addItem((item) => item.setTitle("Create root node").setIcon("circle-plus").onClick(() => this.createRootNode()));
      })
    );
    this.registerEvent(
      this.app.workspace.on("canvas:node-menu", (menu, node) => {
        const canvas = node.canvas;
        menu.addItem((item) => {
          item.setTitle("Copy node link").setIcon("link").onClick(() => {
            const canvasPath = node.canvas.view.file.path;
            void copyText(
              node.nodeEl.win,
              `obsidian://cammvas-navigate?canvas=${encodeURIComponent(canvasPath)}&id=${node.id}`,
              "Node link copied"
            );
          });
        });
        if (import_obsidian9.Platform.isMobile && this.isMindmapCanvas(canvas)) {
          menu.addItem((item) => item.setTitle("Add child node").setIcon("corner-down-right").onClick(() => this.keyboardHandler.addChildNode(canvas, node)));
          menu.addItem((item) => item.setTitle("Add sibling node").setIcon("list-plus").onClick(() => this.keyboardHandler.addSiblingNode(canvas, node)));
          menu.addItem((item) => item.setTitle("Zoom to branch").setIcon("scan").onClick(() => this.navigation.zoomToBranch(canvas, node)));
        }
        const groupIds = getGroupIds(canvas);
        if (this.isMindmapCanvas(canvas) && !groupIds.has(node.id)) {
          const branchColors = [
            ["1", "Red"],
            ["2", "Orange"],
            ["3", "Yellow"],
            ["4", "Green"],
            ["5", "Blue"],
            ["6", "Purple"]
          ];
          for (const [color, label] of branchColors) {
            menu.addItem((item) => item.setTitle(`Branch color: ${label}`).setIcon(`cammvas-color-${color}`).onClick(() => this.branchColors.setBranchColor(canvas, node.id, color)));
          }
        }
        if (groupIds.has(node.id)) {
          menu.addItem((item) => {
            item.setTitle("Layout forest").setIcon("layout-grid").onClick(() => {
              this.layoutEngine.layoutForest(canvas, node.id);
              this.updateGroupBounds(canvas);
            });
          });
        } else if (this.isMindmapCanvas(canvas)) {
          menu.addItem((item) => {
            item.setTitle("Re-layout selected branch").setIcon("list-tree").onClick(() => {
              this.relayoutSelectedBranch(canvas, node);
            });
          });
        }
      })
    );
    this.registerObsidianProtocolHandler("cammvas-navigate", async (params) => {
      var _a;
      const nodeId = params.id;
      if (!nodeId) return;
      const canvasPath = params.canvas;
      let canvas = null;
      if (canvasPath) {
        const file = this.app.vault.getAbstractFileByPath(canvasPath);
        if (file && file instanceof import_obsidian9.TFile) {
          const leaf = this.app.workspace.getLeaf();
          await leaf.openFile(file);
          canvas = await this.waitForCanvas(canvasPath, leaf.view.containerEl.win);
        }
        if (!canvas) {
          new import_obsidian9.Notice("Canvas not found");
          return;
        }
      }
      canvas != null ? canvas : canvas = (_a = this.canvasApi.getActiveCanvas()) != null ? _a : this.canvasApi.getAnyCanvas();
      if (!canvas) {
        new import_obsidian9.Notice("Canvas not found");
        return;
      }
      const node = canvas.nodes.get(nodeId);
      if (!node) {
        new import_obsidian9.Notice("Target node not found");
        return;
      }
      this.canvasApi.selectAndZoom(canvas, node, this.settings.navigationZoomPadding);
    });
    this.addCommand({
      id: "mindmap-nav-back",
      name: "Navigate back",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas || this.navHistoryIndex <= 0) return false;
        if (checking) return true;
        this.navigateBack(canvas);
      }
    });
    this.addCommand({
      id: "mindmap-nav-forward",
      name: "Navigate forward",
      checkCallback: (checking) => {
        const canvas = this.canvasApi.getActiveCanvas();
        if (!canvas || this.navHistoryIndex >= this.navHistory.length - 1) return false;
        if (checking) return true;
        this.navigateForward(canvas);
      }
    });
    this.addCommand({
      id: "mindmap-import-freemind",
      name: "Import mind map (.mm) file to canvas",
      callback: () => this.importFreeMindFile()
    });
    this.addSettingTab(new MindMapSettingTab(this.app, this));
  }
  pushNavHistory(nodeId) {
    if (this.navHistory[this.navHistoryIndex] === nodeId) return;
    this.navHistory.splice(this.navHistoryIndex + 1);
    this.navHistory.push(nodeId);
    if (this.navHistory.length > 50) this.navHistory.shift();
    this.navHistoryIndex = this.navHistory.length - 1;
  }
  navigateBack(canvas) {
    var _a, _b;
    if (this.navHistoryIndex <= 0) return;
    (_b = (_a = this.keyboardHandler) == null ? void 0 : _a.onBeforeLeaveNode) == null ? void 0 : _b.call(_a);
    this.navSkipTracking = true;
    this.navHistoryIndex--;
    const nodeId = this.navHistory[this.navHistoryIndex];
    const node = canvas.nodes.get(nodeId);
    if (!node) {
      this.navSkipTracking = false;
      return;
    }
    this.canvasApi.selectAndZoom(canvas, node, this.settings.navigationZoomPadding);
    this.navSkipTracking = false;
  }
  navigateForward(canvas) {
    var _a, _b;
    if (this.navHistoryIndex >= this.navHistory.length - 1) return;
    (_b = (_a = this.keyboardHandler) == null ? void 0 : _a.onBeforeLeaveNode) == null ? void 0 : _b.call(_a);
    this.navSkipTracking = true;
    this.navHistoryIndex++;
    const nodeId = this.navHistory[this.navHistoryIndex];
    const node = canvas.nodes.get(nodeId);
    if (!node) {
      this.navSkipTracking = false;
      return;
    }
    this.canvasApi.selectAndZoom(canvas, node, this.settings.navigationZoomPadding);
    this.navSkipTracking = false;
  }
  async waitForCanvas(path, win) {
    for (let attempt = 0; attempt < 40; attempt++) {
      if (this.unloaded) return null;
      const canvas = this.canvasApi.getActiveCanvas();
      if ((canvas == null ? void 0 : canvas.view.file.path) === path) return canvas;
      await new Promise((resolve) => win.setTimeout(resolve, 50));
    }
    return null;
  }
  onunload() {
    this.unloaded = true;
    this.cancelPendingAsync();
    this.unwrapCanvasMethods();
    if (this.cleanupClickHandler) {
      this.cleanupClickHandler();
      this.cleanupClickHandler = null;
    }
    if (this.cleanupDragHandler) {
      this.cleanupDragHandler();
      this.cleanupDragHandler = null;
    }
    if (this.cleanupSubtreeDragHandler) {
      this.cleanupSubtreeDragHandler();
      this.cleanupSubtreeDragHandler = null;
    }
    if (this.cleanupDragReparentHandler) {
      this.cleanupDragReparentHandler();
      this.cleanupDragReparentHandler = null;
    }
    if (this.cleanupGroupDragHandler) {
      this.cleanupGroupDragHandler();
      this.cleanupGroupDragHandler = null;
    }
    if (this.cleanupGroupBoundsHandler) {
      this.cleanupGroupBoundsHandler();
      this.cleanupGroupBoundsHandler = null;
    }
    if (this.cleanupSelectionSyncHandler) {
      this.cleanupSelectionSyncHandler();
      this.cleanupSelectionSyncHandler = null;
    }
    if (this.cleanupInsertNodeHandler) {
      this.cleanupInsertNodeHandler();
      this.cleanupInsertNodeHandler = null;
    }
    if (this.cleanupNavHandler) {
      this.cleanupNavHandler();
      this.cleanupNavHandler = null;
    }
    if (this.autoResizeHandle) {
      this.autoResizeHandle.cleanup();
      this.autoResizeHandle = null;
    }
    if (this.mobileEditingBarHandle) {
      this.mobileEditingBarHandle.cleanup();
      this.mobileEditingBarHandle = null;
    }
    if (this.branchCollapseHandle) {
      this.branchCollapseHandle.cleanup();
      this.branchCollapseHandle = null;
    }
    this.keyboardHandler.unregisterArrowKeyNavigation();
    this.lastNavCanvas = null;
    if (this.toggleBtnEl) {
      this.toggleBtnEl.remove();
      this.toggleBtnEl = null;
    }
    if (this.dragReparentBtnEl) {
      this.dragReparentBtnEl.remove();
      this.dragReparentBtnEl = null;
    }
    if (this.enterTabBtnEl) {
      this.enterTabBtnEl.remove();
      this.enterTabBtnEl = null;
    }
    if (this.mobileActionsBtnEl) {
      this.mobileActionsBtnEl.remove();
      this.mobileActionsBtnEl = null;
    }
  }
  /**
   * Called when the active leaf changes — set up canvas-specific UI.
   */
  onLeafChange(leaf) {
    var _a, _b, _c;
    if (((_a = leaf == null ? void 0 : leaf.view) == null ? void 0 : _a.getViewType()) === OUTLINE_VIEW_TYPE) return;
    const root = leaf == null ? void 0 : leaf.getRoot();
    if (root && root !== this.app.workspace.rootSplit) return;
    this.cancelPendingAsync();
    this.unwrapCanvasMethods();
    if (this.cleanupClickHandler) {
      this.cleanupClickHandler();
      this.cleanupClickHandler = null;
    }
    if (this.cleanupDragHandler) {
      this.cleanupDragHandler();
      this.cleanupDragHandler = null;
    }
    if (this.cleanupSubtreeDragHandler) {
      this.cleanupSubtreeDragHandler();
      this.cleanupSubtreeDragHandler = null;
    }
    if (this.cleanupDragReparentHandler) {
      this.cleanupDragReparentHandler();
      this.cleanupDragReparentHandler = null;
    }
    if (this.cleanupGroupDragHandler) {
      this.cleanupGroupDragHandler();
      this.cleanupGroupDragHandler = null;
    }
    if (this.cleanupGroupBoundsHandler) {
      this.cleanupGroupBoundsHandler();
      this.cleanupGroupBoundsHandler = null;
    }
    if (this.cleanupSelectionSyncHandler) {
      this.cleanupSelectionSyncHandler();
      this.cleanupSelectionSyncHandler = null;
    }
    if (this.cleanupInsertNodeHandler) {
      this.cleanupInsertNodeHandler();
      this.cleanupInsertNodeHandler = null;
    }
    if (this.cleanupNavHandler) {
      this.cleanupNavHandler();
      this.cleanupNavHandler = null;
    }
    if (this.autoResizeHandle) {
      this.autoResizeHandle.cleanup();
      this.autoResizeHandle = null;
    }
    if (this.mobileEditingBarHandle) {
      this.mobileEditingBarHandle.cleanup();
      this.mobileEditingBarHandle = null;
    }
    if (this.branchCollapseHandle) {
      this.branchCollapseHandle.cleanup();
      this.branchCollapseHandle = null;
    }
    this.keyboardHandler.unregisterArrowKeyNavigation();
    const canvas = this.canvasApi.getActiveCanvas();
    if (canvas && canvas !== this.lastNavCanvas) {
      this.navHistory = [];
      this.navHistoryIndex = -1;
    }
    if (canvas) {
      this.lastNavCanvas = canvas;
    }
    if (!canvas) {
      if (this.toggleBtnEl) {
        this.toggleBtnEl.remove();
        this.toggleBtnEl = null;
      }
      if (this.dragReparentBtnEl) {
        this.dragReparentBtnEl.remove();
        this.dragReparentBtnEl = null;
      }
      if (this.enterTabBtnEl) {
        this.enterTabBtnEl.remove();
        this.enterTabBtnEl = null;
      }
      if (this.mobileActionsBtnEl) {
        this.mobileActionsBtnEl.remove();
        this.mobileActionsBtnEl = null;
      }
      this.hideOutline();
      return;
    }
    this.keyboardHandler.registerArrowKeyNavigation(canvas);
    this.injectToggleButton(canvas);
    this.cleanupClickHandler = import_obsidian9.Platform.isMobile ? null : this.navigation.registerClickHandler(canvas);
    this.cleanupDragHandler = registerDragEndHandler(canvas);
    this.cleanupSubtreeDragHandler = registerSubtreeDragHandler(canvas, this.canvasApi);
    this.cleanupDragReparentHandler = registerDragReparent(
      canvas,
      this.canvasApi,
      () => this.settings.dragToReparent && this.isMindmapCanvas(canvas),
      (nodes, newParent) => {
        var _a2;
        let changed = false;
        for (const node of nodes) {
          changed = this.nodeOps.reparent(canvas, node, newParent) || changed;
        }
        if (!changed) return;
        if (this.settings.autoLayout) this.layoutEngine.layout(canvas);
        this.updateGroupBounds(canvas);
        (_a2 = this.branchCollapseHandle) == null ? void 0 : _a2.refresh();
      }
    );
    this.cleanupGroupDragHandler = import_obsidian9.Platform.isMobile ? null : registerGroupDragHandler(canvas, this.canvasApi);
    this.branchCollapseHandle = registerBranchCollapse(canvas, this.canvasApi);
    const onDragEnd = () => this.trackedRaf(canvas.wrapperEl.win, () => this.updateGroupBounds(canvas));
    canvas.wrapperEl.addEventListener("pointerup", onDragEnd);
    this.cleanupGroupBoundsHandler = () => canvas.wrapperEl.removeEventListener("pointerup", onDragEnd);
    const syncOutlineSelection = () => {
      this.trackedRaf(canvas.wrapperEl.win, () => {
        for (const leaf2 of this.app.workspace.getLeavesOfType(OUTLINE_VIEW_TYPE)) {
          if (leaf2.view instanceof OutlineView) {
            leaf2.view.syncHighlightFromCanvas(canvas);
          }
        }
      });
    };
    const onCanvasClick = () => syncOutlineSelection();
    const onCanvasKeydown = (e) => {
      if (e.key === "Escape") syncOutlineSelection();
      if (e.key === "s" && (e.ctrlKey || e.metaKey) && !e.shiftKey) syncOutlineSelection();
    };
    canvas.wrapperEl.addEventListener("click", onCanvasClick);
    canvas.wrapperEl.addEventListener("keydown", onCanvasKeydown);
    this.cleanupSelectionSyncHandler = () => {
      canvas.wrapperEl.removeEventListener("click", onCanvasClick);
      canvas.wrapperEl.removeEventListener("keydown", onCanvasKeydown);
    };
    const onInsertNodeClick = (e) => {
      if (!e.altKey) return;
      const target = e.target;
      if (!isHtmlElement(target)) return;
      const connectionPoint = target.closest(".canvas-node-connection-point");
      if (!connectionPoint) return;
      const side = connectionPoint.getAttribute("data-side");
      if (!side) return;
      const canvasPos = canvas.posFromEvt(e);
      let clickedNode = null;
      let closestDist = Infinity;
      for (const node of canvas.nodes.values()) {
        const cx = node.x + node.width / 2;
        const cy = node.y + node.height / 2;
        const dist = Math.hypot(canvasPos.x - cx, canvasPos.y - cy);
        if (dist < closestDist) {
          closestDist = dist;
          clickedNode = node;
        }
      }
      if (!clickedNode) return;
      const incomingEdges = [];
      const outgoingEdges = [];
      for (const edge of canvas.edges.values()) {
        if (edge.to.node.id === clickedNode.id && edge.to.side === side) {
          incomingEdges.push(edge);
        }
        if (edge.from.node.id === clickedNode.id && edge.from.side === side) {
          outgoingEdges.push(edge);
        }
      }
      const edges = outgoingEdges.length > 0 ? outgoingEdges : incomingEdges;
      if (edges.length === 0) return;
      e.preventDefault();
      e.stopPropagation();
      const isOutgoing = outgoingEdges.length > 0;
      const fromSide = edges[0].from.side;
      const toSide = edges[0].to.side;
      if (isOutgoing) {
        const children = edges.map((edge) => edge.to.node);
        const avgY = children.reduce((s, c) => s + c.y + c.height / 2, 0) / children.length;
        const midX = (clickedNode.x + clickedNode.width + children[0].x) / 2 - this.settings.defaultNodeWidth / 2;
        const midY = avgY - this.settings.defaultNodeHeight / 2;
        const newNode = this.canvasApi.createTextNode(canvas, midX, midY);
        for (const edge of edges) canvas.removeEdge(edge);
        this.canvasApi.invalidateEdgeIndex();
        this.canvasApi.createEdge(canvas, clickedNode, newNode, fromSide, toSide);
        for (const child of children) {
          this.canvasApi.createEdge(canvas, newNode, child, fromSide, toSide);
        }
        this.finishInsertNode(canvas, newNode, clickedNode);
      } else {
        const edge = edges[0];
        const parentNode = edge.from.node;
        const midX = (parentNode.x + parentNode.width / 2 + clickedNode.x + clickedNode.width / 2) / 2 - this.settings.defaultNodeWidth / 2;
        const midY = (parentNode.y + parentNode.height / 2 + clickedNode.y + clickedNode.height / 2) / 2 - this.settings.defaultNodeHeight / 2;
        const newNode = this.canvasApi.createTextNode(canvas, midX, midY);
        canvas.removeEdge(edge);
        this.canvasApi.invalidateEdgeIndex();
        this.canvasApi.createEdge(canvas, parentNode, newNode, fromSide, toSide);
        this.canvasApi.createEdge(canvas, newNode, clickedNode, fromSide, toSide);
        this.finishInsertNode(canvas, newNode, parentNode);
      }
    };
    if (!import_obsidian9.Platform.isMobile) {
      canvas.wrapperEl.addEventListener("click", onInsertNodeClick, true);
      this.cleanupInsertNodeHandler = () => canvas.wrapperEl.removeEventListener("click", onInsertNodeClick, true);
    }
    this.autoResizeHandle = registerAutoResize(
      canvas,
      {
        minHeight: this.settings.defaultNodeHeight,
        maxHeight: this.settings.maxNodeHeight
      },
      (canvas2, editedNode) => {
        this.waitForPreview(editedNode, () => {
          if (this.canvasApi.getActiveCanvas() !== canvas2) return;
          const forest = buildForest(canvas2);
          const treeNode = findTreeForNode(forest, editedNode.id);
          if (!treeNode) return;
          let root2 = treeNode;
          while (root2.parent) root2 = root2.parent;
          this.resizeNodes(canvas2, this.collectSubtreeNodes(canvas2, root2.canvasNode));
          this.updateGroupBounds(canvas2);
        });
      }
    );
    this.keyboardHandler.onBeforeLeaveNode = () => {
      var _a2;
      (_a2 = this.autoResizeHandle) == null ? void 0 : _a2.finalizeNode();
      const node = this.canvasApi.getSelectedNode(canvas);
      if (node == null ? void 0 : node.isEditing) {
        this.waitForPreview(node, () => {
          if (this.canvasApi.getActiveCanvas() !== canvas) return;
          this.resizeNodes(canvas, [node]);
          this.finalizeEdit(canvas);
        });
      }
    };
    if (import_obsidian9.Platform.isMobile) {
      this.mobileEditingBarHandle = registerMobileEditingBar(
        canvas,
        () => this.isMindmapCanvas(canvas),
        (node) => this.canvasApi.getParentNode(canvas, node) !== null,
        (node) => this.keyboardHandler.addChildNode(canvas, node, {
          immediateEdit: true,
          transferSelection: false
        }),
        (node) => this.keyboardHandler.addSiblingNode(canvas, node, {
          immediateEdit: true,
          transferSelection: false
        }),
        (node) => {
          var _a2, _b2;
          (_b2 = (_a2 = this.keyboardHandler).onBeforeLeaveNode) == null ? void 0 : _b2.call(_a2);
          node.blur();
        }
      );
    }
    if (this.settings.mouseNavigation && !import_obsidian9.Platform.isMobile) {
      const onPointerDown = (e) => {
        if (e.button === 3) {
          e.preventDefault();
          e.stopImmediatePropagation();
          this.navigateBack(canvas);
        }
        if (e.button === 4) {
          e.preventDefault();
          e.stopImmediatePropagation();
          this.navigateForward(canvas);
        }
      };
      canvas.wrapperEl.addEventListener("pointerdown", onPointerDown, true);
      this.cleanupNavHandler = () => canvas.wrapperEl.removeEventListener("pointerdown", onPointerDown, true);
    }
    if (this.settings.autoColor && this.isMindmapCanvas(canvas)) {
      this.branchColors.applyColors(canvas);
    }
    const origSave = canvas.requestSave.bind(canvas);
    const origCreateGroup = canvas.createGroupNode.bind(canvas);
    const origUndo = (_b = canvas.undo) == null ? void 0 : _b.bind(canvas);
    const origRedo = (_c = canvas.redo) == null ? void 0 : _c.bind(canvas);
    const origSelectOnly = canvas.selectOnly.bind(canvas);
    this.origCanvasMethods = { requestSave: origSave, createGroupNode: origCreateGroup, undo: origUndo, redo: origRedo, selectOnly: origSelectOnly };
    this.interceptedCanvas = canvas;
    canvas.selectOnly = (item) => {
      origSelectOnly(item);
      if (!this.navSkipTracking && "nodeEl" in item) {
        this.pushNavHistory(item.id);
      }
    };
    canvas.requestSave = () => {
      var _a2;
      if (this.isMindmapCanvas(canvas)) {
        this.branchColors.inheritConnectionColors(canvas);
      }
      origSave();
      (_a2 = this.branchCollapseHandle) == null ? void 0 : _a2.refresh();
      this.debouncedOutlineRefresh();
    };
    canvas.createGroupNode = (options) => {
      const group = origCreateGroup(options);
      this.updateGroupBounds(canvas);
      return group;
    };
    if (origUndo) {
      canvas.undo = () => {
        var _a2;
        origUndo();
        this.canvasApi.invalidateEdgeIndex();
        (_a2 = this.branchCollapseHandle) == null ? void 0 : _a2.refresh();
        this.debouncedOutlineRefresh();
      };
    }
    if (origRedo) {
      canvas.redo = () => {
        var _a2;
        origRedo();
        this.canvasApi.invalidateEdgeIndex();
        (_a2 = this.branchCollapseHandle) == null ? void 0 : _a2.refresh();
        this.debouncedOutlineRefresh();
      };
    }
    if (this.isMindmapCanvas(canvas)) {
      this.showOutline(canvas);
    } else {
      this.hideOutline();
    }
  }
  refreshOutline(canvas) {
    for (const leaf of this.app.workspace.getLeavesOfType(OUTLINE_VIEW_TYPE)) {
      const view = leaf.view;
      if (view instanceof OutlineView) {
        view.zoomPadding = this.settings.navigationZoomPadding;
        view.onForestLayout = (c, groupId) => {
          this.layoutEngine.layoutForest(c, groupId);
          this.updateGroupBounds(c);
        };
        view.refresh(canvas);
      }
    }
  }
  /**
   * Collect a node and all its descendants via BFS.
   */
  collectSubtreeNodes(canvas, root) {
    const result = [root];
    const visited = /* @__PURE__ */ new Set([root.id]);
    const queue = [root.id];
    while (queue.length > 0) {
      const id = queue.shift();
      for (const edge of this.canvasApi.getOutgoingEdges(canvas, id)) {
        const childId = edge.to.node.id;
        if (!visited.has(childId)) {
          visited.add(childId);
          result.push(edge.to.node);
          queue.push(childId);
        }
      }
    }
    return result;
  }
  /**
   * Recalculate bounds for all groups to tightly fit their contained subtrees.
   * A root node belongs to a group if its center is inside the group's current bounds.
   */
  updateGroupBounds(canvas) {
    var _a;
    const PADDING = 20;
    const groupIds = getGroupIds(canvas);
    if (groupIds.size === 0) return;
    let changed = false;
    for (const groupId of groupIds) {
      const group = canvas.nodes.get(groupId);
      if (!group) continue;
      const gx = group.x;
      const gy = group.y;
      const gw = group.width;
      const gh = group.height;
      const contained = /* @__PURE__ */ new Set();
      for (const node of canvas.nodes.values()) {
        if (groupIds.has(node.id)) continue;
        const cx = node.x + node.width / 2;
        const cy = node.y + node.height / 2;
        if (cx >= gx && cx <= gx + gw && cy >= gy && cy <= gy + gh) {
          for (const n of this.collectSubtreeNodes(canvas, node)) {
            contained.add(n);
          }
        }
      }
      if (contained.size === 0) continue;
      let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
      for (const node of contained) {
        minX = Math.min(minX, node.x);
        minY = Math.min(minY, node.y);
        maxX = Math.max(maxX, node.x + node.width);
        maxY = Math.max(maxY, node.y + node.height);
      }
      const newX = minX - PADDING;
      const newY = minY - PADDING;
      const newW = maxX - minX + PADDING * 2;
      const newH = maxY - minY + PADDING * 2;
      if (newX !== gx || newY !== gy || newW !== gw || newH !== gh) {
        (_a = group.nodeEl) == null ? void 0 : _a.addClass("mindmap-group-animating");
        group.moveAndResize({ x: newX, y: newY, width: newW, height: newH });
        changed = true;
      }
    }
    if (changed) {
      canvas.requestSave();
      this.trackedTimeout(canvas.wrapperEl.win, () => {
        var _a2;
        for (const groupId of groupIds) {
          const group = canvas.nodes.get(groupId);
          (_a2 = group == null ? void 0 : group.nodeEl) == null ? void 0 : _a2.removeClass("mindmap-group-animating");
        }
      }, 260);
    }
  }
  /**
   * Wait for a node's preview sizer to appear in the DOM, then invoke callback.
   * Uses MutationObserver instead of arbitrary setTimeout for precise timing.
   */
  waitForPreview(node, callback) {
    var _a;
    const sizer = (_a = node.contentEl) == null ? void 0 : _a.querySelector(".markdown-preview-sizer");
    if (sizer && !node.isEditing) {
      callback();
      return;
    }
    const Observer = Reflect.get(node.contentEl.win, "MutationObserver");
    const observer = new Observer(() => {
      var _a2;
      const s = (_a2 = node.contentEl) == null ? void 0 : _a2.querySelector(".markdown-preview-sizer");
      if (s && !node.isEditing) {
        observer.disconnect();
        this.pendingObservers.delete(observer);
        this.trackedRaf(node.contentEl.win, () => callback());
      }
    });
    this.pendingObservers.add(observer);
    observer.observe(node.contentEl, { childList: true, subtree: true });
    this.trackedTimeout(node.contentEl.win, () => {
      observer.disconnect();
      this.pendingObservers.delete(observer);
    }, 500);
  }
  /**
   * Finalize an edit without overriding manually arranged node positions.
   */
  finalizeEdit(canvas) {
    this.updateGroupBounds(canvas);
  }
  /**
   * Resize nodes to fit their rendered content, capped at maxNodeHeight.
   * Handles both preview mode (markdown sizer) and edit mode (CodeMirror).
   */
  resizeNodes(canvas, nodes) {
    var _a;
    const minH = this.settings.defaultNodeHeight;
    const maxH = this.settings.maxNodeHeight;
    const targetW = this.settings.defaultNodeWidth;
    const BORDER = 2;
    const SCALE = 1.2;
    let changed = false;
    const unmeasurable = [];
    for (const node of nodes) {
      let contentH = null;
      let targetH = node.height;
      if (node.isEditing) {
        const { cmContent, scroller } = getEditorElements(node);
        if (cmContent && scroller) {
          contentH = 0;
          for (const child of Array.from(cmContent.children)) {
            if (isHtmlElement(child)) contentH += child.offsetHeight;
          }
          targetH = Math.min(Math.max(Math.ceil(contentH * SCALE) + BORDER, minH), maxH);
          if (targetH !== node.height || targetW !== node.width) {
            node.moveAndResize({ x: node.x, y: node.y, width: targetW, height: targetH });
            changed = true;
          }
          continue;
        }
      }
      const sizer = (_a = node.contentEl) == null ? void 0 : _a.querySelector(".markdown-preview-sizer");
      if (!sizer) {
        if (node.width !== targetW) {
          node.moveAndResize({ x: node.x, y: node.y, width: targetW, height: node.height });
          changed = true;
        }
        if (node.text) unmeasurable.push(node);
        continue;
      }
      contentH = 0;
      for (const child of Array.from(sizer.children)) {
        if (isHtmlElement(child)) contentH += child.offsetHeight;
      }
      if (contentH === 0 && node.text) {
        if (node.width !== targetW) {
          node.moveAndResize({ x: node.x, y: node.y, width: targetW, height: node.height });
          changed = true;
        }
        unmeasurable.push(node);
        continue;
      }
      targetH = Math.min(Math.max(Math.ceil(contentH * SCALE) + BORDER, minH), maxH);
      if (targetH === node.height && targetW === node.width) continue;
      node.moveAndResize({ x: node.x, y: node.y, width: targetW, height: targetH });
      changed = true;
    }
    if (changed) canvas.requestSave();
    if (unmeasurable.length > 0) {
      this.trackedTimeout(canvas.wrapperEl.win, () => this.resizeNodesRetry(canvas, unmeasurable, minH, maxH, BORDER, SCALE), 200);
    }
  }
  /**
   * Retry resizing nodes that couldn't be measured on the first pass.
   * After layout repositions nodes, Obsidian may have rendered their content.
   */
  resizeNodesRetry(canvas, nodes, minH, maxH, BORDER, SCALE) {
    var _a;
    let changed = false;
    for (const node of nodes) {
      const sizer = (_a = node.contentEl) == null ? void 0 : _a.querySelector(".markdown-preview-sizer");
      if (!sizer) continue;
      let contentH = 0;
      for (const child of Array.from(sizer.children)) {
        if (isHtmlElement(child)) contentH += child.offsetHeight;
      }
      if (contentH === 0) continue;
      const targetH = Math.min(Math.max(Math.ceil(contentH * SCALE) + BORDER, minH), maxH);
      if (targetH === node.height) continue;
      node.moveAndResize({ x: node.x, y: node.y, width: node.width, height: targetH });
      changed = true;
    }
    if (changed) canvas.requestSave();
  }
  finishInsertNode(canvas, newNode, nearNode) {
    const forest = buildForest(canvas);
    const treeNode = findTreeForNode(forest, nearNode.id);
    if (treeNode) {
      let root = treeNode;
      while (root.parent) root = root.parent;
      if (this.settings.autoLayout) {
        this.layoutEngine.layoutChildren(canvas, root.canvasNode.id, /* @__PURE__ */ new Set([newNode.id]));
      }
    }
    if (this.settings.autoColor && this.isMindmapCanvas(canvas)) {
      this.branchColors.applyColors(canvas);
    }
    this.updateGroupBounds(canvas);
    this.canvasApi.selectAndEdit(canvas, newNode, this.settings.navigationZoomPadding);
  }
  showOutline(canvas, reveal = !import_obsidian9.Platform.isMobile) {
    const leaves = this.app.workspace.getLeavesOfType(OUTLINE_VIEW_TYPE);
    if (leaves.length > 0) {
      this.refreshOutline(canvas);
      if (reveal) void this.app.workspace.revealLeaf(leaves[0]);
      return;
    }
    const leaf = this.app.workspace.getRightLeaf(false);
    if (!leaf) return;
    void leaf.setViewState({ type: OUTLINE_VIEW_TYPE }).then(() => {
      if (reveal) void this.app.workspace.revealLeaf(leaf);
      if (!import_obsidian9.Platform.isMobile) this.reorderOutlineToTop(leaf);
      this.refreshOutline(canvas);
    });
  }
  hideOutline() {
    for (const leaf of this.app.workspace.getLeavesOfType(OUTLINE_VIEW_TYPE)) {
      leaf.detach();
    }
  }
  reorderOutlineToTop(leaf) {
    var _a;
    const parent = leaf.parent;
    if (!(parent == null ? void 0 : parent.children)) return;
    const children = parent.children;
    const idx = children.indexOf(leaf);
    if (idx > 0) {
      children.splice(idx, 1);
      children.unshift(leaf);
    }
    (_a = parent.selectTab) == null ? void 0 : _a.call(parent, leaf);
  }
  /**
   * Import a FreeMind .mm file and create a .canvas file.
   * @param folderPath Optional target folder; defaults to vault root.
   */
  importFreeMindFile(folderPath) {
    const input = createEl("input");
    input.type = "file";
    input.accept = ".mm";
    const handler = () => {
      var _a;
      input.removeEventListener("change", handler);
      const file = (_a = input.files) == null ? void 0 : _a[0];
      if (!file) return;
      void (async () => {
        const xml = await file.text();
        const canvasData = freemindToCanvas(xml, {
          nodeWidth: this.settings.defaultNodeWidth,
          nodeHeight: this.settings.defaultNodeHeight,
          maxNodeHeight: this.settings.maxNodeHeight,
          horizontalGap: this.settings.horizontalGap,
          verticalGap: this.settings.verticalGap
        });
        if (!canvasData) {
          new import_obsidian9.Notice(
            "Failed to parse .mm file. Make sure it is a valid mind map file."
          );
          return;
        }
        const baseName = file.name.replace(/\.mm$/i, "");
        const folder = folderPath ? folderPath + "/" : "";
        let canvasPath = `${folder}${baseName}.canvas`;
        let counter = 1;
        while (this.app.vault.getAbstractFileByPath(canvasPath)) {
          canvasPath = `${folder}${baseName} ${counter}.canvas`;
          counter++;
        }
        await this.app.vault.create(
          canvasPath,
          JSON.stringify(canvasData, null, "	")
        );
        const created = this.app.vault.getAbstractFileByPath(canvasPath);
        if (created instanceof import_obsidian9.TFile) {
          await this.app.workspace.getLeaf(false).openFile(created);
        }
        new import_obsidian9.Notice(
          `Imported "${file.name}" as "${canvasPath}"`
        );
      })();
    };
    input.addEventListener("change", handler);
    input.click();
  }
  isMindmapCanvas(canvas) {
    const data = canvas.getData();
    if (typeof data.mindmap === "boolean") return data.mindmap;
    return this.settings.defaultMindmapMode;
  }
  toggleMindmapMode(canvas) {
    var _a;
    const data = canvas.getData();
    const newValue = !this.isMindmapCanvas(canvas);
    data.mindmap = newValue;
    canvas.setData(data);
    canvas.requestSave();
    if (newValue && this.settings.autoColor) {
      this.branchColors.applyColors(canvas);
    }
    if (newValue) {
      this.showOutline(canvas);
    } else {
      this.hideOutline();
    }
    this.updateToggleButton(canvas);
    this.updateDragReparentButton(canvas);
    this.updateEnterTabButton(canvas);
    this.updateMobileActionsButton(canvas);
    (_a = this.mobileEditingBarHandle) == null ? void 0 : _a.refresh();
  }
  injectToggleButton(canvas) {
    if (this.toggleBtnEl) {
      this.toggleBtnEl.remove();
      this.toggleBtnEl = null;
    }
    if (this.dragReparentBtnEl) {
      this.dragReparentBtnEl.remove();
      this.dragReparentBtnEl = null;
    }
    if (this.enterTabBtnEl) {
      this.enterTabBtnEl.remove();
      this.enterTabBtnEl = null;
    }
    if (this.mobileActionsBtnEl) {
      this.mobileActionsBtnEl.remove();
      this.mobileActionsBtnEl = null;
    }
    const controls = canvas.view.containerEl.querySelector(".canvas-controls");
    if (!controls) return;
    const btn = controls.createEl("button", { attr: { type: "button" } });
    btn.addClass("cammvas-toggle-btn", "clickable-icon");
    btn.setAttribute("aria-label", "Toggle mindmap mode");
    this.registerDomEvent(btn, "click", (e) => {
      e.stopPropagation();
      this.toggleMindmapMode(canvas);
    });
    controls.prepend(btn);
    this.toggleBtnEl = btn;
    const dragBtn = controls.createEl("button", { attr: { type: "button" } });
    dragBtn.addClass("cammvas-toggle-btn", "cammvas-drag-reparent-btn", "clickable-icon");
    this.registerDomEvent(dragBtn, "click", (e) => {
      e.stopPropagation();
      if (!canvas.handleSelectionDrag) {
        new import_obsidian9.Notice("Drag to reparent is unavailable in this canvas version");
        return;
      }
      this.settings.dragToReparent = !this.settings.dragToReparent;
      this.updateDragReparentButton(canvas);
      void this.saveSettings();
    });
    btn.after(dragBtn);
    this.dragReparentBtnEl = dragBtn;
    if (import_obsidian9.Platform.isMobile) {
      const actionsBtn = controls.createEl("button", { attr: { type: "button" } });
      actionsBtn.addClass("cammvas-toggle-btn", "cammvas-mobile-actions-btn", "clickable-icon");
      actionsBtn.setAttribute("aria-label", "Mind map actions");
      (0, import_obsidian9.setIcon)(actionsBtn, "list-plus");
      this.registerDomEvent(actionsBtn, "click", (event) => {
        event.stopPropagation();
        this.showMobileActionsMenu(canvas, actionsBtn);
      });
      dragBtn.after(actionsBtn);
      this.mobileActionsBtnEl = actionsBtn;
    } else {
      const enterTabBtn = controls.createEl("button", { attr: { type: "button" } });
      enterTabBtn.addClass("cammvas-toggle-btn", "cammvas-enter-tab-btn", "clickable-icon");
      this.registerDomEvent(enterTabBtn, "click", (event) => {
        event.stopPropagation();
        this.settings.enterCreatesSibling = !this.settings.enterCreatesSibling;
        this.updateEnterTabButton(canvas);
        void this.saveSettings();
      });
      dragBtn.after(enterTabBtn);
      this.enterTabBtnEl = enterTabBtn;
    }
    this.updateToggleButton(canvas);
    this.updateDragReparentButton(canvas);
    this.updateEnterTabButton(canvas);
    this.updateMobileActionsButton(canvas);
  }
  showMobileActionsMenu(canvas, anchor) {
    const menu = new import_obsidian9.Menu();
    const selected = this.canvasApi.getSelectedNode(canvas);
    const isMindmap = this.isMindmapCanvas(canvas);
    menu.addItem((item) => item.setTitle("Create root node").setIcon("circle-plus").setDisabled(!isMindmap).onClick(() => this.createRootNode()));
    if (selected && isMindmap) {
      menu.addItem((item) => item.setTitle("Add child node").setIcon("corner-down-right").onClick(() => this.keyboardHandler.addChildNode(canvas, selected)));
      menu.addItem((item) => item.setTitle("Add sibling node").setIcon("list-plus").onClick(() => this.keyboardHandler.addSiblingNode(canvas, selected)));
      menu.addItem((item) => item.setTitle("Zoom to branch").setIcon("scan").onClick(() => this.navigation.zoomToBranch(canvas, selected)));
      menu.addItem((item) => item.setTitle("Re-layout selected branch").setIcon("list-tree").onClick(() => this.relayoutSelectedBranch(canvas, selected)));
    }
    menu.addItem((item) => item.setTitle("Re-layout mind map").setIcon("layout-template").setDisabled(!isMindmap).onClick(() => {
      this.layoutEngine.layout(canvas);
      this.updateGroupBounds(canvas);
    }));
    menu.addItem((item) => item.setTitle("Open map outline").setIcon("list-tree").onClick(() => this.showOutline(canvas, true)));
    const rect = anchor.getBoundingClientRect();
    menu.showAtPosition({ x: rect.left, y: rect.bottom });
  }
  updateToggleButton(canvas) {
    if (!this.toggleBtnEl) return;
    const isActive = this.isMindmapCanvas(canvas);
    this.toggleBtnEl.empty();
    (0, import_obsidian9.setIcon)(this.toggleBtnEl, isActive ? "network" : "layout-dashboard");
    this.toggleBtnEl.toggleClass("is-active", isActive);
    this.toggleBtnEl.setAttribute(
      "aria-label",
      isActive ? "Mindmap mode (active)" : "Mindmap mode (inactive)"
    );
  }
  updateDragReparentButton(canvas = this.canvasApi.getActiveCanvas()) {
    if (!this.dragReparentBtnEl) return;
    const mindmapEnabled = !!canvas && this.isMindmapCanvas(canvas);
    const controlEnabled = mindmapEnabled && !!canvas.handleSelectionDrag;
    const isActive = controlEnabled && this.settings.dragToReparent;
    this.dragReparentBtnEl.empty();
    (0, import_obsidian9.setIcon)(this.dragReparentBtnEl, "git-branch");
    this.dragReparentBtnEl.toggleClass("is-active", isActive);
    this.dragReparentBtnEl.toggleAttribute("disabled", !controlEnabled);
    this.dragReparentBtnEl.setAttribute("aria-disabled", String(!controlEnabled));
    this.dragReparentBtnEl.setAttribute(
      "aria-label",
      !mindmapEnabled ? "Drag to reparent (requires mindmap mode)" : isActive ? "Drag to reparent (active)" : "Drag to reparent (inactive)"
    );
  }
  updateEnterTabButton(canvas = this.canvasApi.getActiveCanvas()) {
    if (!this.enterTabBtnEl) return;
    const controlEnabled = !!canvas && this.isMindmapCanvas(canvas);
    const isActive = controlEnabled && this.settings.enterCreatesSibling;
    this.enterTabBtnEl.empty();
    (0, import_obsidian9.setIcon)(this.enterTabBtnEl, "keyboard");
    this.enterTabBtnEl.toggleClass("is-active", isActive);
    this.enterTabBtnEl.toggleAttribute("disabled", !controlEnabled);
    this.enterTabBtnEl.setAttribute("aria-disabled", String(!controlEnabled));
    this.enterTabBtnEl.setAttribute(
      "aria-label",
      !controlEnabled ? "Mind mapping Enter and Tab (requires mindmap mode)" : isActive ? "Mind mapping Enter and Tab (active)" : "Mind mapping Enter and Tab (inactive)"
    );
  }
  updateMobileActionsButton(canvas = this.canvasApi.getActiveCanvas()) {
    if (!this.mobileActionsBtnEl) return;
    const controlEnabled = !!canvas && this.isMindmapCanvas(canvas);
    this.mobileActionsBtnEl.toggleAttribute("disabled", !controlEnabled);
    this.mobileActionsBtnEl.setAttribute("aria-disabled", String(!controlEnabled));
    this.mobileActionsBtnEl.setAttribute(
      "aria-label",
      controlEnabled ? "Mind map actions" : "Mind map actions (requires mindmap mode)"
    );
  }
  relayoutSelectedBranch(canvas, branchParent) {
    var _a, _b;
    if (!this.isMindmapCanvas(canvas)) {
      new import_obsidian9.Notice("Enable mindmap mode before re-layout");
      return;
    }
    const node = branchParent != null ? branchParent : this.canvasApi.getSelectedNode(canvas);
    if (!node) {
      new import_obsidian9.Notice("Select a branch parent before re-layout");
      return;
    }
    if (this.canvasApi.getChildNodes(canvas, node).length === 0) {
      new import_obsidian9.Notice("The selected node has no child branch to re-layout");
      return;
    }
    (_a = this.autoResizeHandle) == null ? void 0 : _a.finalizeNode();
    this.layoutEngine.layoutChildren(canvas, node.id);
    this.updateGroupBounds(canvas);
    (_b = this.branchCollapseHandle) == null ? void 0 : _b.refresh();
  }
  /** Schedule a setTimeout that is automatically cancelled on unload/canvas switch. */
  trackedTimeout(win, callback, ms) {
    const pending = { id: 0, win };
    pending.id = win.setTimeout(() => {
      this.pendingTimers.delete(pending);
      callback();
    }, ms);
    this.pendingTimers.add(pending);
  }
  /** Schedule a requestAnimationFrame that is automatically cancelled on cleanup. */
  trackedRaf(win, callback) {
    const pending = { id: 0, win };
    pending.id = win.requestAnimationFrame(() => {
      this.pendingRafs.delete(pending);
      callback();
    });
    this.pendingRafs.add(pending);
  }
  /** Cancel all pending tracked timers, RAFs, and observers. */
  cancelPendingAsync() {
    for (const pending of this.pendingTimers) pending.win.clearTimeout(pending.id);
    this.pendingTimers.clear();
    for (const pending of this.pendingRafs) pending.win.cancelAnimationFrame(pending.id);
    this.pendingRafs.clear();
    for (const obs of this.pendingObservers) obs.disconnect();
    this.pendingObservers.clear();
  }
  /** Restore wrapped canvas methods to originals. */
  unwrapCanvasMethods() {
    if (this.interceptedCanvas) {
      if (this.origCanvasMethods.requestSave) {
        this.interceptedCanvas.requestSave = this.origCanvasMethods.requestSave;
      }
      if (this.origCanvasMethods.createGroupNode) {
        this.interceptedCanvas.createGroupNode = this.origCanvasMethods.createGroupNode;
      }
      if (this.origCanvasMethods.undo) {
        this.interceptedCanvas.undo = this.origCanvasMethods.undo;
      }
      if (this.origCanvasMethods.redo) {
        this.interceptedCanvas.redo = this.origCanvasMethods.redo;
      }
      if (this.origCanvasMethods.selectOnly) {
        this.interceptedCanvas.selectOnly = this.origCanvasMethods.selectOnly;
      }
    }
    this.interceptedCanvas = null;
    this.origCanvasMethods = {};
  }
  async loadSettings() {
    const data = await this.loadData();
    this.settings = normalizeSettings(data);
  }
  async saveSettings() {
    await this.saveData(this.settings);
    this.layoutEngine = new LayoutEngine({
      horizontalGap: this.settings.horizontalGap,
      verticalGap: this.settings.verticalGap,
      nodeWidth: this.settings.defaultNodeWidth,
      nodeHeight: this.settings.defaultNodeHeight
    });
    this.nodeOps = new NodeOperations(this.canvasApi, {
      nodeWidth: this.settings.defaultNodeWidth,
      nodeHeight: this.settings.defaultNodeHeight,
      horizontalGap: this.settings.horizontalGap,
      verticalGap: this.settings.verticalGap
    });
    this.branchColors = new BranchColors(
      this.canvasApi,
      this.settings.branchPalette,
      this.settings.colorLeafNodes
    );
    if (this.keyboardHandler) {
      this.keyboardHandler.nodeOps = this.nodeOps;
      this.keyboardHandler.layoutEngine = this.layoutEngine;
      this.keyboardHandler.branchColors = this.branchColors;
      this.keyboardHandler.zoomPadding = this.settings.navigationZoomPadding;
    }
    this.updateDragReparentButton();
    this.updateEnterTabButton();
    const canvas = this.canvasApi.getActiveCanvas();
    if (canvas && this.settings.autoColor && this.isMindmapCanvas(canvas)) {
      this.branchColors.applyColors(canvas);
    }
  }
  createRootNode() {
    var _a;
    const canvas = (_a = this.canvasApi) == null ? void 0 : _a.getActiveCanvas();
    if (!canvas) {
      new import_obsidian9.Notice("Open a canvas before creating a root node");
      return;
    }
    if (!this.isMindmapCanvas(canvas)) {
      new import_obsidian9.Notice("Enable mindmap mode before creating a root node");
      return;
    }
    const incomingIds = new Set(
      Array.from(canvas.edges.values(), (edge) => edge.to.node.id)
    );
    const groupIds = getGroupIds(canvas);
    const roots = Array.from(canvas.nodes.values()).filter(
      (node2) => !groupIds.has(node2.id) && !incomingIds.has(node2.id)
    );
    let x;
    let y;
    const previousRoot = roots[roots.length - 1];
    if (previousRoot) {
      const subtree = this.collectSubtreeNodes(canvas, previousRoot);
      const subtreeBottom = Math.max(...subtree.map((node2) => node2.y + node2.height));
      x = previousRoot.x;
      y = subtreeBottom + Math.max(40, this.settings.verticalGap * 2);
    } else {
      const rect = canvas.wrapperEl.getBoundingClientRect();
      const center = canvas.posFromEvt(new MouseEvent("mousemove", {
        clientX: rect.left + rect.width / 2,
        clientY: rect.top + rect.height / 2
      }));
      x = center.x - this.settings.defaultNodeWidth / 2;
      y = center.y - this.settings.defaultNodeHeight / 2;
    }
    const node = this.canvasApi.createTextNode(
      canvas,
      x,
      y,
      "",
      this.settings.defaultNodeWidth,
      this.settings.defaultNodeHeight
    );
    canvas.requestSave();
    this.canvasApi.selectAndEdit(canvas, node, this.settings.navigationZoomPadding);
  }
  registerBranchColorIcons() {
    const colors = {
      "1": "#e75545",
      "2": "#e9973f",
      "3": "#e0de71",
      "4": "#44cf6e",
      "5": "#53aaf5",
      "6": "#a882f7"
    };
    for (const [id, color] of Object.entries(colors)) {
      (0, import_obsidian9.addIcon)(`cammvas-color-${id}`, `<svg viewBox="0 0 24 24" fill="${color}"><circle cx="12" cy="12" r="8"/></svg>`);
    }
  }
};

/* nosourcemap */