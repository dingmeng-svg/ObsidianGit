---
title: "双节点协作规范"
date: 2026-06-30
version: 1.0
status: active
tags: [系统文档]
---

# 双节点协作规范

> **定位**：云端/本地双 Hermes Agent 的协作协议。事前预防为主，事后纠正为辅。
> **前提**：两个 Agent 共享文件系统（Syncthing 同步），但对话记录不互通。

---

## 一、分工协议

| 任务类型 | 执行节点 | 规则 |
|:---|:---|:---|
| 所有定时任务（cron） | **云端独占** | 本地停掉所有与云端重叠的 cron |
| A 股复盘播报 | **云端独占** | 每日 18:18 触发 |
| 数据采集 | **云端独占** | 同上 |
| Ingest 编译 | **本地** | 用户显式触发，交互式任务 |
| 模板维护 | **本地** | 需要用户在 Obsidian 中即时验证 |
| 文件整理/归档 | **本地** | 需要用户确认决策 |
| Lint 健康检查 | **本地** | 避免两个 Agent 同时扫描全库 |

**不设主从关系。** 两个 Agent 各司其职，无主次之分。云端故障时本地不接管复盘任务，本地故障时云端不接管 Ingest。


## 二、故障感知：本地产出物完整性检查

本地 Agent 启动时，或在每日 10:00 后首次收到用户消息时，执行只读检查：

1. 检查 T-1 日（昨日）的复盘文件是否存在于 `04_产出/股市数据播报/` 下
2. 不检查"当日"文件，因为当日复盘可能尚未到触发时间
3. 若 T-1 日和 T-2 日均无产出文件 → 触发告警：
   - 在 Worklog 中写入告警
   - 向用户发出提醒："云端复盘稿已连续 2 天未生成，请检查云端 Hermes 状态"

⚠️ 这是只读校验，不执行复盘任务。本地不接管云端职责。2 天窗口避免网络波动或休市误报。


## 三、写入纪律

1. **用户原话与 Agent 推断必须分离。**
   - 用户明确指令或陈述 → 可写入日记/笔记，标记 `source: user`
   - Agent 自身的观察/推断/建议 → **禁止写入用户日记正文或原子笔记正文**
2. **Agent 推断的合法出口：**
   - Worklog（`90_System/Worklogs/`）
   - 日记中的 Callout 区块：
     ```markdown
     > [!note] 🤖 Hermes 观察记录（待验证）
     > GLM-5.2 在 14:30-18:00 响应延迟显著高于 DS，未排除限流因素。
     ```
     <!-- FUTURE: 若 [!note] 语义污染成为实际问题，可在 v2.4 引入自定义 Callout 类型 [!hermes] + CSS snippet，届时需同步更新本规范和所有历史观察记录的格式 -->
3. **发现另一个 Agent 的可疑陈述时**，按 `Agent跨实例纠正工作流.md` 执行：
   - 先向用户求证，不直接修改或删除
   - 用户确认后修正源文件
   - 将正确事实写入 memory


## 四、信息共享

| 信息类型 | 存放位置 | 说明 |
|:---|:---|:---|
| 云端执行 cron 后的简报 | `90_System/Worklogs/YYYY-MM-DD.md` | 追加到当日 Worklog，使用固定格式（见 §五） |
| 本地发现的可疑内容 | 同上，使用标题 `## ⚠️ 跨实例待确认` | 归集到当日 Worklog |
| 定期同步状态 | `90_System/记忆交接快照/` | 已有机制，继续使用 |

**不新建独立通讯目录。**


## 五、云端简报格式

云端 Agent 完成 cron 任务后，追加到当日 Worklog：

```markdown
## 云端执行简报
- 任务：[任务名称]
- 触发时间：[ISO 时间]
- 完成时间：[ISO 时间]
- 状态：ok / error
- 产出文件：[文件路径]
- from: cloud
```


## 六、启动协议

本地 Agent 启动时，必须执行以下步骤：

1. 读取最近 24 小时的 Worklog，建立云端执行状态感知
2. 若发现云端已执行某任务，本地跳过该任务的待办检查
3. 执行产出物完整性检查（见第二章）
4. 读取最近一份记忆交接快照


## 七、相关文档

- [[Agent跨实例纠正工作流]] — 发现错误后的修正流程（事后补救）
- [[90_System/HERMES]] — AI 操作最高准则
- [[旧资产处置决策框架]] — 通用资产处置决策算法