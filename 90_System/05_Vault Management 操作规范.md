---
title: "05_Vault Management 操作规范"
date: 2026-07-11
status: active
tags:
  - 系统文档
aliases:
  - Vault Management SOP
related:
  - "[[90_System/HERMES]]"
  - "[[01_知识库统一规范总纲]]"
  - "[[02_原子笔记炼金术·终局版]]"
---

# 15_Vault Management 操作规范

> **地位声明**：本文件是 Hermes Agent 管理丁萌 Obsidian 知识库的操作规范。所有归档、校准、清理、维护操作均以此为准。
>
> **版本**：v1.0.0
> **生效日期**：2026-07-11
>
> **相关规范**：
> - [[HERMES]] — Agent 行为规范（权限分级、写入协议）
> - [[01_知识库统一规范总纲]] — 文件规范（命名、标签、目录、元数据）
> - [[02_原子笔记炼金术·终局版]] — 原子笔记生产方法论
> - [[07_YAML FrontMatter 规范]] — YAML 校准规范
>
> **⚠️ 关键铁律**：每次处理笔记归档前，必须先查阅 [[07_YAML FrontMatter 规范]]！

---

## 一、核心铁律

### 1.1 文件操作安全

| 铁律 | 说明 |
|:---|:---|
| **write_file 前备份** | 文件已存在且内容将变更时，先 `cp` 备份 |
| **rename 后全库查引用** | 重命名后必须全库搜索旧名，批量更新引用 |
| **删文件前必请示** | 任何删除操作都先问再执行 |
| **归档时只移动指定文件** | 🔴 禁止清空目标目录（2026-07-11 数据丢失事故） |
| **创建后必验证可用性** | 检查 CSS 污染、双链引用、插件依赖 |

### 1.2 用户沟通风格

- **命令式、直接执行**：用户给任务后直接执行，不确认中间步骤
- **每阶段汇报结果**：执行完一批操作后清单式汇总
- **不要问"要xxx吗？"**：用户说什么就做什么
- **"播完通知我"模式**：批量任务做完通知即可，不更新进度
- **审计报告不夸大问题**：区分 P0（需立即修复）与 P1/P2（不紧急）

---

## 二、版本号去外显化原则

> **核心原则**：系统文档版本号只出现在正文头部，不在文件名体现。无版本号的文件名 = 现行执行标准。

### 实现方法

| 位置 | 内容 |
|:---|:---|
| **文件名** | `01_知识库统一规范总纲.md`（去掉 `v3.0`） |
| **正文头部** | `> **版本**：v3.0.0` |
| **变更日志** | 版本号在变更摘要中记录 |
| **版本矩阵** | `SOP/系统文档版本矩阵.md` 统一管理 |
| **互链** | 使用无版本号文件名 `[[01_知识库统一规范总纲]]` |

---

## 三、原子笔记命名合规 SOP

### 3.1 NAMING-01 合规标准

**合规格式**：纯概念名，无后缀

| 状态 | 示例 |
|:---|:---|
| ✅ 合规 | `LLM Wiki.md`、`杀意未被转化的三种症状.md` |
| ❌ 冗余副标题 | `LLM Wiki：AI 驱动的知识库构建方法论.md` |
| ❌ 括号出处 | `蔡格尼克效应（心理学）.md` |

### 3.2 执行流程

```
步骤1：全库搜索引用
  grep -r "旧文件名" --include="*.md" | grep -v "_备份"

步骤2：重命名文件
  mv "旧路径/旧文件名.md" "旧路径/新文件名.md"

步骤3：更新文件内容
  - YAML title 字段
  - 正文 # 标题

步骤4：批量更新引用（Python）
  for root, dirs, files in os.walk(BASE):
      dirs[:] = [d for d in dirs if "_备份" not in d]
      for f in files:
          if f.endswith(".md"):
              # 替换旧名 → 新名

步骤5：验证无残留
  grep -r "旧文件名" --include="*.md" | grep -v "_备份" || echo "✅ 无残留"
```

---

## 四、文件归档操作规范

### 前置检查：项目结项萃取

在执行项目归档前，必须先完成结项萃取检查点。Agent 扫描项目目录并生成萃取建议清单，人类逐项决策后方可继续归档。

### 系统文档废弃流程

系统文档的废弃须执行前置依赖校验。详见 [[HERMES#系统文档废弃前置校验规则]]。

### 4.1 核心铁律

|| 铁律 | 说明 |
|:---|:---|
| **只移动指定文件** | 禁止清空目标目录 |
| **移动前检查目标** | 确认哪些需要保留 |
| **移动后验证完整性** | 原有 + 新增文件都存在 |

### 4.2 归档流程

```
步骤1：检查目标目录内容
  ls "目标目录/"

步骤2：列出需移动的文件
  避免误删目标目录已有内容

步骤3：执行移动
  mv "源路径/文件.md" "目标路径/"

步骤4：验证
  ls "目标路径/" | wc -l  # 文件数
```

**项目向导快照复用**：项目启动/结项向导同步复用 Ingest 流程的 Worklog 快照机制。
中断时在当月 Worklog 中记录 `【流程快照】`，恢复时读取最新快照从断点继续。

---

## 五、Git 版本控制

Vault 已纳入 Git 版本控制。`.gitignore` 排除规则禁止手动修改，新增排除目录统一通过 Hermes 指令追加。

- 查看历史：`git log --oneline`
- 恢复单文件：`git checkout [commit] -- [文件路径]`

### Git 初始化前置兼容要求

1. Vault 根目录路径禁止包含空格、中文、特殊符号
2. 已全局配置兼容中文文件名，无需重复执行
3. Windows 环境优先使用系统终端操作 Git 推送、克隆

### Agent 自动提交与推送规则

**触发时机**：

| 触发事件 | commit message 格式 |
|:---|:---|
| 完成一项系统架构升级执行指令 | `[系统] 简短描述` |
| 批量 Lint 修复、断链清理、标签规整 | `[批量] 操作类型 N篇` |
| Skill/文档废弃退役、解绑 | `[生命周期] 对象名 操作` |
| Ingest 大规模编译（≥ 10 篇素材） | `[Ingest] 编译 N 篇素材` |
| 人类手动指令「快照备份」 | `[快照] 人类指定的描述` |

统一 commit 完整格式：`[模块-操作类型] 变更简述 | 影响文件数`

**提交前预检**（Agent 自动执行）：

1. **P0 断链标记**（不阻断提交）：检查是否存在新增 P0 文件级/Skill 依赖断链。若存在，不阻断提交，在 commit message 尾部追加 `[含P0断链]` 标记，提醒人类闭环。

2. **违规废弃阻断**（阻断提交）：检查是否存在已标记 `deprecated`/`retired` 但仍被 `related_system_docs` 或 `dependent_skills` 字段引用的文档/Skill。若存在，判定为「未解绑依赖违规废弃」，直接阻断自动提交，输出完整依赖清单，全部解绑后方可生成快照。

   **阻断后清理**：若判定为违规废弃并阻断提交，Agent **必须自动执行 `git reset HEAD .`** 撤销暂存区，确保不污染下一次合法提交。清理完成后再输出依赖清单和阻断提示。

   **说明**：废弃主校验在修改 YAML 状态字段时执行，本项仅为兜底安全网，拦截并发、遗漏导致的违规废弃。

**push 失败降级处理**：
- 若 `git push` 失败（网络异常、凭证失效等），Agent **不阻断**本地 commit 和后续工作流
- 必须在对话中明确提示：「✅ 本地提交成功，❌ 远程推送失败：[失败原因]。本地数据已受版本控制保护，但异地容灾未完成。请稍后检查网络或凭证后手动执行 `git push`。」
- 同时将此次异常写入当月 Worklog 的「异常说明」区块
- 若远程仓库持续推送失败（连续 4 次），次月 Lint 标记 P2 格式警告：「远程仓库推送持续失败，异地容灾已中断，请检查」

**提交防抖备注**（仅执行约束，不修改正文）：
- 若连续触发多次提交事件（如批量操作分多批执行），Agent 在最后一次操作完成后 60 秒内合并为单次 commit，避免 Git 仓库产生大量碎片化微小提交。

### 321 异地多介质备份策略

| 3份数据副本 | 实现方案 | 自动化规则 |
|:---|:---|:---|
| 本地工作库 | 电脑 SSD 日常 Vault 目录 | 每次修改自动纳入 Git 本地版本 |
| 外置冷存储 | 外置移动硬盘/NAS HDD 完整镜像 | 每周 Hermes 自动提醒同步，不阻塞主流程 |
| 异地云端副本 | Git 私有远程仓库 | 每次本地 commit 完成自动 push 推送 |

| 原则 | 实现 |
|:---|:---|
| 2 种存储介质 | SSD（日常读写）+ HDD/NAS（冷备份） |
| 1 份异地副本 | 私有 Git 远程仓库，满足异地容灾需求 |

**外置硬盘同步容错约束**：
1. Hermes 持久记录外置硬盘固定挂载路径；每周同步前自动检测路径是否存在
2. 硬盘未挂载、盘符失效：仅在当月 Worklog 写入提醒记录，不阻断 Git 提交、推送全流程
3. 连续 4 周检测不到外置硬盘挂载：月度 Lint 标记 P2 格式警告，提醒人工检查存储介质

**超大媒体文件约束**：
大于 10MB 图片、视频、压缩包已在 `.gitignore` 排除，仅本地 + 外置硬盘双留存，不上传远程仓库，防止仓库体积膨胀。

**仓库体积维护备注**（仅执行约束）：
- 当 `.git` 目录总大小超过 500MB，季度健康度快照追加提醒
- 人类可本地执行 `git gc --aggressive` 压缩仓库，不自动执行

### 灾难恢复标准化 SOP

**核心原则**：灾难发生时，**优先从 Git 远程仓库恢复，绝不动用 `_备份` 目录**（除非 Git 也损坏）。恢复完成后必须执行双重校验，方可正常使用 Vault。

#### 场景 1：Vault 整体损毁（硬盘故障、设备丢失）

1. 在新设备安装 Obsidian + Git
2. 克隆远程完整仓库：`git clone <仓库地址>`
3. 手动恢复本地 Obsidian 配置文件
4. 通过 Obsidian 社区市场重新安装插件
5. 从外置硬盘拷贝所有 `.gitignore` 排除的超大媒体素材
6. 执行全库断链扫描，确认数据完整性

#### 场景 2：单文件误删/误改

1. 发送指令：「恢复文件 [文件路径]」
2. Agent 执行：`git checkout HEAD -- [文件路径]`
3. 若需恢复到更早版本：「查看 [文件路径] 的历史版本」→ Agent 列出最近 5 次涉及该文件的 commit → 人类选择恢复目标
4. 恢复后校验：该文件 YAML 可解析 + 双链完整

#### 场景 3：批量操作需要整体回滚

1. 发送指令：「回滚到上一个快照」→ Agent 执行：`git reset --hard HEAD~1`
2. 若需回滚到指定快照：「回滚到快照 [commit前6位]」
3. 恢复后强制校验：全库 YAML 格式校验 + 全库系统文档断链扫描

**恢复后强制校验**（所有场景通用）：
1. `vault-management` 全库 YAML 格式校验
2. 系统文档交叉断链扫描
3. 若存在异常 → 输出清单，人工逐项修复
4. 校验通过后，方可正常使用 Vault

**人工操作高危约束**（执行备注）：
- 禁止人类终端手动执行 `git reset --hard`、`git push --force` 等高危命令
- 所有回滚、恢复统一通过 Hermes 自然语言指令操作
- 若手动误操作破坏仓库，优先重新克隆远程仓库修复

**废弃/归档文件的版本留存**：

- 标记 `deprecated`、`retired`、`archived` 的文件**不物理删除**，仅修改 YAML 状态字段
- Git 历史永久保留其全部版本记录

**物理删除前置自动校验**：
- Agent 处理物理删除退役文件前，自动读取文件 YAML 中首次标记 `status: deprecated` 或 `status: retired` 的日期
- 计算标记日期距今的时间间隔
- 若不足 180 天 → **禁止删除**，输出：「[文件名] 标记退役仅 X 天，距 180 天观察期还有 Y 天，暂不可删除」
- 若超过 180 天 → 生成独立 commit：`[清理] 物理删除退役文件 [文件名] | 退役已满180天`，记录删除对象、标记日期、删除时间，然后执行删除

---

## 六、断链修复 SOP

### 6.1 执行流程

```
步骤1：用 find 查实际路径
  find . -name "*关键词*" -type f

步骤2：列诊断表格
  | 原链接 | 实际文件 | 操作建议 |

步骤3：根据数量决定
  - 断链 > 3 条 → 给清单让用户手动修
  - 断链 ≤ 3 条且路径确定 → 直接 patch
```

---

## 七、Worklog 位置规则

### 7.1 正式日志文件

| 规则 | 说明 |
|:---|:---|
| **位置** | `90_System/Worklogs/YYYY-MM/` 子目录 |
| **禁止** | 放在 `Worklogs/` 根目录 |
| **命名** | `YYYY-MM-DD.md` |

### 6.2 任务过程产物

| 规则 | 说明 |
|:---|:---|
| **位置** | `90_System/Worklogs/YYYY-MM/YYYY-MM-DD/` 子目录 |
| **示例** | `2026-07-10/Language Chunk处理/` |
| **整合后删除** | 避免内容重复 |

### 7.3 Worklog 双链规范

每个 Worklog 的 `related` 字段必须包含：
1. `[[Worklog MOC]]`
2. 前一天的 worklog
3. 后一天的 worklog

---

## 八、目录 MOC 索引规范

### 8.1 MOC 文件位置

| 目录 | MOC 文件路径 |
|:---|:---|
| `00_Inbox/` | `00_Inbox/00_Inbox.md` |
| `10_Projects/` | `10_Projects/10_Projects.md` |
| `20_Areas/` | `20_Areas/20_Areas.md` |
| `30_Resources/` | `30_Resources/30_Resources.md` |
| `40_Archive/` | `40_Archive/40_Archive.md` |
| `90_System/` | `90_System/90_System.md` |

### 8.2 MOC 标准结构

```
1. YAML frontmatter（title/date/tags/aliases）
2. 标题（带 emoji）
3. 简短说明（1-2 句）
4. 📝 最近更新（Dataview 动态列表）
5. 目录结构表格
6. 各子目录 Dataview 列表
7. 领域特定规则
8. 相关文档链接
```

### 7.3 MOC 放置注意事项

**MOC 不要放在 Dataview 查询的目标目录内**，否则 Dataview 会把 MOC 自己也抓进去。

---

## 九、YAML 规范（v3.0）

### 9.1 标签系统

| 类型 | 数量 | 说明 |
|:---|:---:|:---|
| **核心标签** | 9 个 | 个人成长、工作、生活、关系、财务、技术、心理学、知识管理、语言 |
| **细分标签** | 16 种 | 详见总纲附录 A |

### 9.2 status 合法值

```
draft / active / mature / deprecated / archived
```

### 9.3 原子笔记必填字段

```yaml
---
title: 概念名
date: YYYY-MM-DD
status: active
type: concept  # concept/model/practice/critique
tags: [领域]   # 只能从标签字典选
source: 核心来源
---
```

### 9.4 字段顺序

```
title → date → status → type → tags → project → source → author → aliases → related
```

---

## 十、系统文档编码规范

### 10.1 编码规则

| 规则 | 说明 |
|:---|:---|
| **从 01 开始连续分配** | 不预留空号 |
| **HERMES.md 不编码** | 保持原文件名不动 |
| **编码顺序** | 最高规范 → 配套文档 → 架构设计 → 操作指南 → 专项标准 |

### 10.2 什么不编码

| 类型 | 说明 |
|:---|:---|
| ❌ 临时任务产物 | P0 测试记录、清理清单、文件整理方案 |
| ❌ 改造前状态快照 | 移入 `知识库改造文档/` 或归档 |
| ✅ 正式系统规范 | 才编码 |

### 10.3 重命名后必须更新

- 全库双链引用（find + sed 批量替换）
- 架构文档中的目录结构图
- 权威文档索引表
- 正文中的文本引用

---

## 十一、习惯打卡双轨更新

### 11.1 文件位置

| 文件 | 路径 |
|:---|:---|
| **Habit Scorecard** | `20_Areas/22_个人成长/Habit Tracking/Habit Scorecard.md` |
| **BuJo Daily** | `20_Areas/22_个人成长/Bullet Journal/Daily/YYYY-MM/YYYY-MM-DD.md` |

### 11.2 执行流程（两处必须同时更新）

```
1. BuJo Daily 习惯打卡区：[ ] 改为 [x]
2. Habit Scorecard 周表：对应星期列打 ✓
```

### 11.3 周表规则

- 每个表格代表一周（一~日）
- 列对照：一=周一、二=周二、...、日=周日
- 更新前验证日期是星期几

---

## 十二、外部方法论与本系统关系

### 12.1 处理原则

当发现原子笔记记录了外部方法论来源（如 Karpathy 的 LLM Wiki），但本系统做出了不同选择时：

| 原则 | 说明 |
|:---|:---|
| **保留来源笔记** | 方法论来源是重要参考，不删除 |
| **添加关系声明** | 在笔记末尾明确"来源 vs 本系统选择"的分野 |
| **命名合规检查** | 确保文件名符合 NAMING-01 |

### 12.2 声明模板

```markdown
## 与本系统设计哲学的关系

> [!important] 方法论来源与实现路径的分野
> 
> 本文记录了 XXX 方法论，是本系统的设计思想来源之一。
> 但本系统在实际实现中走出了不同的哲学路径：
> 
> | 维度 | XXX 方法论 | 本系统 |
> |:---|:---|:---|
> | ... | ... | ... |
```

---

## 十三、附件与首页规范

### 13.1 附件存放规则

| 规则 | 说明 |
|:---|:---|
| **位置** | `90_System/92_Attachments/` |
| **命名** | `{来源缩写}_{原始ID}_{序号}.{ext}` |
| **链接** | 本地相对路径 `![](../90_System/92_Attachments/filename.ext)` |

### 13.2 首页 CSS 陷阱

| 陷阱 | 说明 |
|:---|:---|
| **HTML 块级元素内的 Wikilink 不被解析** | 这是 Obsidian 设计限制 |
| **解决方案** | 使用 Obsidian Callout（内部完全支持 Wikilink） |
| **目录链接** | 创建索引笔记（MOC），链接指向索引笔记 |

---

## 变更日志

| 版本 | 日期 | 变更内容 |
|:---|:---|:---|
| v1.0.0 | 2026-07-11 | 首次发布。从 `vault-management` Skill 提取核心操作规范，精简为系统文档。 |
| v1.1.0 | 2026-07-11 | 新增第五章「Git 版本控制」，包含自动提交规则、321 备份策略、灾难恢复 SOP、废弃文件版本留存规则。 |