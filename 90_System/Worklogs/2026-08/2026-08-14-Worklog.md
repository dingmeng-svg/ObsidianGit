---
date: 2026-08-14
type: worklog
status: active
tags:
  - Backup
  - 安全审计
  - Cron 修复
related:
  - "[[Worklog MOC]]"
  - "[[2026-08-14-Daily-Log]]"
---

# Worklog 2026-08-14

## AI Agent 全机安全审计

### 审计范围
扫描本机全部 AI Agent 相关组件：Skills、插件、MCP、Hook、脚本、网络监听、开机启动、配置文件。

### 发现与处理

| # | 问题 | 处理 | 结果 |
|---|------|------|------|
| 1 | MCP `ocr-siliconflow` / `vision-siliconflow` 硬编码 API Key | 2 个 index.js 改为 `process.env.SILICONFLOW_API_KEY` | ✅ |
| 2 | `.env` 缺少 `SILICONFLOW_API_KEY` | 追加到 .env 末尾 | ✅ |
| 3 | `config.yaml` vision `api_key` 明文 | 改为 `api_key_env: SILICONFLOW_API_KEY` | ✅ |
| 4 | `config.yaml` Kimi `api_key` 冗余明文（前一行已有 `api_key_env`） | 删除冗余行 | ✅ |
| 5 | 8 个 Cron job 全部 error（provider 漂移 guard 拦截） | pin 到 `sfkey-glm/glm-5.2` | ✅ |
| 6 | Git 备份 cron `no_agent=True` 路径 bug（Windows 反斜杠被剥离） | 改回 agent 模式 | ✅ |
| 7 | Typeless 开机自启（6 进程常驻+联网） | 删除注册表 `HKCU\Run\Typeless.exe` | ✅ |

### 未处理（保留现状）
- **千问输入法**（QianwenIME）：崩溃上报到 quark.cn 发送硬件指纹。属用户安装的输入法，保留。
- **Claude Code**：已安装但无活跃进程，凭据文件存在。暂不删除。
- **.env 权限**：仅当前用户 + SYSTEM + Administrators，合理。

## Cron 修复详情

| Job | 修复前 | 修复后 | 验证 |
|-----|--------|--------|------|
| Git 备份 | `no_agent=True` 路径 bug + `custom/Qwen3-14B-AWQ` | agent 模式 + `sfkey-glm/glm-5.2` | ✅ `8356259` 推送成功 |
| 每日洞察 | `zai/glm-5.2` provider drift | `sfkey-glm/glm-5.2` | ⏳ 待明晚调度 |
| Collector [T] | `zai/glm-5.2` | `sfkey-glm/glm-5.2` | ⏳ 待调度 |
| Weekly/Monthly/Quarterly/Annual Insight | `zai/glm-5.2` | `sfkey-glm/glm-5.2` | ⏳ 待调度 |
| Worklog 月度归档 | `zai/glm-5.2` | `sfkey-glm/glm-5.2` | ⏳ 待调度 |

## 残留观察
- **Git push `origin/master` 显示 ahead**：因 failover 脚本走 IP 直连，不更新本地跟踪引用。实际数据已推送，不影响功能。
- **MCP 修改后首次调用**：需重启 Hermes 才能加载新的 `process.env.SILICONFLOW_API_KEY`。当前会话内 MCP 可能仍用旧缓存。
