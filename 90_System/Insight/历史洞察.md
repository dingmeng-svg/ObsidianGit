---
title: "历史洞察"
tags: [洞察, 系统文档]
---

# 📂 历史洞察

```dataviewjs
const files = dv.pages('"90_System/Insight"')
  .where(p => p.file.name.match(/^\d{4}-\d{2}-\d{2}-Insight$/))
  .sort(p => p.file.day, "desc");
if (files.length > 0) {
  dv.list(files.map(p => p.file.link));
} else {
  dv.span("暂无历史洞察记录");
}
```